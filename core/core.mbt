// mmm core — markdown / mindmap / mirror.
//
// Markdown のテキストが唯一の真実で、木はそこから導いた見え方にすぎない。
// どちらのペインからの変更も、1 本の undo スタックに載るテキストの編集に
// なる。入口は api.mbt、読み取りは parser.mbt / doc.mbt、
// コマンドは add / move / cmds。
