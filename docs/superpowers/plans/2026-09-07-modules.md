# modules — 実装の段取り

決めは [2026-09-07-modules-design.md](../specs/2026-09-07-modules-design.md)。各段は 1 PR、main に squash。

## 段 1: core を 2 module に

- [x] `core/` に tree / view / op / read / map を残す。`edit/` は `op/edit.mbt`（view は型が衝突するので残した）
- [x] `app/`（module `mmm/app`）に web / parts / render / app / file / main / js を移す。moon.work の workspace で core を引く
- [x] package.json の `core` / `check:core` / `test:core` は workspace の根で回す。test/tools/pure.ts が core の moon.pkg を見る
- [x] vite / src / lab の import の道を `_build/js/release/build/mmm/app/js/js.js` に
- [x] docs/spec.md・docs/core.md の一覧

## 段 2: app の科

- [x] `bar/` — files / more / export / theme(+logo) / name（app.mbt から名乗りと未保存の印を切り出した）
- [x] `disk/` — io / handles / assets / dnd（改名は段 3）
- [x] `mindmap/` — render/ を改名し、mindmap.mbt を mindmap / input / act / marks に割った。export.mbt → svg.mbt
- [x] `parts/` に ask / asks / draw。hex_of / accent は parts/color.mbt（theme も draw も読む）
- [x] root — app / panes / shortcuts / link を平らに。`main/` は無くなった。persist は `prefs/`（bar が読むので root には置けない）。
      md_exts / is_markdown / bare は core/read/name.mbt へ（文書の名前の話）
- [x] docs

## 段 3: 改名

- [x] prefs / link / disk・Disk / recent・Recent / images・Images / drop、web の disk → fs
- [x] `spawn` を web に 1 つ（after / promise_of / settled も web/async.mbt に集めた）
- [x] `src/coreApi.ts` → `src/app.ts`、`src/map/highlight.ts` → `src/highlight.ts`
- [x] `#topbar` → `#bar`（index.html / style.css）
- [x] docs
