# ops — 操作を戻す 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 段 3〜5（構造・ドラッグ・カード・貼る/落とす/描く）を 1 枝で戻す。spec.md の挙動を Op に写す。

**Architecture:** 入口が何であれ `Intent` を作り `act` が実行する（段 2 の形）。キーは `keys.ts` の表、右クリックは `context.ts` の表、ドラッグの落とし先は `drop.ts` の純粋関数、貼り付けの判定は `paste.ts` の純粋関数。core に足すのは中身の地番と `Opaque` の送り口だけ。旧コード（c3de96b）から純粋な算術と器を戻し、古い core の呼び出しを Op に置き換える。

**Tech Stack:** MoonBit（core、Task 1 のみ）、TypeScript、CodeMirror 6、Node `node:test`。

**Spec:** `docs/superpowers/specs/2026-09-04-ops-design.md`

## Global Constraints

- 作業は worktree `.worktrees/feat/ops`（枝 `feat/ops`、feat/tree の d90ba33 から）。1 塊 1 コミット以上、最後に 1 PR で feat/tree に squash
- コミットは `<Type>: <Emoji> <Title>` の日本語。末尾に `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
- **Windows:** moon の前に `export PATH="$HOME/.moon/bin:$PATH"`。`.mbt` / `.ts` / `.css` は Write / Edit ツールで書く（Bash の heredoc は `\\` を潰す）。Python は無い。旧コードを戻すときは `git show c3de96b:<path> > <path>` を使ってよい（バックスラッシュが保たれる）
- TS: `as T` も `!` も置かない。コメントは日本語、画面の文字は英語。読みの規則（`#` / `-`）を TS で書き直さない — 「骨格があるか」は core に読ませる
- 宛先は anchor、動かすのは選択（段 2 の決め）。`Tab` / `Shift+Tab` だけ複数選択で段下げ・上げ
- できない操作は `failed`（core が edits 空・focus 無しで言ったとき）。キーの端は null で拾わない
- 旧コードを戻すとき、**古い core の呼び出し（`core.xxx(...)`、`NodeInfo`、`n.from/to/depth/parent/hidden`、`host.selection().has`）は全部 Op と今の型（`Box.node` / `Box.parent` / `Layout.order`）に置き換える**。`View` / `k` / `tx` は `Camera`
- レビューはこの段では core と純粋層（`select` / `keys` / `context` / `drop` / `card` の配置 / `paste`）だけ。配線は `pnpm run check` と煙試験
- ゲート: `pnpm run check && pnpm run check:core && pnpm run test:core && pnpm test`

---

## ファイル構成

```
core/tree/content.mbt        fill(b, content, from~, to~) — 中身の範囲を控える
core/tree/build.mbt          Frame.body_at / Build.body_at、Build.pend_from、number_blocks が id を振るとき chart に渡す、spans_sig
core/tree/fold.mbt           Piece の Capsule / Text に from~
core/tree/spans_wbtest.mbt   中身の行が指紋に入る
src/coreApi.ts               Content に opaque、encode の位置渡し
src/map/select.ts            neighbor / prevSibling / nextSibling / parentOf
src/map/keys.ts              Intent に keep / pick / editCard / link / code / draw / paste。A・C の行
src/map/context.ts           contextItems(L, sel, picked)（新規・純粋）
src/map/drop.ts              resolveDrop（戻す。group / edge を削る）
src/map/pick.ts              CardPick（戻す）
src/map/card.ts              cardPlacement + CardEditor（新規。旧 overlay.ts と mindmap の card 編集を合わせる）
src/app/paste.ts             decidePaste（新規。core に読ませる）
src/app/dnd.ts               initDrop（戻す）
src/app/draw.ts              showDrawing（戻す）
src/app/assets.ts            saveToDisk と道具（戻す。宣言の書き込みは外す）
src/mindmap.ts               contextmenu / 長押し / ドラッグ / カード / act の増分
src/main.ts                  apply(op, edit, keep) / picked / 貼り付け・投下・描く・保存
src/style.css                drop-line / dragging / drop-parent / card-pick / #card-editor
test/select.test.ts / keys.test.ts / context.test.ts / drop.test.ts / card.test.ts / paste.test.ts / coreApi.test.ts
```

---

### Task 1: core — 中身の地番、ts — Opaque の送り口

**Files:**
- Modify: `core/tree/content.mbt`（`fill`）、`core/tree/build.mbt`、`core/tree/fold.mbt`
- Modify: `core/tree/spans_wbtest.mbt`
- Modify: `src/coreApi.ts`、`test/coreApi.test.ts`

**Interfaces:**
- Produces: `survey(md)` の表に中身の id の行 `{ from, label: None, to }`。指紋 `spans_sig` に中身の行（例 `"# a\n\nhello\n"` → `1[0,-,0] 2[0,2,11] 3[5,-,11]`）。TS `Content` に `{ kind: "opaque"; text: string }`、`encode` が `["Opaque", text]` を出す

- [ ] **Step 1: 指紋の試験（RED）**

`core/tree/spans_wbtest.mbt` の「見出し 1 つ」の後ろに:

```moonbit
///|
/// 中身にも地番が在る。範囲はその塊の原文で、label は無い（行ではない）
test "中身の地番" {
  inspect(spans_sig("# a\n\nhello\n"), content="1[0,-,0] 2[0,2,11] 3[5,-,11]")
  inspect(
    spans_sig("intro\n\n# a\n\n![p](p.png)\n\n```\nx\n```\n"),
    content="1[0,-,7] 2[0,-,5] 3[7,-,32] 4[12,-,23] 5[25,-,32]",
  )
}
```

（2 例目の数字: `intro` は 0..5、doc の散文は frontmatter 無しで 0 から中身の尻 7 — 実測で `1[0,-,7]` でなければ、doc_id の行の `to` は `body_to` のままなので 5 か 7 のどちらかになる。**`--update` で埋めてから、`intro` の中身の行が `2[0,-,5]`、画像が `4[12,-,23]`、フェンスが `5[25,-,32]` であることを確かめる**。既存の指紋は中身の行が増えるので全部 `--update` → 目で確かめる: 中身の行はどれも `label` が `-`、範囲は元の塊）

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd core && moon test -p mmm-app/core/tree`
Expected: 「中身の地番」が Diff で FAIL（中身の行が無い）

- [ ] **Step 3: core**

`core/tree/content.mbt` の `fill` に `from~` を足し、範囲を控える:

```moonbit
fn fill(b : Build, content : Content, from~ : Int, to~ : Int) -> Unit {
  let block : Block = { id: 0, content }
  let at : Spot = { from, label: None, to }
  if b.stack.is_empty() {
    b.body.push(block)
    b.body_at.push(at)
    if to > b.body_to {
      b.body_to = to
    }
    return
  }
  let f = b.stack[b.stack.length() - 1]
  match f.skel {
    Line(body~, ..) => {
      body.push(block)
      f.body_at.push(at)
    }
    Gap => ()
  }
  if to > f.to {
    f.to = to
  }
}
```

`core/tree/build.mbt`:
- `Frame` に `body_at : Array[Spot]`（中身の範囲。`body` と並走）、`Build` に `body_at : Array[Spot]` と `mut pend_from : Int`（保留中の水平線の頭）。Frame / Build の構築子 3 か所に `body_at: []`、Build に `pend_from: 0`
- `spill` の `fill(b, ThematicBreak, to=b.pend_to)` → `fill(b, ThematicBreak, from=b.pend_from, to=b.pend_to)`。水平線を保留するところ（`b.pend = true; b.pend_to = span.to`）で `b.pend_from = span.from`
- `take` の `fill` 3 か所: 項目の中の見出し `fill(b, Opaque(verbatim(b.md, block)), from=span.from, to=span.to)`、項目の中の水平線 `from=span.from`、中身の塊 `from=s.from`
- `lay` の `Capsule(..., from~, to~)` → `fill(..., from~, to~)`、`Text(text, from~, to~)` → `fill(b, Opaque(text), from~, to~)`
- **番号と地番**: `number_blocks(body, next)` は id を振るだけなので、`chart` で中身の行を出す: `chart(f, spans)` の中で `for i, at in f.body_at { spans[f.body[i].id] = at }`（Frame の body は `Line(body~, ..)` から取る）。doc の散文も `build` で `for i, at in b.body_at { spans[b.body[i].id] = at }`
- `spans_sig` は変更なし（label None は `-`）

`core/tree/fold.mbt`: `Piece` の `Capsule` に `from~ : Int`、`Text(String, from~ : Int, to~ : Int)`。作る側: `capsule(md, spots, head, tail, from, upto)` は `from` を持っている → `from~` に入れる。`Text(carve(md, defs[d].from, defs[d].to), from=defs[d].from, to=eol_after(...))`。`lay` の match を合わせる

- [ ] **Step 4: 通す**

Run: `cd core && moon check && moon test -p mmm-app/core/tree --update && moon test -p mmm-app/core/tree && moon test -p mmm-app/core/edit -p mmm-app/core/op`
Expected: 全部 PASS。`git diff core/tree/spans_wbtest.mbt` を読み、中身の行が期待どおりか確かめる（Step 1 の注意）。edit / op の総当たりも通る（地番に行が増えても follow は label None を当てない）

- [ ] **Step 5: TS の Opaque**

`src/coreApi.ts` の `Content` に `| { kind: "opaque"; text: string }`（コメント: 読み解かない原文。View には来ない — その場編集が原文をそのまま書き戻すときに送る）。`content()` の decode には足さない（View に来ない）。`encode` の svg の行の隣に `if (kind === "opaque" && "text" in v) return [tag, v.text];`。

`test/coreApi.test.ts` の encode の試験に 1 行: `assert.deepEqual(encode({ kind: "setBlock", id: 3, content: { kind: "opaque", text: "x\n" } }), ["SetBlock", { id: 3, content: ["Opaque", "x\n"] }]);`

Run: `pnpm test && pnpm run check` → PASS

- [ ] **Step 6: コミット**

```bash
cd core && moon fmt tree && cd ..
git add core/tree src/coreApi.ts test/coreApi.test.ts
git commit -m "feat: ✨ 中身にも地番を振り、Opaque を ts から送れるようにする

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: A 構造 — 道具・キー・右クリック・長押し

**Files:**
- Modify: `src/map/select.ts`、`src/map/keys.ts`、`src/main.ts`、`src/mindmap.ts`
- Create: `src/map/context.ts`
- Test: `test/select.test.ts`、`test/keys.test.ts`、`test/context.test.ts`

**Interfaces:**
- Produces（select.ts）:
  ```ts
  export function parentOf(L: Layout, id: number): number | null
  export function prevSibling(L: Layout, id: number): number | null   // 同じ親（根なら根どうし）で文書順の 1 つ前
  export function nextSibling(L: Layout, id: number): number | null
  export function neighbor(L: Layout, ids: number[]): number | null    // 消した後に選ぶ隣
  ```
- Produces（keys.ts）:
  ```ts
  export type Intent =
    | { kind: "op"; op: core.Op; edit: boolean; keep?: number }
    | { kind: "edit"; id: number; seed: string | null }
    | { kind: "select"; sel: Selection; reveal: boolean }
    | { kind: "center" };
  ```
  `keyed(L, sel, k)` に Delete / Backspace / Alt+↑↓ / 複数 Tab・Shift+Tab / Shift+H
- Produces（context.ts）:
  ```ts
  export interface Item { label: string; key?: string; mark?: IconName; intent: Intent | null; why?: string; items?: Item[] }
  export type Entry = Item | "sep";
  export function contextItems(L: Layout, sel: Selection): Entry[]
  ```
- Produces（main / mindmap）: `apply(op, edit, keep?)`; `MapHost.apply(op, edit, keep?)`

- [ ] **Step 1: select の道具（RED → GREEN）**

`test/select.test.ts` に足す（`L` は既存: r(1) → a(2), b(3) → c(4)）:

```ts
test("兄弟と親 — 同じ親の文書順で前後。根の兄弟は根どうし", () => {
  assert.equal(parentOf(L, 2), 1);
  assert.equal(parentOf(L, 1), null);
  assert.equal(prevSibling(L, 3), 2);
  assert.equal(nextSibling(L, 2), 3);
  assert.equal(prevSibling(L, 2), null);
  assert.equal(nextSibling(L, 3), null);
  assert.equal(nextSibling(L, 4), null);
});

test("消した後の隣 — 次、無ければ前、無ければ親。消える部分木は隣に数えない", () => {
  assert.equal(neighbor(L, [2]), 3);
  assert.equal(neighbor(L, [3]), 2); // 3 の次は 4 だが 4 は消える部分木
  assert.equal(neighbor(L, [4]), 3);
  assert.equal(neighbor(L, [2, 3]), 1);
  assert.equal(neighbor(L, [1]), null);
});
```

`src/map/select.ts` に:

```ts
/** 親の id。根なら null */
export const parentOf = (L: Layout, id: number): number | null => L.boxes.get(id)?.parent?.id ?? null;

/** 同じ親の子（根なら根どうし）を文書順に */
const siblingsOf = (L: Layout, id: number): number[] => {
  const p = parentOf(L, id);
  return L.order.filter((x) => parentOf(L, x) === p);
};

export function prevSibling(L: Layout, id: number): number | null {
  const s = siblingsOf(L, id);
  const i = s.indexOf(id);
  return i > 0 ? s[i - 1] : null;
}

export function nextSibling(L: Layout, id: number): number | null {
  const s = siblingsOf(L, id);
  const i = s.indexOf(id);
  return i >= 0 && i < s.length - 1 ? s[i + 1] : null;
}

/** id の祖先に ids のどれかが居るか（= 一緒に消える） */
function under(L: Layout, id: number, ids: Set<number>): boolean {
  let cur: number | null = id;
  while (cur !== null) {
    if (ids.has(cur)) return true;
    cur = parentOf(L, cur);
  }
  return false;
}

/**
 * 消した後に選ぶ隣。消す並びの次の見えているノード、無ければ前、無ければ先頭の親。
 * 消える部分木（選んだものの子孫）は隣に数えない。無ければ null
 */
export function neighbor(L: Layout, ids: number[]): number | null {
  const gone = new Set(ids);
  const stays = L.order.filter((x) => !under(L, x, gone));
  const first = L.order.findIndex((x) => gone.has(x));
  const last = L.order.length - 1 - [...L.order].reverse().findIndex((x) => gone.has(x));
  if (first === -1) return null;
  const after = L.order.slice(last + 1).find((x) => stays.includes(x));
  if (after !== undefined) return after;
  const before = L.order.slice(0, first).reverse().find((x) => stays.includes(x));
  if (before !== undefined) return before;
  const p = parentOf(L, L.order[first]);
  return p !== null && stays.includes(p) ? p : null;
}
```

`neighbor(L, [3])` は 4 が 3 の子孫なので飛ばして 2（前）。試験と合うことを確かめる。

- [ ] **Step 2: keys の行（RED → GREEN）**

`test/keys.test.ts` に（`L`: r(2) → a(3), 空(4); `one`/`k` は既存）:

```ts
test("Delete は選択を消し、隣を keep する", () => {
  assert.deepEqual(keyed(L, one(3), k("Delete")), {
    kind: "op",
    op: { kind: "delete", ids: [3] },
    edit: false,
    keep: 4,
  });
  assert.deepEqual(keyed(L, { ids: [3, 4], anchor: 4 }, k("Backspace")), {
    kind: "op",
    op: { kind: "delete", ids: [3, 4] },
    edit: false,
    keep: 2,
  });
  assert.equal(keyed(L, NONE, k("Delete")), null);
});

test("Alt+↑↓ は塊を前の兄弟の前 / 次の兄弟の後ろへ。端では拾わない", () => {
  assert.deepEqual(keyed(L, one(4), k("ArrowUp", { alt: true })), {
    kind: "op",
    op: { kind: "moveNode", ids: [4], at: { kind: "before", node: 3 } },
    edit: false,
  });
  assert.deepEqual(keyed(L, one(3), k("ArrowDown", { alt: true })), {
    kind: "op",
    op: { kind: "moveNode", ids: [3], at: { kind: "after", node: 4 } },
    edit: false,
  });
  assert.equal(keyed(L, one(3), k("ArrowUp", { alt: true })), null);
  assert.equal(keyed(L, one(4), k("ArrowDown", { alt: true })), null);
});

test("複数選択の Tab は先頭の前の兄弟の子へ、Shift+Tab は先頭の親の後ろへ", () => {
  const M: Layout = layoutMap(
    [tree(node(2, "r", [node(3, "a"), node(4, "b", [node(5, "c"), node(6, "d")])]), ["Right", "Right"])],
    size,
  );
  assert.deepEqual(keyed(M, { ids: [5, 6], anchor: 6 }, k("Tab", { shift: true })), {
    kind: "op",
    op: { kind: "moveNode", ids: [5, 6], at: { kind: "after", node: 4 } },
    edit: false,
  });
  assert.deepEqual(keyed(M, { ids: [4, 6], anchor: 6 }, k("Tab")), {
    kind: "op",
    op: { kind: "moveNode", ids: [4, 6], at: { kind: "in", node: 3, side: null } },
    edit: false,
  });
  // 先頭 a(3) に前の兄弟は無い / 根の親は無い
  assert.equal(keyed(M, { ids: [3, 4], anchor: 4 }, k("Tab")), null);
  assert.equal(keyed(M, { ids: [2, 3], anchor: 3 }, k("Tab", { shift: true })), null);
});

test("Shift+H は畳む / 畳みを外す。Implicit と無選択は拾わない", () => {
  const F: Layout = layoutMap(
    [tree(node(2, "r", [{ id: 3, label: "a", fold: { open: false, summary: "a" }, blocks: [], children: [] }, node(4, null, [node(5, "x")])]), ["Right", "Right"])],
    size,
  );
  assert.deepEqual(keyed(F, one(2), k("H", { shift: true })), { kind: "op", op: { kind: "fold", id: 2, open: false }, edit: false });
  assert.deepEqual(keyed(F, one(3), k("H", { shift: true })), { kind: "op", op: { kind: "unfold", id: 3 }, edit: false });
  assert.equal(keyed(F, one(4), k("H", { shift: true })), null);
  assert.equal(keyed(F, NONE, k("H", { shift: true })), null);
});
```

（`node()` の helper は label に `null` を受ける。`fold` 付きのノードは literal で組む）

`src/map/keys.ts`:
- `Intent` の `op` に `keep?: number`
- `keyed` の頭の `if (k.alt) return null;` を、Alt+↑↓ の行の後ろへ移す（Alt を先に捨てると並べ替えが書けない）。Alt + 他のキーは null のまま
- 行を足す（順は Enter → Tab → Delete → Alt+↑↓ → Shift+H → Escape → Home → Mod+A → 矢印 → 字）:

```ts
  if ((k.key === "Delete" || k.key === "Backspace") && !k.mod) {
    if (sel.ids.length === 0) return null;
    const keep = neighbor(L, sel.ids);
    const o: Intent = { kind: "op", op: { kind: "delete", ids: sel.ids }, edit: false };
    return keep === null ? o : { ...o, keep };
  }
  if (k.alt && (k.key === "ArrowUp" || k.key === "ArrowDown") && !k.mod) {
    if (sel.ids.length === 0) return null;
    const first = sel.ids[0];
    const last = sel.ids[sel.ids.length - 1];
    const at: core.NodePlace | null =
      k.key === "ArrowUp"
        ? ((p) => (p === null ? null : { kind: "before", node: p }))(prevSibling(L, first))
        : ((n) => (n === null ? null : { kind: "after", node: n }))(nextSibling(L, last));
    return at === null ? null : { kind: "op", op: { kind: "moveNode", ids: sel.ids, at }, edit: false };
  }
  if (k.alt) return null;
```

複数選択の Tab（`solo` が null で ids が 2 つ以上）:

```ts
  if (k.key === "Tab" && !k.mod) {
    const id = solo(sel);
    if (id !== null) {
      return k.shift ? op({ kind: "wrap", id, label: "" }) : add({ kind: "in", node: id, side: null });
    }
    if (sel.ids.length < 2) return null;
    const first = sel.ids[0];
    if (k.shift) {
      const p = parentOf(L, first);
      return p === null ? null : { kind: "op", op: { kind: "moveNode", ids: sel.ids, at: { kind: "after", node: p } }, edit: false };
    }
    const prev = prevSibling(L, first);
    return prev === null ? null : { kind: "op", op: { kind: "moveNode", ids: sel.ids, at: { kind: "in", node: prev, side: null } }, edit: false };
  }
```

Shift+H（`k.key === "H" && k.shift && !k.mod`）: anchor が null → null。`L.boxes.get(anchor)?.node` の label が null（Implicit）→ null。`fold !== null` → `{ kind: "op", op: { kind: "unfold", id: anchor }, edit: false }`、else `fold` with `open: false`。

`sel.ids` は文書順（数の順）なので先頭・末尾は `[0]` / `[length-1]`。

- [ ] **Step 3: apply の keep**

`src/main.ts` の `apply`:

```ts
function apply(op: core.Op, edit: boolean, keep: number | null = null): void {
  // 消す前に隣を選んでおけば、目印が編集をまたいで追いかける（段 1）
  if (keep !== null) setSelection({ ids: [keep], anchor: keep }, false);
  const r = core.edit(text, op);
  …（以下そのまま）
}
```

`MapHost.apply(op, edit, keep?)`。`mindmap.ts` の `act` の `"op"`: `this.host.apply(intent.op, intent.edit, intent.keep ?? null)`。

- [ ] **Step 4: context.ts（RED → GREEN）**

`test/context.test.ts`:

```ts
// 右クリックの行。Intent の表で、沈む行は理由を持つ。
//
// 実行: pnpm test

import { test } from "node:test";
import assert from "node:assert/strict";
import type * as core from "../src/coreApi.ts";
import { type Entry, contextItems } from "../src/map/context.ts";
import { type Layout, type SizeOf, layoutMap } from "../src/map/layout.ts";

const size: SizeOf = () => ({ w: 100, h: 30 });
const node = (id: number, label: string | null, children: core.Node[] = [], fold: core.Fold | null = null): core.Node => ({ id, label, fold, blocks: [], children });
const tree = (n: core.Node, sides: core.Side[] = []): core.Tree => ({ node: n, sides });
const L: Layout = layoutMap([tree(node(2, "r", [node(3, "a", [], { open: false, summary: "a" }), node(4, null, [node(5, "x")])]), ["Right", "Right"])], size);

const labels = (es: Entry[]) => es.map((e) => (e === "sep" ? "—" : e.label));
const row = (es: Entry[], label: string) => {
  const e = es.find((x) => x !== "sep" && x.label === label);
  if (!e || e === "sep") throw new Error(`${label} が無い`);
  return e;
};

test("並び — Add / Rename / Hide / Flip side / Link / Code / Draw / Delete", () => {
  assert.deepEqual(labels(contextItems(L, { ids: [3], anchor: 3 })), [
    "Add", "Rename", "—", "Show (unfold)", "Flip side", "—", "Link", "Code", "Draw", "—", "Delete",
  ]);
});

test("Add は押せば子、開けば 4 つ", () => {
  const add = row(contextItems(L, { ids: [3], anchor: 3 }), "Add");
  assert.deepEqual(add.intent, { kind: "op", op: { kind: "addNode", at: { kind: "in", node: 3, side: null }, labels: [""] }, edit: true });
  assert.deepEqual(add.items?.map((i) => i.label), ["Child", "Below", "Above", "Parent"]);
});

test("複数選択では宛先が 1 つの行が沈む。Delete は沈まない", () => {
  const es = contextItems(L, { ids: [3, 5], anchor: 5 });
  assert.equal(row(es, "Add").intent, null);
  assert.equal(row(es, "Add").why, "Select one node");
  assert.equal(row(es, "Rename").why, "Select one node");
  assert.equal(row(es, "Link").why, "Select one node");
  assert.notEqual(row(es, "Delete").intent, null);
});

test("Implicit は畳めず、根は側を持たない", () => {
  assert.equal(row(contextItems(L, { ids: [4], anchor: 4 }), "Hide (fold)").why, "Nothing to fold here");
  assert.equal(row(contextItems(L, { ids: [2], anchor: 2 }), "Flip side").why, "The root has no side");
  assert.deepEqual(row(contextItems(L, { ids: [5], anchor: 5 }), "Flip side").intent, { kind: "op", op: { kind: "flipSide", id: 5 }, edit: false });
});
```

`src/map/context.ts`（Link / Code / Draw の Intent は Task 4 で足すので、ここでは `{ kind: "link" | "code" | "draw" }` を Intent に先に足しておく — 次の Step 5）:

```ts
// 右クリックメニューの行。何をするかは Intent で言い、沈む行は理由を持つ。
// キーの表（keys.ts）と同じ作り方を引き、意味を 2 か所に書かない。

import type * as core from "../coreApi.ts";
import type { IconName } from "../icons.ts";
import { type Intent, keyed } from "./keys.ts";
import type { Layout } from "./layout.ts";
import type { Selection } from "./select.ts";

export interface Item {
  label: string;
  key?: string;
  mark?: IconName;
  /** null なら沈む（why が理由） */
  intent: Intent | null;
  why?: string;
  items?: Item[];
}
export type Entry = Item | "sep";

const ONE = "Select one node";

const press = (L: Layout, sel: Selection, key: string, shift = false, mod = false): Intent | null =>
  keyed(L, sel, { key, shift, mod, alt: false });

export function contextItems(L: Layout, sel: Selection): Entry[] {
  const solo = sel.ids.length === 1 && sel.anchor !== null ? sel.anchor : null;
  const node = sel.anchor === null ? null : (L.boxes.get(sel.anchor)?.node ?? null);
  const one = (intent: Intent | null): Pick<Item, "intent" | "why"> =>
    solo === null ? { intent: null, why: ONE } : { intent };
  const folded = node?.fold !== null && node?.fold !== undefined;
  return [
    {
      label: "Add",
      key: "Tab",
      ...one(press(L, sel, "Tab")),
      items: [
        { label: "Child", key: "Tab", ...one(press(L, sel, "Tab")) },
        { label: "Below", key: "Enter", ...one(press(L, sel, "Enter")) },
        { label: "Above", key: "Shift+Enter", ...one(press(L, sel, "Enter", true)) },
        { label: "Parent", key: "Shift+Tab", ...one(press(L, sel, "Tab", true)) },
      ],
    },
    { label: "Rename", key: "Mod+Enter", mark: "pencil", ...one(press(L, sel, "Enter", false, true)) },
    "sep",
    {
      label: folded ? "Show (unfold)" : "Hide (fold)",
      key: "Shift+H",
      mark: folded ? "chevrons-up-down" : "chevrons-down-up",
      intent: node?.label === null || node === null ? null : press(L, sel, "H", true),
      ...(node !== null && node.label === null ? { why: "Nothing to fold here" } : {}),
    },
    {
      label: "Flip side",
      mark: "flip-horizontal",
      ...(sel.anchor === null || L.boxes.get(sel.anchor)?.parent === null
        ? { intent: null, why: "The root has no side" }
        : { intent: { kind: "op", op: { kind: "flipSide", id: sel.anchor }, edit: false } }),
    },
    "sep",
    { label: "Link", key: "Shift+L", mark: "link", ...one(solo === null ? null : { kind: "link", id: solo }) },
    { label: "Code", key: "Shift+C", mark: "code", ...one(solo === null ? null : { kind: "code", id: solo }) },
    { label: "Draw", key: "Shift+D", mark: "paintbrush", ...one(solo === null ? null : { kind: "draw", id: solo }) },
    "sep",
    { label: "Delete", key: "Del", mark: "trash-2", intent: press(L, sel, "Delete") },
  ];
}
```

**「Below」の Enter は空ラベルなら編集になる**（段 2 の規則）。メニューの Below は常に「足す」なので、`press` でなく直接 `{ kind: "op", op: { kind: "addNode", at: { kind: "after", node: solo }, labels: [""] }, edit: true }` にする。Above も同じく `before`。試験の期待に合わせる。`IconName` は `src/icons.ts` の export を確かめて合わせる（無ければ `string`）。

- [ ] **Step 5: Intent の増分**

`src/map/keys.ts` の `Intent` に:

```ts
  /** クリップボードの URL をリンクカードにして題を打つ / 空のコードを足して打つ / 描いて貼る（Task 4・5 が実行） */
  | { kind: "link"; id: number }
  | { kind: "code"; id: number }
  | { kind: "draw"; id: number }
```

`keyed` に `Shift+L` / `Shift+C` / `Shift+D`（`k.shift && !k.mod && key === "L" | "C" | "D"`、anchor 1 つ → その Intent。Enter の前に置く）。試験 3 行を `test/keys.test.ts` に足す。`mindmap.ts` の `act` は当面 `case "link": case "code": case "draw": return;`（Task 4・5 で埋める）。

- [ ] **Step 6: 右クリックと長押しの配線（mindmap.ts）**

- `import { ContextMenu, type MenuEntry } from "./map/menu.ts"`、`import { contextItems, type Entry } from "./map/context.ts"`
- 欄 `private menu = new ContextMenu();`、`private hold: ReturnType<typeof setTimeout> | null = null;`、`private holdAt: { x: number; y: number } | null = null;`、`private menuOpenedByHold = false;`
- `toEntries(es: Entry[]): MenuEntry[]`: `"sep"` はそのまま、Item は `{ label, key, mark, run: () => this.act(intent), disabled: intent === null && why, items: items?.map(...) }`（`menu.ts` の `MenuEntry` の形に合わせる。`disabled` が文字列なら理由）
- `openMenu(x, y)`: `const id = this.nodeAt(x, y); if (id === null) { this.menu.hide(); return; } const sel = this.host.selection(); if (!sel.ids.includes(id)) this.host.setSelection({ ids: [id], anchor: id }, false); this.menu.show(x, y, this.toEntries(contextItems(this.layout, this.host.selection())));`
- `pane.addEventListener("contextmenu", (e) => { e.preventDefault(); if (this.menuOpenedByHold) { this.menuOpenedByHold = false; return; } this.dropHold(); this.openMenu(e.clientX, e.clientY); })`
- 長押し: touch の `pointerdown`（`tapped` を立てる枝）で `this.holdAt = {x, y}; this.hold = setTimeout(() => { const at = this.holdAt; this.dropHold(); if (!at) return; this.tapped = null; this.menuOpenedByHold = true; this.openMenu(at.x, at.y); }, HOLD_MS)`。`HOLD_MS = 500`。`pointermove` で slop を越えたら `dropHold()`、`end` の頭で `dropHold()`。`dropHold()` は timer を消して `holdAt = null`
- `act` の `"op"` に keep

- [ ] **Step 7: 通す・コミット**

Run: `pnpm test && pnpm run check`
Expected: PASS

```bash
git add src/map/select.ts src/map/keys.ts src/map/context.ts src/main.ts src/mindmap.ts test/select.test.ts test/keys.test.ts test/context.test.ts
git commit -m "feat: ✨ 構造 — 消す・並べ替え・段下げ・畳み・側、右クリックと長押し

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: B ドラッグ — 落とし先の算術と配線

**Files:**
- Create: `src/map/drop.ts`（c3de96b から戻して削る）、`test/drop.test.ts`
- Modify: `src/mindmap.ts`、`src/style.css`

**Interfaces:**
```ts
export type Drop = { kind: "node"; id: number; pos: 0 | 1 | 2 } | { kind: "side"; root: number; left: boolean };
export interface DropScene { at: Pt; layout: Layout; dragging: Set<number> }
export function resolveDrop(scene: DropScene): Drop | null
export function dropOp(drop: Drop, ids: number[]): core.Op   // Drop → MoveNode
```

- [ ] **Step 1: 戻して削る**

`git show c3de96b:src/map/drop.ts > src/map/drop.ts` の後:
- `DropTarget.pos` は `0 | 1 | 2`（3 = 線への割り込みを消す）。`Drop` の `group` を消す。`DropScene` は `{ at, layout, dragging }` に（`order` / `boxes` / `parentOf` は `layout` から: `parentOf(id)` = `layout.boxes.get(id)?.parent?.id`。`single` / `preferEdge` / `newGroup` / `polyline` は消す）。`DropDecision` の包みは要らない — `Drop | null` を返す
- `nearestRoot` / `findSlot` / `findEdge` を丸ごと消す。`resolveDrop` の中の `newGroup` の分岐、`edge` / `edgeTarget` / `shiftWon` と `target = edgeTarget` を消し、外側ゾーンの調停は `if (outTarget && best > 0 && (outU <= NEAR || !target)) target = outTarget;` に
- `local(at, b)` の `dirOf(b.n)` → `dirOf(b.parent?.side ?? "Right")`（geometry.ts の `dirOf(side)`）。`b.n.depth === 1` → `b.parent === null`。`b.n.left` → `b.parent?.side === "Left"`。`b.n.id` → `b.node.id`
- `import { type Box, GAP } from "./layout.ts"; import { dirOf, type Pt, centerOf } from "./geometry.ts";`（`distToSeg` は消えた `findEdge` のもの）
- 末尾に:
```ts
/** 落とし先を Op に。側は根のものなので In(root, side) で言う */
export function dropOp(drop: Drop, ids: number[]): core.Op {
  if (drop.kind === "side") return { kind: "moveNode", ids, at: { kind: "in", node: drop.root, side: drop.left ? "Left" : "Right" } };
  if (drop.pos === 0) return { kind: "moveNode", ids, at: { kind: "in", node: drop.id, side: null } };
  return { kind: "moveNode", ids, at: { kind: drop.pos === 1 ? "before" : "after", node: drop.id } };
}
```

- [ ] **Step 2: 試験（新規）**

`test/drop.test.ts` — `layoutMap` で r(1) → a(2), b(3) を組み（size 100×30）、`GAP` を使って:
- a の箱の中心 → `{ kind: "node", id: 2, pos: 0 }`
- a の箱の上の帯（`y = a.y - 10`） → `pos: 1`、下の帯 → `pos: 2`
- a の右の外側ゾーン（`x = a.x + a.w + 10`、y は中心） → `pos: 0`
- 根の右外側（子が居る側）で `x = r.x + r.w + 10`、y は根の中心 → `{ kind: "side", root: 1, left: false }`（旧コードどおり、根の外側ゾーンは子の隣を決めず側に落ちる）。根の箱の中（中心より右） → 同じく `side` で `left: false`
- 根の左外側（子が居ない側） → `{ kind: "side", root: 1, left: true }`
- 遠い空所 → null
- `dragging = {2}` のとき a の中心 → 2 は候補外（b か null）
- `dropOp` の 3 形

期待値は式から数える（`GAP.x`、箱 100×30、a は `(100+GAP.x, 0)`、b は `(100+GAP.x, 40)`、r は y=20）。当たらなければ `resolveDrop` の定数（SLOP / BAND / REACH / SLACK / NEAR）を読んで期待値を直す — アルゴリズムは旧コードのまま。

- [ ] **Step 3: 配線（mindmap.ts）**

- 欄: `private dragCand: { id: number; x: number; y: number } | null`、`private dragging: { ids: number[]; subtree: Set<number> } | null`、`private drop: Drop | null`、印の要素 `dropLine`（`svgEl("line", { id: "drop-line", visibility: "hidden" })` を world に）
- マウスの `pointerdown` でノードに当たったとき: 選択の更新は今のまま、加えて `this.dragCand = { id, x: e.clientX, y: e.clientY }` と `pane.setPointerCapture`
- `pointermove`: `dragCand` があり slop（8px）を越えたら `startDrag()`: ids = 選択に居れば選択の全部（文書順）、居なければ単独で選び直してそれ。`subtree` = ids とその子孫（`Layout.boxes` の `parent` を辿る）。掴んだ箱に `dragging` クラス（`renderer.nodeEl(id)`）。以後 `pointermove` で `this.drop = resolveDrop({ at: world, layout, dragging: subtree })` と予告（`showDrop()`）
- 予告: `node, 0` / `side` → 落ちる先の箱に `drop-parent` クラス、`pos 1 / 2` → その箱の上端 / 下端に `drop-line`。前の印は消す
- `end`（pointerup）: `dragging` なら `drop` があれば `this.host.apply(dropOp(drop, ids), false, null)`、印を消す。`pointercancel` と `Escape`（keydown、ドラッグ中）は印を消して何もしない
- 指: `tapped` の枝で slop を越えたら同じく `startDrag()`（長押しのメニューが開いていれば始めない）

- [ ] **Step 4: CSS**

`.node` の塊に `&.dragging { opacity: 0.35; }` と `&.drop-parent rect.box { stroke: var(--accent); stroke-width: 2; stroke-dasharray: 5 4; }`。`#drop-line { stroke: var(--accent); stroke-width: 3; stroke-linecap: round; pointer-events: none; }`。

- [ ] **Step 5: 通す・コミット**

Run: `pnpm test && pnpm run check`

```bash
git add src/map/drop.ts test/drop.test.ts src/mindmap.ts src/style.css
git commit -m "feat: ✨ ドラッグ — 掴んで子・兄弟・側へ落とす

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: C カード — 選ぶ・キー・×・その場編集・Link / Code

**Files:**
- Create: `src/map/pick.ts`（戻す）、`src/map/card.ts`、`test/card.test.ts`
- Modify: `src/map/keys.ts`、`src/main.ts`、`src/mindmap.ts`、`src/style.css`、`test/keys.test.ts`

**Interfaces:**
- `keys.ts`: `keyedCard(L, picked: number, k: Key): Intent | null`（カードを選んでいるときの表。`Intent` に `{ kind: "pick"; id: number | null }` と `{ kind: "editCard"; id: number }` を足す）
- `card.ts`: `cardPlacement(rect, cam, text: { lines, widest }) -> Placement`、`CardEditor`（`open(id, rect, cam, text, from?, to?)` / `place` / `close`; 閉じるとき `commit(id, text)`）
- `main.ts`: `picked: number | null`。`setPicked(id)` はノードの選択を外し、md 側を薄塗り（中身の地番）。`setSelection` は `picked = null`
- `MapHost`: `picked(): number | null; setPicked(id: number | null): void; blockText(id): string`（地番で md から切り出す）

- [ ] **Step 1: keyedCard の試験（RED → GREEN）**

`test/keys.test.ts` に。Layout の blocks は `node()` の helper を拡張して `blocks: core.Block[]` を渡せるようにし、r(2) に blocks [{ id: 3, content: { kind: "thematicBreak" } }, { id: 4, content: { kind: "code", info: "", text: "x" } }]:

- `keyedCard(L, 3, k("Delete"))` → `{ kind: "op", op: { kind: "delete", ids: [3] }, edit: false, keep: 2 }`（keep は持ち主のノード）
- `k("ArrowDown")` → `{ kind: "pick", id: 4 }`、4 で `ArrowDown` → null（端）、`ArrowUp` で 3 → null
- `k("ArrowLeft")` → `{ kind: "select", sel: { ids: [2], anchor: 2 }, reveal: false }`
- `k("ArrowUp", { alt: true })` on 4 → `{ kind: "op", op: { kind: "moveBlock", ids: [4], at: { kind: "before", block: 3 } }, edit: false }`、3 で Alt+↑ → null
- `k("Enter", { mod: true })` → `{ kind: "editCard", id: 3 }`
- `k("Escape")` → `{ kind: "pick", id: null }`

持ち主と隣は `Layout` から: 箱の `node.blocks` に id が居るノードが持ち主。

- [ ] **Step 2: cardPlacement の試験（RED → GREEN）**

旧 `overlay.ts` の `cardPlacement` を `card.ts` に（`View` → `Camera`、`CODE_LINE` は `metrics.ts` から）。試験は `labelPlacement` と同じ作り（k=1 / k=2 / 中身が増えたら伸びる）。

- [ ] **Step 3: CardEditor と pick**

`git show c3de96b:src/map/pick.ts > src/map/pick.ts`。`CardRef` は無い — `show(id: number, rect: Rect | null)` にし、`data-kill` は id。
`card.ts` の `CardEditor`: 旧 mindmap の `editBox` / `editInk` / `cardEditor` と `paintEditInk` / `positionCardEditor` を器にまとめる。`tokenizeBlock` は `map/highlight.ts`。閉じるとき（Esc / Mod+Enter / blur）に `commit(id, value)` を 1 回。値が変わっていなければ呼ばない。`Mod+Z` は欄のもの（段 2 の shortcuts の guard が textarea も見ている）。

- [ ] **Step 4: 配線**

- `main.ts`: `picked`。`setPicked(id)`: `selection = NONE; ghosts = []; picked = id; map.refreshSelection(); editor.highlight(picked の地番)`。`setSelection` の頭で `picked = null`。sync の後、`picked` の id が `spots` に無ければ null。`host.blockText(id)` = `text.slice(spots.get(id).from, .to)`。`act` の増分: `pick` → `setPicked`、`editCard` → `map.editCard(id)`、`link` / `code` → Task 5 と一緒に main へ（`host.act` を通して main が実行）
- `mindmap.ts`: `click` でカード（`cardAt(x, y, "data-card")` → id は `data-card` の `"node,index"` から `boxes.get(node).node.blocks[index].id`）→ `host.setPicked(同じなら null)`。`dblclick` → `editCard`。× は `data-kill`。`keydown` の頭で `picked !== null` なら `keyedCard` に流し、null なら（矢印以外は）ノードの表へ。`render` 後に `pick.show(picked, cardRect)` と `CardEditor.place`
- `editCard(id)`: `rect = cardRect(box, index)`、`text = host.blockText(id)`、開く。閉じるときの `commit` → `host.apply({ kind: "setBlock", id, content: { kind: "opaque", text } }, false, null)`
- CSS: 旧 `#card-pick` / `.card-picked` / `.card-kill` / `#card-editor` / `.card-ink` を戻す

- [ ] **Step 5: Link / Code（main.ts の act）**

`link`: `navigator.clipboard.readText()` → `^https?://\S+$` でなければ `failed("Couldn't read that as a link")` → `apply({ kind: "addBlock", at: { kind: "in", node: id }, content: { kind: "link", text: "", href: url, title: "" } }, false, null)` → focus は中身の id → `setPicked(focus)` → `map.editCard(focus, 1, 1)`（題の位置）。`code`: `addBlock` with `{ kind: "code", info: "", text: "" }` → `editCard(focus, 4, 4)`（`` ```\n `` の後ろ）。`apply` は focus を返すように（戻り値 `number | null`）。

- [ ] **Step 6: 通す・コミット**

```bash
git add src/map/pick.ts src/map/card.ts src/map/keys.ts src/main.ts src/mindmap.ts src/style.css test/card.test.ts test/keys.test.ts
git commit -m "feat: ✨ カード — 選ぶ・消す・並べ替え・その場編集、Link と Code を足す

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: D 貼る・落とす・描く

**Files:**
- Modify: `src/app/assets.ts`（`saveToDisk` と道具を戻す）、`src/main.ts`、`src/map/keys.ts`
- Create: `src/app/paste.ts`、`test/paste.test.ts`、`src/app/dnd.ts`（戻す）、`src/app/draw.ts`（戻す）

- [ ] **Step 1: paste の判定（RED → GREEN）**

```ts
export type Paste =
  | { kind: "noop" }
  | { kind: "link"; url: string }
  | { kind: "labels"; labels: string[] }
  | { kind: "md"; md: string };
/** 骨格（見出し・項目）があるかは core に読ませる（TS で `#` を見ない） */
export function decidePaste(clip: string, hasSkeleton: (md: string) => boolean): Paste
```

規則: CRLF → LF。空 → noop。1 行で `^https?://\S+$` → link。`hasSkeleton(md)` が偽 → 空でない行の trim を labels。真 → md。試験は `hasSkeleton` に手で書いた関数を渡す。main では `hasSkeleton = (md) => core.view(md)…` — `coreApi.view` は消えているので `core.survey(md, [], []).view.trees.length > 0`。

- [ ] **Step 2: saveToDisk を戻す**

`git show c3de96b:src/app/assets.ts` から `saveToDisk` と `nameParts` / `nameProblem` / `taken` / `nestedFile` / `webp` / `ensureBinding` / `declaredPath` / `forget` / `mdPath` を今の `assets.ts` へ移す。`deps.declare(...)` の呼び出しと、宣言を打つ `folder` の欄は消す（`shape` は `["![](", declaredPath(), name, ".webp)"]`）。`Assets` の interface に `saveToDisk(blob: Blob): Promise<string | null>`。`initAssets` の deps に足りないもの（`ensurePlace` は main が持つので、`saveToDisk` の頭で `deps.place()` を待つ形に）。

- [ ] **Step 3: main の配線**

- `Mod+V`: `keys.ts` に `{ kind: "paste" }`（`k.mod && key.toLowerCase() === "v"`、anchor 無しでも拾う）。main: クリップボードに画像があれば `attachImage(anchor, blob)`。無ければ `decidePaste` → link は `addBlock(Link)`、labels は `addNode(In(anchor ?? DOC_ID), labels)`（edit false）、md は `graft(In(anchor ?? DOC_ID), md)`
- `attachImage(id, blob)`: `await ensurePlace()` → `assets.saveToDisk(blob)` → `apply({ kind: "addBlock", at: { kind: "in", node: id }, content: { kind: "image", alt: "", src: rel, title: "" } }, false, null)`
- `draw`: `git show c3de96b:src/app/draw.ts > src/app/draw.ts`。`act` の `draw` → `showDrawing()` → blob → `attachImage`
- `dnd`: `git show c3de96b:src/app/dnd.ts > src/app/dnd.ts`。`initDrop({ markDrop: (at) => map.markFileDrop(at), openMarkdown, addImages })`。`map.markFileDrop(at)` は `nodeAt` で id を返し、`drop-parent` の印を出す/消す（Task 3 の印を使う）。`-1` の代わりに `null` を返す形に直す

- [ ] **Step 4: 通す・コミット**

```bash
git add src/app src/main.ts src/map/keys.ts test/paste.test.ts test/keys.test.ts
git commit -m "feat: ✨ 貼る・落とす・描く — Mod+V、ファイルの投下、Shift+D、画像の保存

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: docs → 煙試験 → PR

- [ ] `docs/spec.md`「構成」: `map/` に context / drop / pick / card、`app/` に paste / dnd / draw、assets に「読む・置く」、mindmap / main の説明。`docs/core.md`「地番」に中身の行。`docs/superpowers/specs/2026-09-04-ops-design.md` の signature を実装に合わせる
- [ ] 全ゲート。煙試験（controller）: spec の「試験」の並び
- [ ] push、PR（feat/tree 向け）
