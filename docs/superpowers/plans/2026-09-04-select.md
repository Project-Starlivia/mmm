# select — 選択 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 地図で選べるようにし、md の打鍵をまたいでも同じノードを指し続け、md のカーソルと地図の選択を互いに映す。操作は 1 つも無い。

**Architecture:** 同一性は core（地番にラベルの頭を足し、`follow` が前のサイクルの目印を編集列で写して今の id を答える）。選択は TS の値（main.ts が 1 つ持ち、`map/select.ts` の純粋関数で動かし、地図は塗るだけ）。打鍵ごとの core 呼び出しは `mmmSurvey` 1 回で、View・地番・引き直した id を一度に返す。

**Tech Stack:** MoonBit（`moon 0.1.20260803`、target js）、TypeScript、CodeMirror 6（`@codemirror/state` の ChangeSet を編集列に直して渡す）、Node の `node:test`。

**Spec:** `docs/superpowers/specs/2026-09-04-select-design.md`

## Global Constraints

- 作業は worktree `.worktrees/feat/select`（枝 `feat/select`、feat/tree から）。1 段 1 コミット。最後に 1 PR で feat/tree に squash
- コミットは `<Type>: <Emoji> <Title>` の日本語。末尾に `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
- **Windows:** moon を呼ぶ前に PATH を前置する（Bash: `export PATH="$HOME/.moon/bin:$PATH"`）。`.mbt` と `\n` を含む `.ts` は Write / Edit ツールで書く（Bash の heredoc は `\\` を潰す）。Python は無い
- MoonBit の罠: 局所関数は定義順（相互再帰は不可）。match の腕に `guard` だけは置けない。`try?` は deprecated（`try … catch { e => … }`）。`Json::object` / `Json::array`。`inspect(x, content="")` を `moon test --update` で埋めると多行形になる
- core のテスト: `cd core; moon test -p mmm-app/core/tree`。整形: `moon fmt tree view op`。型: `moon check`。TS: `pnpm test`（core を組んでから走る）、`pnpm run check`、`pnpm run check:core`
- **型は名乗らせず確かめる。** TS に `as T` も `!` も置かない（spec.md「構成」）
- 画面に出る文字は英語。docs とコメントは日本語
- **読みの規則を TS で書き直さない。** ラベルの頭（マーカーの読み飛ばし）は core の地番が言う
- 反映（`reflect.mbt`）の振る舞いは変えない。地番の型が変わるだけ

---

## ファイル構成

```
core/tree/build.mbt          Spot { from, label?, to }。Frame に label、put に label~、item_start、chart、spans_sig
core/tree/reflect.mbt        Map[Int, @markdown.Span] → Map[Int, Spot]（3 か所）。Edit に FromJson
core/tree/follow.mbt         Mark / Trail / follow（新規）
core/tree/follow_wbtest.mbt  spec の表の行を 1 つずつ（新規）
core/tree/spans_wbtest.mbt   指紋を id[from,label,to] に
core/tree/reflect_law_wbtest.mbt  law の引数の型
core/tree/moon.pkg           moonbitlang/core/json を import（FromJson の導出に要る）
core/tree/js/exports.mbt     mmmSurvey
core/tree/js/moon.pkg        moonbitlang/core/json を import
src/coreApi.ts               Spot / Edit / Mark / Trail / Survey と survey()、decodeSurvey()
src/caret.ts                 caretIds(view, spots, ranges)（新規）
src/map/select.ts            Selection と click / rubber / hit / arrow / extend / all / NONE / depthOf（新規）
src/editor.ts                onChange(text, edits) / onCaret / highlight / reveal / caret
src/map/render.ts            paintSelection(ids)
src/mindmap.ts               叩く・矩形・キーを値にして host へ。selected と輪を塗る。Space+ドラッグでパン
src/main.ts                  選択と幽霊（当たっていない目印）を持つ。sync(text, edits) → survey → 引き直し
src/style.css                .node.selected / #rubber / .caret-ring
test/select.test.ts          純粋層（新規）
test/caret.test.ts           最深・閉じ際・Implicit（新規）
test/coreApi.test.ts         survey の JSON の形と往復
docs/spec.md, docs/core.md   構成と地番
```

---

### Task 1: 地番にラベルの頭を足す（Spot）

**Files:**
- Modify: `core/tree/build.mbt`（Frame / put / items_take / Heading の take / chart / survey / build / spans_sig）
- Modify: `core/tree/reflect.mbt:26,50,63`（引数の型）、`core/tree/reflect_law_wbtest.mbt:141`
- Modify: `core/tree/moon.pkg`
- Test: `core/tree/spans_wbtest.mbt`

**Interfaces:**
- Produces: `pub(all) struct Spot { from : Int, label : Int?, to : Int }`、`survey(md) -> (Doc, Map[Int, Spot])`、指紋 `id[from,label,to]`（label が None なら `-`）

- [ ] **Step 1: spans_wbtest の期待値を新しい指紋にする（RED）**

`core/tree/spans_wbtest.mbt` の頭の説明と、次の 5 つの test の `content` を差し替える（他の test は Step 5 で `--update`）。

```moonbit
// 「このノードは原文のここに書かれている」を指紋 `id[from,label,to]` で固定する。
//
// 範囲は骨格の行の頭から自分の最後の中身の尻まで。label はラベルの頭（マーカーの
// 後ろ）で、行を持たない Implicit と文書の散文（`1`）は `-`。空行と側の水平線は
// どのノードのものでもない（隙間）。

///|
test "見出し 1 つ" {
  inspect(spans_sig("# a\n"), content="1[0,-,0] 2[0,2,4]")
}

///|
test "親は子の尻まで、子は自分の中身の尻まで" {
  inspect(
    spans_sig("# r\n\n## a\n\nhello\n\n## b\n"),
    content=(
      #|1[0,-,0] 2[0,2,23] 3[5,8,17] 5[18,21,23]
    ),
  )
}

///|
test "末尾の空行はどのノードのものでもない" {
  inspect(spans_sig("# a\n\n\n"), content="1[0,-,0] 2[0,2,4]")
}

///|
test "Implicit は子の和。行が無いのでラベルの頭も無い" {
  inspect(spans_sig("# r\n\n### x\n"), content="1[0,-,0] 2[0,2,11] 3[5,-,11] 4[5,9,11]")
}

///|
test "側の水平線は隙間" {
  inspect(
    spans_sig("# r\n\n## a\n\n---\n\n## b\n"),
    content="1[0,-,0] 2[0,2,21] 3[5,8,10] 4[16,19,21]",
  )
}
```

ファイルの末尾に項目の test を足す（`to` は Step 5 で埋める。`from` と `label` は下の値でなければならない — 入れ子の項目の `from` はマーカーの位置、`label` はその 2 つ後ろ）。

```moonbit
///|
/// 項目のラベルの頭はマーカーの後ろ。入れ子の項目は容器の字下げが範囲の外に残る
/// ので from はマーカーの位置
test "項目のラベルの頭はマーカーの後ろ" {
  inspect(spans_sig("- a\n  - b\n"), content="")
}
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd core && moon test -p mmm-app/core/tree`
Expected: spans_wbtest の 5 つが `1[0,0] 2[0,4]` 形との Diff で FAIL（`spans_sig` の型は今のまま通る）

- [ ] **Step 3: Spot と Frame.label を置く**

`core/tree/build.mbt`:

Frame に欄を足す（`mut to : Int` の直後）:

```moonbit
  /// ラベルの頭（マーカーの後ろ）。飛び（Gap）は行を持たないので -1
  mut label : Int
```

`survey` の直前に Spot を置く:

```moonbit
///|
/// 地番。ノードが原文のどこに書かれているか。`label` はラベルの頭（マーカーの後ろ）で、
/// 選択が md の打鍵をまたいで同じノードを指し続けるための目印（follow.mbt）。
/// Implicit と文書の散文は行を持たないので None。
pub(all) struct Spot {
  from : Int
  label : Int?
  to : Int
} derive(Eq, Debug, ToJson)
```

`survey` / `build` / `chart` の `Map[Int, @markdown.Span]` を `Map[Int, Spot]` にし、`build` の文書の散文の行を:

```moonbit
  spans[doc_id] = {
    from,
    label: None,
    to: if b.body_to > from { b.body_to } else { from },
  }
```

`chart` の最後の行を:

```moonbit
  spans[f.id] = {
    from: f.from,
    label: if f.label < 0 { None } else { Some(f.label) },
    to: f.to,
  }
```

`spans_sig` の 1 行を:

```moonbit
      Some(s) => {
        let label = match s.label {
          Some(l) => l.to_string()
          None => "-"
        }
        "\{id}[\{s.from},\{label},\{s.to}]"
      }
```

`put` に `label~` を足す（署名と、Gap の枠と、座る枠の 2 か所）:

```moonbit
fn put(
  b : Build,
  level : Int,
  skel : Notation,
  at~ : @markdown.Span,
  label~ : Int,
) -> Unit {
```

Gap の枠の `to: -1,` の後ろに `label: -1,`、座る枠 `f` の `to: at.to,` の後ろに `label,` を足す。

`take` の Heading の腕（`put(` の呼び出し）に `label=head_start(b.md, span, style),` を足す:

```moonbit
    Heading(level~, style~, children~, span~, ..) =>
      put(
        b,
        level,
        Line(
          sign=Heading,
          label=head_label(b.md, span, style, children),
          fold~,
          body=[],
        ),
        at=span,
        label=head_start(b.md, span, style),
      )
```

`items_take` の項目 1 つぶんの頭に `label_at` を置き、段落があればその頭、無ければマーカーを読み飛ばした位置:

```moonbit
    let rest = []
    let mut label = ""
    // ラベルの頭。1 行目が段落ならその頭（段落の span は中身の先頭から始まる）。
    // 段落が無ければマーカーを読み飛ばした位置
    let mut label_at = item_start(b.md, it.span)
    // ラベルの範囲に入った定義行はラベルが持っている。残りは項目の中身
    let mut from = it.span.from
    for i, c in it.children {
      if i == 0 && c is Paragraph(..) {
        label = item_label(b.md, c)
        label_at = block_span(c).from
        from = block_span(c).to
      } else {
        rest.push(c)
      }
    }
    put(b, level, Line(sign=Item, label~, fold~, body=[]), at=it.span, label=label_at)
```

`head_start` の隣に `item_start` を足す:

```moonbit
///|
/// 項目のラベルの頭。マーカー（`-` `*` `+`、または数字と `.` `)`）と空白を読み飛ばす。
/// 1 行目が段落ならその span の頭と一致する。段落の無い項目のために持つ。
fn item_start(md : String, span : @markdown.Span) -> Int {
  let mut p = skip_blank(md, span.from, span.to)
  while p < span.to && md[p] >= '0' && md[p] <= '9' {
    p = p + 1
  }
  if p < span.to &&
    (md[p] == '-' || md[p] == '*' || md[p] == '+' || md[p] == '.' || md[p] == ')') {
    p = p + 1
  }
  skip_blank(md, p, span.to)
}
```

`core/tree/reflect.mbt` の 3 か所（`reflect` の引数、`Reflect` の欄、`plan` の引数）と `core/tree/reflect_law_wbtest.mbt:141` の `Map[Int, @markdown.Span]` を `Map[Int, Spot]` に。

`core/tree/moon.pkg` に `"moonbitlang/core/json",` を import の先頭に足す（Task 2 の FromJson に要る。ここで足しておく）。

- [ ] **Step 4: 通ることを確かめる**

Run: `cd core && moon check && moon test -p mmm-app/core/tree`
Expected: `moon check` 0 errors。spans_wbtest の 5 つが PASS、項目の test だけ `content=""` との Diff で FAIL。他は全部 PASS（reflect と law は型が変わっただけ）

- [ ] **Step 5: 残りの指紋を埋め、目で確かめる**

Run: `cd core && moon test -p mmm-app/core/tree --update && moon test -p mmm-app/core/tree`
Expected: 全部 PASS

`git diff core/tree/spans_wbtest.mbt` を読み、埋まった値を確かめる: 項目の test は `2[0,2,…] 3[6,8,…]`（`to` はライブラリの値のまま）。畳み（`<details>`）の test があれば、`from` は `<details>` の頭のまま、`label` は `## ` の後ろ。Implicit の行は `-`。1 つでも違えば Step 3 の `label_at` / `head_start` を疑う。

- [ ] **Step 6: 整形してコミット**

```bash
cd core && moon fmt tree && cd ..
git add core/tree
git commit -m "feat: ✨ 地番にラベルの頭を足す（Spot）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: follow — 目印を編集列で写す

**Files:**
- Create: `core/tree/follow.mbt`
- Modify: `core/tree/reflect.mbt`（`Edit` に FromJson）
- Test: `core/tree/follow_wbtest.mbt`

**Interfaces:**
- Consumes: `Spot`、`survey`、`apply(md, edits)`（reflect.mbt）、`Edit { from, to, insert }`
- Produces: `pub(all) struct Mark { from : Int, label : Int }`、`pub(all) struct Trail { mark : Mark, id : Int? }`、`pub fn follow(spots : Map[Int, Spot], edits : Array[Edit], marks : Array[Mark]) -> Array[Trail?]`

- [ ] **Step 1: 法則の表を test にする（RED）**

`core/tree/follow_wbtest.mbt`:

```moonbit
// 選択の持ち越し（follow）を、spec の表の行ごとに固定する。
// 入力が md と編集列なので、ここは読みに依存する（位置の話だから）。

///|
/// id のノードの目印（前のサイクルの地番から抜く）。
fn mark_of(md : String, id : Int) -> Mark {
  let (_, spots) = survey(md)
  let s = spots.get(id).unwrap()
  { from: s.from, label: s.label.unwrap() }
}

///|
/// md に edits を当てて読み直し、ids の目印がいまどの id かを並べる。
fn followed(md : String, edits : Array[Edit], ids : Array[Int]) -> Array[Int?] {
  let (_, spots) = survey(apply(md, edits))
  follow(spots, edits, ids.map(id => mark_of(md, id)))
  .map(t => t.bind(x => x.id))
}

///|
test "上にノードを足しても同じノード" {
  // "# r\n\n## a\n" の a は 3。5 に "## n\n\n" を差すと n が 3 になり、a は 4
  assert_eq(
    followed("# r\n\n## a\n", [{ from: 5, to: 5, insert: "## n\n\n" }], [3]),
    [Some(4)],
  )
}

///|
test "頭に # を足す・項目を字下げする" {
  assert_eq(followed("## a\n", [{ from: 0, to: 0, insert: "#" }], [2]), [Some(2)])
  // b が a の子になるが、文書順は変わらないので id も 3 のまま
  assert_eq(
    followed("- a\n- b\n", [{ from: 4, to: 4, insert: "  " }], [3]),
    [Some(3)],
  )
}

///|
test "行ごと消せば死ぬ。後ろのノードは残る" {
  // a = 3、b = 4。a の行 "## a\n\n" を消すと b が 3
  assert_eq(
    followed("# r\n\n## a\n\n## b\n", [{ from: 5, to: 11, insert: "" }], [3, 4]),
    [None, Some(3)],
  )
}

///|
test "マーカーの一部が残れば生きる。丸ごと消えれば死ぬ" {
  assert_eq(followed("## a\n", [{ from: 0, to: 1, insert: "" }], [2]), [Some(2)])
  assert_eq(followed("## a\n", [{ from: 0, to: 3, insert: "" }], [2]), [None])
}

///|
test "ラベルを消して打ち直しても生きる。行をまるごと打ち直せば死ぬ" {
  assert_eq(followed("## a\n", [{ from: 3, to: 4, insert: "" }], [2]), [Some(2)])
  assert_eq(followed("## a\n", [{ from: 3, to: 4, insert: "b" }], [2]), [Some(2)])
  assert_eq(followed("## a\n", [{ from: 0, to: 4, insert: "## b" }], [2]), [None])
}

///|
test "ラベルの頭が消去の内側に落ちれば死ぬ" {
  assert_eq(followed("## a\n", [{ from: 1, to: 4, insert: "" }], [2]), [None])
}

///|
/// 頭にカーソルを置いて `## n` を打ち、それから Enter。打った瞬間は 1 つの見出し
/// `## n## a` で当たらないが、目印は黙って持ち越し、Enter で戻る。
test "途中のサイクルでは黙って持ち越し、次で戻る" {
  let md = "# r\n\n## a\n"
  let m = mark_of(md, 3)
  assert_eq(m, { from: 5, label: 8 })
  let e1 : Array[Edit] = [{ from: 5, to: 5, insert: "## n" }]
  let md1 = apply(md, e1)
  let (_, s1) = survey(md1)
  let t1 = follow(s1, e1, [m])
  assert_eq(t1, [Some({ mark: { from: 5, label: 12 }, id: None })])
  let e2 : Array[Edit] = [{ from: 9, to: 9, insert: "\n\n" }]
  let (_, s2) = survey(apply(md1, e2))
  // 当たったら目印は今の地番のもの（from も新しくなる）
  assert_eq(
    follow(s2, e2, [t1[0].unwrap().mark]),
    [Some({ mark: { from: 11, label: 14 }, id: Some(4) })],
  )
}

///|
test "目印が無ければ何も返らない。編集が無ければそのまま当たる" {
  assert_eq(followed("# a\n", [], []), [])
  assert_eq(followed("# a\n", [], [2]), [Some(2)])
}
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd core && moon test -p mmm-app/core/tree`
Expected: `Mark` / `follow` が無い、で compile error

- [ ] **Step 3: follow を書く**

`core/tree/reflect.mbt` の `Edit` の derive に `FromJson` を足す:

```moonbit
} derive(Eq, Debug, ToJson, FromJson)
```

`core/tree/follow.mbt`:

```moonbit
// 選択が md の打鍵をまたいで同じノードを指し続けるための、同一性の引き直し。
//
// 前のサイクルの目印（マーカーの範囲とラベルの頭）を編集列で写し、いまの地番で
// ラベルの頭が一致するノードを答える。id は読みのサイクルを越えて持たないので、
// 持ち越すのは位置。決めは docs/superpowers/specs/2026-09-04-select-design.md。
//
// core は状態を持たない — 前のサイクルの目印は ts が渡す。

///|
/// 前のサイクルの目印。from はマーカーの頭（骨格の行の頭）、label はラベルの頭。
pub(all) struct Mark {
  from : Int
  label : Int
} derive(Eq, Debug, ToJson, FromJson)

///|
/// 写した目印と、いま当たるノード。当たらなくても目印は返す（黙って持ち越す）。
pub(all) struct Trail {
  mark : Mark
  id : Int?
} derive(Eq, Debug, ToJson)

///|
/// 目印ごとに、編集の後でどこに居るか。None は死んだ — マーカーが丸ごと消されたか、
/// ラベルの頭が消去の内側に落ちた。当たったら目印は今の地番のものに置き換える。
pub fn follow(
  spots : Map[Int, Spot],
  edits : Array[Edit],
  marks : Array[Mark],
) -> Array[Trail?] {
  marks.map(m => match carry(edits, m) {
    Some(mark) => {
      let mut hit : Int? = None
      for id, s in spots {
        if s.label == Some(mark.label) {
          hit = Some(id)
        }
      }
      match hit {
        Some(id) => {
          let s = spots.get(id).unwrap()
          Some({ mark: { from: s.from, label: mark.label }, id: Some(id) })
        }
        None => Some({ mark, id: None })
      }
    }
    None => None
  })
}

///|
/// 目印を編集列で写す。編集は前の座標で from 順、重ならない。
fn carry(edits : Array[Edit], m : Mark) -> Mark? {
  let mut from = m.from
  let mut label = m.label
  for e in edits {
    let deleting = e.to > e.from
    // マーカーが丸ごと消えた
    if deleting && e.from <= m.from && e.to >= m.label {
      return None
    }
    // ラベルの頭が消去の内側
    if e.from < m.label && m.label < e.to {
      return None
    }
    from = shift(from, e)
    label = shift(label, e)
  }
  Some({ from, label })
}

///|
/// 位置 1 つを編集 1 つで写す。手前の編集ぶんずれる。**ちょうどその位置への挿入は
/// 動かさない**（挿入の前に留まる — ラベルの頭に字を足せば、その字がラベルの先頭）。
/// その位置で終わる消去はずらす（`##` を 1 つ消せばラベルは 1 つ手前）。
/// 消去の内側なら消去の頭へ。
fn shift(pos : Int, e : Edit) -> Int {
  let grown = e.insert.length() - (e.to - e.from)
  if e.to < pos || (e.to == pos && e.to > e.from) {
    pos + grown
  } else if e.from < pos && pos < e.to {
    e.from + e.insert.length()
  } else {
    pos
  }
}
```

- [ ] **Step 4: 通ることを確かめる**

Run: `cd core && moon check && moon test -p mmm-app/core/tree`
Expected: 全部 PASS。落ちたら、その行の期待値を紙で数え直す（位置は 0 始まり、`\n` も 1 文字）。`e.to == pos && e.to > e.from` の分岐が「`##` の 1 つ消し」を、`e.from < m.label && m.label < e.to` が「ラベルの頭が内側」を受け持つ

- [ ] **Step 5: 整形してコミット**

```bash
cd core && moon fmt tree && cd ..
git add core/tree
git commit -m "feat: ✨ 目印を編集列で写して今の id を答える（follow）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: mmmSurvey と coreApi.survey — 1 回の parse で View・地番・引き直し

**Files:**
- Modify: `core/tree/js/exports.mbt`、`core/tree/js/moon.pkg`
- Modify: `src/coreApi.ts`
- Test: `test/coreApi.test.ts`

**Interfaces:**
- Consumes: `@tree.survey`、`@tree.follow`、`@view.project`、`@tree.Edit` / `@tree.Mark`（FromJson）
- Produces（TS）:
  ```ts
  export interface Spot { from: number; label: number | null; to: number }
  export interface Edit { from: number; to: number; insert: string }
  export interface Mark { from: number; label: number }
  export interface Trail { mark: Mark; id: number | null }
  export interface Survey { view: View; spots: Map<number, Spot>; trails: (Trail | null)[] }
  export const survey: (md: string, edits: Edit[], marks: Mark[]) => Survey
  export function decodeSurvey(json: unknown): Survey
  ```

- [ ] **Step 1: TS の試験を書く（RED）**

`test/coreApi.test.ts` の import を `import { decode, decodeSurvey, survey } from "../src/coreApi.ts";` にし、末尾に足す:

```ts
test("survey の JSON — spots の鍵は数になり、label の無い鍵は null、trails の null はそのまま", () => {
  const s = decodeSurvey({
    view: { trees: [] },
    spots: { "1": { from: 0, to: 0 }, "2": { from: 0, label: 2, to: 4 } },
    trails: [null, { mark: { from: 0, label: 2 } }, { mark: { from: 0, label: 2 }, id: 2 }],
  });
  assert.deepEqual(s.spots.get(1), { from: 0, label: null, to: 0 });
  assert.deepEqual(s.spots.get(2), { from: 0, label: 2, to: 4 });
  assert.deepEqual(s.trails, [
    null,
    { mark: { from: 0, label: 2 }, id: null },
    { mark: { from: 0, label: 2 }, id: 2 },
  ]);
});

test("survey は core を往復する — 上にノードを足しても目印は同じノードを指す", () => {
  const first = survey("# r\n\n## a\n", [], []);
  assert.equal(first.view.trees.length, 1);
  assert.deepEqual(first.spots.get(3), { from: 5, label: 8, to: 10 });
  const s = first.spots.get(3);
  if (!s || s.label === null) throw new Error("a の地番が無い");
  const next = survey(
    "# r\n\n## n\n\n## a\n",
    [{ from: 5, to: 5, insert: "## n\n\n" }],
    [{ from: s.from, label: s.label }],
  );
  assert.deepEqual(next.trails, [{ mark: { from: 11, label: 14 }, id: 4 }]);
});
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `pnpm test`
Expected: `decodeSurvey` / `survey` が export されていない、で失敗（core は今のまま組める）

- [ ] **Step 3: core の出口**

`core/tree/js/moon.pkg` の import の先頭に `"moonbitlang/core/json",` を足す。

`core/tree/js/exports.mbt` の末尾に:

```moonbit
///|
/// 打鍵 1 回ぶんの読み。View と地番と、前のサイクルの目印がいまどのノードかを
/// **1 度の parse で**返す（読みは打鍵ごとに 1 回に保つ）。edits は反映の Edit の形
/// （前の座標、from 順）、marks は前のサイクルの地番から ts が抜いた目印。
/// 壊れた JSON は ts の側のバグなので、静かに断らず投げる。
#export_name("mmmSurvey")
pub fn mmm_survey(md : String, edits : String, marks : String) -> String {
  let edits : Array[@tree.Edit] = try @json.from_json(@json.parse(edits)) catch {
    e => abort("mmmSurvey: edits の JSON が読めない — \{e}")
  }
  let marks : Array[@tree.Mark] = try @json.from_json(@json.parse(marks)) catch {
    e => abort("mmmSurvey: marks の JSON が読めない — \{e}")
  }
  let (doc, spots) = @tree.survey(md)
  let out : Survey = {
    view: @view.project(doc),
    spots,
    trails: @tree.follow(spots, edits, marks),
  }
  out.to_json().stringify()
}

///|
/// mmmSurvey の返り値の形。JSON にするためだけの器
priv struct Survey {
  view : @view.View
  spots : Map[Int, @tree.Spot]
  trails : Array[@tree.Trail?]
} derive(ToJson)
```

Run: `pnpm run core`
Expected: 組める。`grep -c mmmSurvey core/_build/js/release/build/tree/js/js.js` が 1 以上（`Map[Int, …]` の ToJson は builtin が持つ。鍵は文字列で出る）

- [ ] **Step 4: TS の入口**

`src/coreApi.ts` の `view` の直後に型と関数を足す:

```ts
/** 地番。ノードが md のどこに書かれているか。label はラベルの頭（Implicit と文書の散文は null） */
export interface Spot {
  from: number;
  label: number | null;
  to: number;
}

/** 編集 1 つ。CodeMirror の changes と同じ形（前の座標、from 順、重ならない） */
export interface Edit {
  from: number;
  to: number;
  insert: string;
}

/** 前のサイクルの目印。選択を md の打鍵をまたいで持ち越すためのもの */
export interface Mark {
  from: number;
  label: number;
}

/** 写した目印と、いま当たるノード。当たらなくても目印は生きている */
export interface Trail {
  mark: Mark;
  id: number | null;
}

/** 打鍵 1 回ぶんの読み */
export interface Survey {
  view: View;
  spots: Map<number, Spot>;
  /** marks と同じ並び。null は死んだ目印 */
  trails: (Trail | null)[];
}

/**
 * md を core に読ませ、View と地番と、前の目印の行き先を 1 度に受け取る。
 * 読みのサイクルの入口はこちら。`view(md)` は lab と試験のために残す
 */
export const survey = (md: string, edits: Edit[], marks: Mark[]): Survey =>
  decodeSurvey(JSON.parse(mbt.mmmSurvey(md, JSON.stringify(edits), JSON.stringify(marks))));
```

`decode` の後ろに:

```ts
const spot = (v: unknown): Spot => {
  const o = record(v);
  return { from: field(o, "from", num), label: option(o, "label", num), to: field(o, "to", num) };
};

const mark = (v: unknown): Mark => {
  const o = record(v);
  return { from: field(o, "from", num), label: field(o, "label", num) };
};

const trail = (v: unknown): Trail | null => {
  if (v === null) return null;
  const o = record(v);
  return { mark: field(o, "mark", mark), id: option(o, "id", num) };
};

/** core の JSON（`mmmSurvey` の出力）を Survey にする。Map の鍵は文字列で来る */
export function decodeSurvey(json: unknown): Survey {
  const o = record(json);
  const spots = new Map<number, Spot>();
  for (const [k, v] of Object.entries(record(field(o, "spots", record)))) {
    const id = Number(k);
    if (!Number.isInteger(id)) bad(`spots の鍵 ${k}`);
    spots.set(id, spot(v));
  }
  return {
    view: decode(field(o, "view", (v) => v)),
    spots,
    trails: field(o, "trails", (t) => list(t, trail)),
  };
}
```

- [ ] **Step 5: 通ることを確かめる**

Run: `pnpm test && pnpm run check && pnpm run check:core`
Expected: 全部 PASS

- [ ] **Step 6: コミット**

```bash
cd core && moon fmt tree && cd ..
git add core/tree/js src/coreApi.ts test/coreApi.test.ts
git commit -m "feat: ✨ 打鍵 1 回の読みで View・地番・引き直しを返す（mmmSurvey / coreApi.survey）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: select.ts と caret.ts — 純粋層

**Files:**
- Create: `src/map/select.ts`、`src/caret.ts`
- Test: `test/select.test.ts`、`test/caret.test.ts`

**Interfaces:**
- Consumes: `Layout { order, boxes }`、`Box { node, parent: Edge | null, x, y, w, h }`（layout.ts）、`dirOf(side)`（geometry.ts）、`View` / `Spot`（coreApi.ts）
- Produces:
  ```ts
  // map/select.ts
  export interface Selection { ids: number[]; anchor: number | null }
  export const NONE: Selection
  export type Modifier = "none" | "shift" | "mod"
  export type ArrowKey = "ArrowUp" | "ArrowDown" | "ArrowLeft" | "ArrowRight"
  export const isArrowKey: (key: string) => key is ArrowKey
  export function click(sel: Selection, id: number, mod: Modifier, order: number[]): Selection
  export function rubber(L: Layout, r: Rect): Selection
  export function hit(L: Layout, x: number, y: number): number | null
  export function depthOf(L: Layout, id: number): number
  export function arrow(L: Layout, anchor: number | null, key: ArrowKey): number | null
  export function extend(sel: Selection, next: number): Selection
  export const all: (L: Layout) => Selection
  // caret.ts
  export interface Range { from: number; to: number }
  export function caretIds(view: View, spots: Map<number, Spot>, ranges: Range[]): number[]
  ```

- [ ] **Step 1: select の試験（RED）**

`test/select.test.ts`:

```ts
// 選択の値と、入力でそれがどう変わるか。箱は layoutMap に組ませる（配置は layout.test.ts が固定済み）。
//
// 実行: pnpm test

import { test } from "node:test";
import assert from "node:assert/strict";
import type * as core from "../src/coreApi.ts";
import { GAP, type Layout, type SizeOf, layoutMap } from "../src/map/layout.ts";
import { NONE, all, arrow, click, extend, hit, rubber } from "../src/map/select.ts";

/** 全部 100 × 30 */
const size: SizeOf = () => ({ w: 100, h: 30 });

const node = (id: number, label: string, children: core.Node[] = []): core.Node => ({
  id,
  label,
  fold: null,
  blocks: [],
  children,
});

const tree = (n: core.Node, sides: core.Side[] = []): core.Tree => ({ node: n, sides });

/** r(1) → a(2), b(3) → c(4)。b は左の枝 */
const L: Layout = layoutMap(
  [tree(node(1, "r", [node(2, "a"), node(3, "b", [node(4, "c")])]), ["Right", "Left"])],
  size,
);

test("クリック — 選ぶ。Shift は anchor から文書順に範囲、Mod は足す・外す", () => {
  const one = click(NONE, 3, "none", L.order);
  assert.deepEqual(one, { ids: [3], anchor: 3 });
  assert.deepEqual(click(one, 1, "shift", L.order), { ids: [1, 2, 3], anchor: 3 });
  assert.deepEqual(click(one, 1, "mod", L.order), { ids: [1, 3], anchor: 1 });
  assert.deepEqual(click({ ids: [1, 3], anchor: 1 }, 3, "mod", L.order), { ids: [1], anchor: 1 });
  // anchor が無ければ Shift も普通のクリック
  assert.deepEqual(click(NONE, 2, "shift", L.order), { ids: [2], anchor: 2 });
});

test("矩形 — 触れる箱を全部。anchor は文書順の最後", () => {
  // a は (100 + GAP.x, 0) から 100 × 30
  const x = 100 + GAP.x;
  assert.deepEqual(rubber(L, { x: x + 90, y: 20, w: 20, h: 20 }), { ids: [2], anchor: 2 });
  assert.deepEqual(rubber(L, { x: -1000, y: -1000, w: 3000, h: 3000 }), { ids: [1, 2, 3, 4], anchor: 4 });
  assert.deepEqual(rubber(L, { x: 5000, y: 5000, w: 1, h: 1 }), NONE);
});

test("点 — どの箱に居るか。外なら null", () => {
  const a = L.boxes.get(2);
  if (!a) throw new Error("a が無い");
  assert.equal(hit(L, a.x + 1, a.y + 1), 2);
  assert.equal(hit(L, a.x + a.w + 1, a.y), null);
});

test("矢印 — 上下は同じ深さの列を端でループ、何も選んでいなければ先頭", () => {
  assert.equal(arrow(L, null, "ArrowDown"), 1);
  assert.equal(arrow(L, 2, "ArrowDown"), 3);
  assert.equal(arrow(L, 3, "ArrowDown"), 2);
  assert.equal(arrow(L, 2, "ArrowUp"), 3);
  // 根の列は 1 つだけなので自分に戻る
  assert.equal(arrow(L, 1, "ArrowDown"), 1);
});

test("矢印 — 左右は画面の向き。右の枝は ← が親、左の枝は鏡像。子が無ければ先頭へ", () => {
  assert.equal(arrow(L, 2, "ArrowLeft"), 1);
  assert.equal(arrow(L, 1, "ArrowRight"), 2);
  assert.equal(arrow(L, 2, "ArrowRight"), 1); // a に子は無い → 先頭
  assert.equal(arrow(L, 3, "ArrowRight"), 1); // b は左の枝。→ が親
  assert.equal(arrow(L, 3, "ArrowLeft"), 4); // ← が子
  assert.equal(arrow(L, 1, "ArrowLeft"), null); // 根に親は無い
});

test("Shift+矢印 — 行き先を足す。既に選んでいれば、いま居た側を外して縮める", () => {
  const two = extend({ ids: [2], anchor: 2 }, 3);
  assert.deepEqual(two, { ids: [2, 3], anchor: 3 });
  assert.deepEqual(extend(two, 2), { ids: [2], anchor: 2 });
});

test("全部", () => {
  assert.deepEqual(all(L), { ids: [1, 2, 3, 4], anchor: 4 });
});
```

- [ ] **Step 2: caret の試験（RED）**

`test/caret.test.ts`:

```ts
// md のカーソルがどのノードに掛かっているか。区間の重なりだけの層。
//
// 実行: pnpm test

import { test } from "node:test";
import assert from "node:assert/strict";
import type * as core from "../src/coreApi.ts";
import { caretIds } from "../src/caret.ts";

const node = (id: number, label: string | null, children: core.Node[] = []): core.Node => ({
  id,
  label,
  fold: null,
  blocks: [],
  children,
});

const view = (...trees: core.Node[]): core.View => ({
  frontmatter: null,
  trees: trees.map((n) => ({ node: n, sides: n.children.map(() => "Right" as const) })),
});

const spots = (rows: [number, number, number | null, number][]): Map<number, core.Spot> =>
  new Map(rows.map(([id, from, label, to]) => [id, { from, label, to }]));

/** "# r\n\n## a\n\nhello\n\n## b\n" */
const doc = view(node(2, "r", [node(3, "a"), node(5, "b")]));
const at = spots([
  [1, 0, null, 0],
  [2, 0, 2, 23],
  [3, 5, 8, 17],
  [5, 18, 21, 23],
]);
const point = (p: number) => [{ from: p, to: p }];

test("最も深いノードだけ。自身の文は地番の頭から最初の子の頭まで", () => {
  assert.deepEqual(caretIds(doc, at, point(1)), [2]);
  assert.deepEqual(caretIds(doc, at, point(7)), [3]);
  assert.deepEqual(caretIds(doc, at, point(12)), [3]); // hello の中
  assert.deepEqual(caretIds(doc, at, point(20)), [5]);
});

test("閉じ際は中と見なす — 継ぎ目ちょうどでは両側", () => {
  assert.deepEqual(caretIds(doc, at, point(5)), [2, 3]);
  assert.deepEqual(caretIds(doc, at, point(17)), [3]);
});

test("範囲と複数カーソルは掛かるもの全部。無ければ空", () => {
  assert.deepEqual(caretIds(doc, at, [{ from: 1, to: 20 }]), [2, 3, 5]);
  assert.deepEqual(caretIds(doc, at, [{ from: 1, to: 1 }, { from: 20, to: 20 }]), [2, 5]);
  assert.deepEqual(caretIds(doc, at, []), []);
});

test("Implicit は行が無く自身の文が空なので掛からない", () => {
  // "# r\n\n### x\n": r=2, Implicit=3, x=4
  const gap = view(node(2, "r", [node(3, null, [node(4, "x")])]));
  const s = spots([
    [1, 0, null, 0],
    [2, 0, 2, 11],
    [3, 5, null, 11],
    [4, 5, 9, 11],
  ]);
  assert.deepEqual(caretIds(gap, s, point(7)), [4]);
  assert.deepEqual(caretIds(gap, s, point(5)), [2, 4]);
});
```

- [ ] **Step 3: 落ちることを確かめる**

Run: `pnpm test`
Expected: `../src/map/select.ts` / `../src/caret.ts` が無い、で失敗

- [ ] **Step 4: select.ts を書く**

`src/map/select.ts`:

```ts
// 選択。何を選んでいるかの値と、入力（クリック・矩形・矢印）でそれがどう変わるか。
// DOM も地図も知らない — 地図は入力を値にして渡し、返った値を塗るだけ。
// 決めは docs/superpowers/specs/2026-09-04-select-design.md と spec.md「Mindmap 側」。
//
// id は文書順の通し番号なので、数の順がそのまま文書順。

import { dirOf, type Rect } from "./geometry.ts";
import type { Layout } from "./layout.ts";

/** 選んでいるノード（文書順）と、範囲選択・矢印の基点 */
export interface Selection {
  ids: number[];
  anchor: number | null;
}

export const NONE: Selection = { ids: [], anchor: null };

export type Modifier = "none" | "shift" | "mod";

const sorted = (ids: Iterable<number>): number[] => [...new Set(ids)].sort((a, b) => a - b);

const last = (ids: number[]): number | null => (ids.length === 0 ? null : ids[ids.length - 1]);

/** 選ぶ。`shift` は anchor から文書順に範囲（anchor は動かない）、`mod` は足す・外す */
export function click(sel: Selection, id: number, mod: Modifier, order: number[]): Selection {
  if (mod === "mod") {
    const ids = sel.ids.includes(id) ? sel.ids.filter((x) => x !== id) : sorted([...sel.ids, id]);
    return { ids, anchor: ids.includes(id) ? id : last(ids) };
  }
  if (mod === "shift" && sel.anchor !== null) {
    const a = order.indexOf(sel.anchor);
    const b = order.indexOf(id);
    if (a !== -1 && b !== -1) {
      const [lo, hi] = a < b ? [a, b] : [b, a];
      return { ids: sorted(order.slice(lo, hi + 1)), anchor: sel.anchor };
    }
  }
  return { ids: [id], anchor: id };
}

/** 矩形（world）に触れる箱を全部。anchor は文書順の最後 */
export function rubber(L: Layout, r: Rect): Selection {
  const ids = L.order.filter((id) => {
    const b = L.boxes.get(id);
    return b !== undefined && b.x < r.x + r.w && r.x < b.x + b.w && b.y < r.y + r.h && r.y < b.y + b.h;
  });
  return { ids, anchor: last(ids) };
}

/** world の点がどの箱に居るか。重なりは文書順の後ろが上。外なら null */
export function hit(L: Layout, x: number, y: number): number | null {
  for (let i = L.order.length - 1; i >= 0; i--) {
    const id = L.order[i];
    const b = L.boxes.get(id);
    if (b && x >= b.x && x < b.x + b.w && y >= b.y && y < b.y + b.h) return id;
  }
  return null;
}

export const all = (L: Layout): Selection => ({ ids: sorted(L.order), anchor: last(L.order) });

export type ArrowKey = "ArrowUp" | "ArrowDown" | "ArrowLeft" | "ArrowRight";

export const isArrowKey = (key: string): key is ArrowKey =>
  key === "ArrowUp" || key === "ArrowDown" || key === "ArrowLeft" || key === "ArrowRight";

/** 根からの深さ。Layout は持たないので親を辿って数える */
export function depthOf(L: Layout, id: number): number {
  let d = 0;
  let b = L.boxes.get(id);
  while (b && b.parent) {
    d++;
    b = L.boxes.get(b.parent.id);
  }
  return d;
}

/**
 * 矢印 1 回ぶんの行き先。行けなければ null。何も選んでいなければ先頭。
 *
 * - 上下 … **同じ深さの列**を文書順に辿り、端でループする。兄弟に限らずいとこも含む
 * - 左右 … **画面の向き**で読む。根と右の枝は ← が親・→ が子、左の枝は鏡像。
 *   子が無ければ先頭へ回る（行き止まりで無反応になるより一周できるほうが迷わない）
 */
export function arrow(L: Layout, anchor: number | null, key: ArrowKey): number | null {
  const order = L.order;
  if (order.length === 0) return null;
  if (anchor === null) return order[0];
  const cur = L.boxes.get(anchor);
  if (!cur) return null;
  if (key === "ArrowUp" || key === "ArrowDown") {
    const depth = depthOf(L, anchor);
    const level = order.filter((id) => depthOf(L, id) === depth);
    const i = level.indexOf(anchor);
    const step = key === "ArrowUp" ? -1 : 1;
    return level[(i + step + level.length) % level.length];
  }
  // 側を符号に読むのは dirOf だけ、という不変条件を守る
  const dir = cur.parent ? dirOf(cur.parent.side) : 1;
  const toParent = key === (dir === -1 ? "ArrowRight" : "ArrowLeft");
  if (toParent) return cur.parent ? cur.parent.id : null;
  const kid = order.find((id) => L.boxes.get(id)?.parent?.id === anchor);
  return kid ?? order[0];
}

/** Shift+矢印。行き先を足す。行き先が既に選ばれていれば、いま居た側を外して縮める */
export function extend(sel: Selection, next: number): Selection {
  if (sel.ids.includes(next) && sel.ids.length > 1 && sel.anchor !== null) {
    return { ids: sel.ids.filter((x) => x !== sel.anchor), anchor: next };
  }
  return { ids: sorted([...sel.ids, next]), anchor: next };
}
```

- [ ] **Step 5: caret.ts を書く**

`src/caret.ts`:

```ts
// md のカーソル（と選択）が、いまどのノードに掛かっているか。DOM も文書の意味も
// 知らない、区間の重なりだけの層。
//
// 自身の文 = 地番の頭から最初の子の頭まで（中身は子より前に書かれる）。地番は
// 入れ子なので、これで「最も深いノード」が出る。子孫まで含む `to` で見ると、
// カーソル 1 つでも根までの祖先が全部光り、選択と見分けが付かない（spec.md
// 「二つをまたぐ印」）。

import type { Node, Spot, View } from "./coreApi.ts";

/** md 側のカーソル 1 つ、または選択 1 つぶん（`from === to` なら点） */
export interface Range {
  from: number;
  to: number;
}

/**
 * その範囲たちが掛かるノードの id、文書順。無ければ空。
 *
 * **閉じ際は中と見なす。** 区間は半開だが、カーソルはその位置に立てる（文字と文字の
 * 隙間に居る）。半開で読むと文書の末尾では何にも掛からず、追記しているあいだ印が
 * 出ない。裏返しとして、継ぎ目ちょうどでは両側が挙がる。
 * Implicit は行が無く自身の文が空なので、掛からない。
 */
export function caretIds(view: View, spots: Map<number, Spot>, ranges: Range[]): number[] {
  if (ranges.length === 0) return [];
  const out: number[] = [];
  const walk = (n: Node): void => {
    const s = spots.get(n.id);
    if (s) {
      const first = n.children.length > 0 ? spots.get(n.children[0].id) : undefined;
      const own = first ? first.from : s.to;
      if (own > s.from && ranges.some((r) => s.from <= r.to && own >= r.from)) out.push(n.id);
    }
    for (const k of n.children) walk(k);
  };
  for (const t of view.trees) walk(t.node);
  return out;
}
```

- [ ] **Step 6: 通ることを確かめる**

Run: `pnpm test && pnpm run check`
Expected: 全部 PASS。`test/tsconfig.json` の `include` は `"."` なので新しい試験は拾われる

- [ ] **Step 7: コミット**

```bash
git add src/map/select.ts src/caret.ts test/select.test.ts test/caret.test.ts
git commit -m "feat: ✨ 選択の純粋層（select）とカーソルの掛かり（caret）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: editor — 編集列とカーソルを外へ、薄塗りとスクロールを中へ

**Files:**
- Modify: `src/editor.ts`

**Interfaces:**
- Produces:
  ```ts
  constructor(parent: HTMLElement, onChange: (text: string, edits: core.Edit[]) => void, onCaret: (ranges: Range[]) => void)
  highlight(ranges: Range[]): void   // 地図で選んだ範囲を薄く塗る（装飾。実選択にしない）
  reveal(pos: number): void          // その位置を中央へスクロール
  caret(): Range[]                   // フォーカスが無ければ空
  ```
  `setText` は全文の置き換え `[{ from: 0, to: 前の長さ, insert: text }]` を onChange に渡す

（この層は DOM と CodeMirror の配線なので、試験は Task 7 の煙試験。壊し方は
`pnpm run check` が見張る）

- [ ] **Step 1: 書き換える**

import を足す:

```ts
import { Compartment, EditorState, StateEffect, StateField } from "@codemirror/state";
import { Decoration, type DecorationSet, EditorView, keymap, lineNumbers } from "@codemirror/view";
import type * as core from "./coreApi.ts";
import type { Range } from "./caret.ts";
```

`LIGHT_EXT` の後ろに薄塗りの装飾:

```ts
/** 地図で選んでいる範囲。実選択にするとカーソルを奪うので、装飾で塗る（spec.md「二つをまたぐ印」） */
const setHighlights = StateEffect.define<Range[]>();
const highlightMark = Decoration.mark({ class: "cm-mmm-selected" });
const highlightField = StateField.define<DecorationSet>({
  create: () => Decoration.none,
  update(deco, tr) {
    let next = deco.map(tr.changes);
    for (const e of tr.effects) {
      if (e.is(setHighlights)) {
        next = Decoration.set(
          e.value.filter((r) => r.to > r.from).map((r) => highlightMark.range(r.from, r.to)),
          true,
        );
      }
    }
    return next;
  },
  provide: (f) => EditorView.decorations.from(f),
});
```

class の欄と constructor:

```ts
  private onChange: (text: string, edits: core.Edit[]) => void;
  private onCaret: (ranges: Range[]) => void;

  constructor(
    parent: HTMLElement,
    onChange: (text: string, edits: core.Edit[]) => void,
    onCaret: (ranges: Range[]) => void,
  ) {
    this.onChange = onChange;
    this.onCaret = onCaret;
```

extensions の `updateListener` を差し替え、`highlightField` を足す:

```ts
        highlightField,
        EditorView.updateListener.of((u) => {
          if (u.docChanged) {
            // 何がどう変わったかを、前の座標の編集列にして渡す。選択の持ち越しが使う
            const edits: core.Edit[] = [];
            u.changes.iterChanges((fromA, toA, _fromB, _toB, inserted) => {
              edits.push({ from: fromA, to: toA, insert: inserted.toString() });
            });
            this.onChange(u.state.doc.toString(), edits);
          }
          if (u.docChanged || u.selectionSet || u.focusChanged) this.onCaret(this.caret());
        }),
```

`setText`:

```ts
  /** 文書を丸ごと入れ替える（開く / 新規）。履歴も新しくなる。setState は listener を呼ばないので自分で言う。
   *  編集列は全文の置き換え — 前の文書の目印は全部死ぬ */
  setText(text: string): void {
    const before = this.view.state.doc.length;
    this.view.setState(this.state(text));
    this.onChange(text, [{ from: 0, to: before, insert: text }]);
    this.onCaret(this.caret());
  }
```

`undo` の前に:

```ts
  /** 地図で選んでいる範囲を、こちらの行にも映す */
  highlight(ranges: Range[]): void {
    this.view.dispatch({ effects: setHighlights.of(ranges) });
  }

  /** その位置を中央へ。地図で選び直したときだけ呼ぶ（打鍵のたびに寄せると手元が揺れる） */
  reveal(pos: number): void {
    if (pos < 0 || pos > this.view.state.doc.length) return;
    this.view.dispatch({ effects: EditorView.scrollIntoView(pos, { y: "center" }) });
  }

  /**
   * いまのカーソルと選択の範囲（複数カーソルならその数だけ）。**このペインに
   * フォーカスが無ければ空** — 「いまどこを書いているか」はここに居るあいだだけの事実
   */
  caret(): Range[] {
    if (!this.view.hasFocus) return [];
    return this.view.state.selection.ranges.map((r) => ({ from: r.from, to: r.to }));
  }
```

- [ ] **Step 2: 型が通ることを確かめる**

Run: `pnpm run check`
Expected: `src/main.ts` の `new MdEditor(mdPane, sync)` だけが引数の数で落ちる（Task 7 で直す）。他に落ちれば editor.ts を直す

- [ ] **Step 3: コミット**

```bash
git add src/editor.ts
git commit -m "feat: ✨ md ペインが編集列とカーソルを外へ出し、薄塗りとスクロールを受ける

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 地図 — 入力を値にして host へ、選択と輪を塗る

**Files:**
- Modify: `src/map/render.ts`（`paintSelection`）
- Modify: `src/mindmap.ts`
- Modify: `src/style.css`

**Interfaces:**
- Consumes: `select.ts` / `caret.ts` の型、`toWorld` / `centerOn` / `panToShow(cam, box, pane, margin)`（camera.ts）、`unionRect`（geometry.ts）、`svgEl(tag, attrs)`（svg.ts。attrs は属性としてそのまま付く）
- Produces（MapHost に足す）:
  ```ts
  selection(): Selection;                                  // 値は main.ts が持つ
  setSelection(sel: Selection, reveal: boolean): void;    // 地図で選び直した
  ```
  Mindmap に足す: `refreshSelection(): void`（塗り直し）、`showCaret(ids: number[]): void`、`centerOnTarget(): void`、`ensureVisible(id: number): void`

- [ ] **Step 1: renderer に塗り替えを足す**

`src/map/render.ts` の `edgeEl` の後ろに:

```ts
  /** 選ばれた箱に印を付ける。レイアウトを見直さない軽い塗り替え（値は main.ts のもの） */
  paintSelection(sel: Set<number>): void {
    for (const [id, g] of this.nodeEls) g.classList.toggle("selected", sel.has(id));
  }
```

- [ ] **Step 2: CSS**

`src/style.css` の `.node` の塊の中、`&.root rect.box` の直後に:

```css
  /* ---- 選択（付けるのは map/render.ts の paintSelection）---- */
  &.selected rect.box {
    stroke: var(--accent);
    stroke-width: 2;
    /* md 側の薄塗りと同じ濃さ。ただし箱は下が透けては困る（地のドットが出る）ので、
       transparent ではなく node-bg に混ぜる */
    fill: color-mix(in srgb, var(--accent) var(--accent-mix), var(--node-bg));
  }
```

（`--accent-mix` は `:root` に在る。md 側の `.cm-mmm-selected { background: var(--accent-soft) }` も在るので足さない）

`.node` の塊の後ろに:

```css
/* ========== 二つをまたぐ印 ==========
   矩形選択の面と、md のカーソルが掛かるノードの内側の輪。
   輪の濃さはここだけが持つ — `--accent-soft` は面を塗るための薄さで、
   2px の線に当てると消える。内側に居る以上は選択と紛れようがない。 */
#rubber {
  position: absolute;
  border: 1px solid var(--accent);
  background: var(--accent-soft);
  pointer-events: none;
  display: none;
}

.caret-ring {
  fill: none;
  stroke: color-mix(in srgb, var(--accent) 55%, transparent);
  stroke-width: 2;
  rx: 5px;
  /* 印は乗っているだけ。掴めると、下のノードを選べなくなる */
  pointer-events: none;
}
```

- [ ] **Step 3: mindmap.ts**

import を足す:

```ts
import { type Camera, type Pane, centerOn, fitToPane, panBy, panToShow, pinch, toWorld, zoomAt } from "./map/camera.ts";
import { unionRect } from "./map/geometry.ts";
import { NONE, type Selection, all, arrow, click, extend, hit, isArrowKey, rubber } from "./map/select.ts";
```

`FIT_MARGIN` の隣に定数を足す:

```ts
/** 矢印で辿るとき、選んだ箱が縁から離れている距離（画面 px） */
const SHOW_MARGIN = 40;
```

MapHost に足す:

```ts
  /** いま選んでいるもの。値は main.ts が持つ */
  selection(): Selection;
  /** 地図で選び直した。reveal は md 側をその頭へスクロールするか */
  setSelection(sel: Selection, reveal: boolean): void;
```

class の欄に足す:

```ts
  /** カーソルの輪の層。world に浮かぶ別の印（ノードの子にすると、動くたびに中身が作り直される） */
  private caretLayer: SVGGElement;
  private caretRings: SVGRectElement[] = [];
  private caretIds: number[] = [];
  /** 矩形選択の面（画面 px）。始点は pane の左上から */
  private rubber: HTMLDivElement;
  private rubberStart: { x: number; y: number } | null = null;
  /** 指で押したノード。動かさずに離せば選ぶ */
  private tapped: { id: number; x: number; y: number } | null = null;
  /** Space を押している間、左ドラッグはパン */
  private spaceHeld = false;
```

constructor: `this.world.append(this.renderer.edgeLayer, this.renderer.nodeLayer);` を

```ts
    this.caretLayer = svgEl("g");
    this.world.append(this.renderer.edgeLayer, this.renderer.nodeLayer, this.caretLayer);
```

にし、`pane.append(svg);` の後ろに:

```ts
    this.rubber = document.createElement("div");
    this.rubber.id = "rubber";
    pane.append(this.rubber);
```

的ボタンと `Home` は `centerOnRoot` から `centerOnTarget` へ。constructor の `keydown` の塊を丸ごと `this.bindKeys();` にする。

`render()` の `this.updateIndicator();` の前に:

```ts
    this.renderer.paintSelection(new Set(this.host.selection().ids));
    this.showCaret(this.caretIds);
```

`centerOnRoot` を差し替え:

```ts
  /** 選択（無ければ根）を画面の中心へ。拡大率は変えない */
  centerOnTarget(): void {
    const sel = this.host.selection().ids.flatMap((id) => {
      const b = this.layout.boxes.get(id);
      return b ? [b] : [];
    });
    const target = unionRect(sel) ?? rootBox(this.layout);
    if (target) this.setCamera(centerOn(this.camera, target, this.paneSize()));
  }

  /** その箱が画面に入るまでだけ寄せる（矢印で選び直したとき） */
  ensureVisible(id: number): void {
    const b = this.layout.boxes.get(id);
    if (b) this.setCamera(panToShow(this.camera, b, this.paneSize(), SHOW_MARGIN));
  }

  /** 選択の塗り直し。レイアウトは見直さない */
  refreshSelection(): void {
    this.renderer.paintSelection(new Set(this.host.selection().ids));
  }

  /**
   * カーソルの輪を、掛かっているノードの**内側**へ重ねる。外から掴むのが選択、
   * 中に居るのがカーソルで、形がそのまま意味になる。箱の無い（畳まれて埋もれた）
   * ノードには出さない。本数が変わったときだけ作り足す/捨てる
   */
  showCaret(ids: number[]): void {
    this.caretIds = ids;
    const boxes = ids.flatMap((id) => {
      const b = this.layout.boxes.get(id);
      return b ? [b] : [];
    });
    while (this.caretRings.length > boxes.length) this.caretRings.pop()?.remove();
    while (this.caretRings.length < boxes.length) {
      const ring = svgEl("rect", { class: "caret-ring" });
      this.caretLayer.append(ring);
      this.caretRings.push(ring);
    }
    const inset = 3;
    boxes.forEach((b, i) => {
      const ring = this.caretRings[i];
      ring.setAttribute("x", String(b.x + inset));
      ring.setAttribute("y", String(b.y + inset));
      ring.setAttribute("width", String(b.w - inset * 2));
      ring.setAttribute("height", String(b.h - inset * 2));
    });
  }

  /** 画面の点がどの箱に居るか。無ければ null */
  private nodeAt(clientX: number, clientY: number): number | null {
    const p = this.local(clientX, clientY);
    const w = toWorld(this.camera, p.x, p.y);
    return hit(this.layout, w.x, w.y);
  }
```


`bindPointer` の 2 つ目の `pointerdown` を差し替え:

```ts
    pane.addEventListener("pointerdown", (e) => {
      if (targetIn(e, ".link-open, .img-connect, .pane-tool")) return;
      if (e.pointerType === "touch" && this.fingers.pinching) return;
      if (e.button !== 0 && e.button !== 1) return;
      pane.focus();
      const id = this.nodeAt(e.clientX, e.clientY);
      // パンは 3 つ入り口を持つ: 中クリック / Space+ドラッグ / 指で背景をなぞる。
      // 担当する手が違うので、どれか 1 つでは塞がる場面がある
      const pan = e.button === 1 || this.spaceHeld || (e.pointerType === "touch" && id === null);
      if (pan) {
        this.panning = { px: e.clientX, py: e.clientY, ox: this.camera.tx, oy: this.camera.ty };
        pane.style.cursor = "grabbing";
        pane.setPointerCapture(e.pointerId);
        e.preventDefault();
        return;
      }
      if (e.pointerType === "touch") {
        // 指は離したときに選ぶ（なぞったら選ばない）
        this.tapped = id === null ? null : { id, x: e.clientX, y: e.clientY };
        return;
      }
      if (id !== null) {
        const mod = e.shiftKey ? "shift" : e.ctrlKey || e.metaKey ? "mod" : "none";
        this.host.setSelection(click(this.host.selection(), id, mod, this.layout.order), true);
        e.preventDefault();
        return;
      }
      // 背景。ドラッグすれば矩形選択、動かさずに離せば選択を解く
      this.rubberStart = this.local(e.clientX, e.clientY);
      pane.setPointerCapture(e.pointerId);
      e.preventDefault();
    });
```

`pointermove` の `if (!this.panning) return;` の前に:

```ts
      if (this.tapped && Math.hypot(e.clientX - this.tapped.x, e.clientY - this.tapped.y) > 8) {
        this.tapped = null;
      }
      if (this.rubberStart) {
        const p = this.local(e.clientX, e.clientY);
        const x = Math.min(this.rubberStart.x, p.x);
        const y = Math.min(this.rubberStart.y, p.y);
        const w = Math.abs(p.x - this.rubberStart.x);
        const h = Math.abs(p.y - this.rubberStart.y);
        Object.assign(this.rubber.style, { display: "block", left: `${x}px`, top: `${y}px`, width: `${w}px`, height: `${h}px` });
        const a = toWorld(this.camera, x, y);
        const b = toWorld(this.camera, x + w, y + h);
        this.host.setSelection(rubber(this.layout, { x: a.x, y: a.y, w: b.x - a.x, h: b.y - a.y }), false);
        return;
      }
```

`end` の頭に:

```ts
      if (this.tapped) {
        this.host.setSelection(click(this.host.selection(), this.tapped.id, "none", this.layout.order), true);
        this.tapped = null;
      }
      if (this.rubberStart) {
        const dragged =
          this.rubber.style.display === "block" &&
          (parseFloat(this.rubber.style.width) > 3 || parseFloat(this.rubber.style.height) > 3);
        this.rubber.style.display = "none";
        this.rubberStart = null;
        if (!dragged) this.host.setSelection(NONE, false);
      }
```

キー:

```ts
  /** 地図の中だけで効くキー。全体のキーは app/shortcuts.ts */
  private bindKeys(): void {
    this.pane.addEventListener("keydown", (e) => {
      const mod = e.ctrlKey || e.metaKey;
      if (e.key === " ") {
        this.spaceHeld = true;
        this.pane.style.cursor = "grab";
        e.preventDefault();
        return;
      }
      if (e.key === "Home" && !mod && !e.altKey) {
        e.preventDefault();
        this.centerOnTarget();
        return;
      }
      if (e.key === "Escape") {
        e.preventDefault();
        this.host.setSelection(NONE, false);
        return;
      }
      if (mod && !e.altKey && e.key.toLowerCase() === "a") {
        e.preventDefault();
        this.host.setSelection(all(this.layout), false);
        return;
      }
      if (isArrowKey(e.key) && !mod && !e.altKey) {
        e.preventDefault();
        const sel = this.host.selection();
        const next = arrow(this.layout, sel.anchor, e.key);
        if (next === null) return;
        this.host.setSelection(e.shiftKey ? extend(sel, next) : { ids: [next], anchor: next }, true);
        this.ensureVisible(next);
      }
    });
    this.pane.addEventListener("keyup", (e) => {
      if (e.key === " ") {
        this.spaceHeld = false;
        if (!this.panning) this.pane.style.cursor = "";
      }
    });
  }
```

- [ ] **Step 4: 型が通ることを確かめる**

Run: `pnpm run check`
Expected: `src/main.ts` の host（`selection` / `setSelection` が無い）と editor の引数だけが落ちる

- [ ] **Step 5: コミット**

```bash
git add src/map/render.ts src/mindmap.ts src/style.css
git commit -m "feat: ✨ 地図が叩く・矩形・矢印を値にして渡し、選択と輪を塗る

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: main — 選択を持ち、打鍵をまたいで引き直す

**Files:**
- Modify: `src/main.ts`

**Interfaces:**
- Consumes: `core.survey`、`caretIds`、`Selection` / `NONE`、editor の `highlight` / `reveal`、map の `refreshSelection` / `showCaret`

- [ ] **Step 1: 配線する**

import を足す:

```ts
import { caretIds, type Range } from "./caret.ts";
import { NONE, type Selection } from "./map/select.ts";
```

app state（`let doc: core.View = …` の後ろ）:

```ts
/** いまの地番。カーソルの輪・md 側の薄塗り・選択の持ち越しが読む */
let spots = new Map<number, core.Spot>();
/** 何を選んでいるか。地図はこれを塗るだけで、自分では持たない */
let selection: Selection = NONE;
/**
 * 幽霊 — 前のサイクルで当たらなかった目印。捨てずに持ち越す（`## n## a` の
 * 途中のサイクルで捨てると、Enter で戻れない）。地図で選び直したら消える
 */
let ghosts: core.Mark[] = [];
```

`sync` を差し替え:

```ts
/**
 * 本文が変わった。**ここが読みのサイクルの唯一の入口** — 打鍵も、開くも、
 * 新規も、リンクで開くも、全部ここを通って同じ順で映る。
 *
 * 選択は id でなく目印（前の地番）で持ち越す。id は読みのサイクルを越えて
 * 持たないので、core に「この目印はいまどれか」を訊く（`follow`）。
 */
function sync(next: string, edits: core.Edit[]): void {
  const wasEmpty = doc.trees.length === 0;
  // 目印と、その持ち主（幽霊は null）。Implicit は行が無いので捨てる
  const marks: core.Mark[] = [];
  const owners: (number | null)[] = [];
  for (const id of selection.ids) {
    const s = spots.get(id);
    if (s && s.label !== null) {
      marks.push({ from: s.from, label: s.label });
      owners.push(id);
    }
  }
  for (const g of ghosts) {
    marks.push(g);
    owners.push(null);
  }
  const r = core.survey(next, edits, marks);
  text = next;
  doc = r.view;
  spots = r.spots;
  const ids: number[] = [];
  const kept: core.Mark[] = [];
  let anchor: number | null = null;
  r.trails.forEach((t, i) => {
    if (!t) return;
    if (t.id === null) {
      kept.push(t.mark);
      return;
    }
    ids.push(t.id);
    if (owners[i] === selection.anchor) anchor = t.id;
  });
  ids.sort((a, b) => a - b);
  selection = { ids, anchor: anchor ?? (ids.length ? ids[ids.length - 1] : null) };
  ghosts = kept;
  map.render();
  editor.highlight(selectedRanges());
  // 白紙の言い出し。**出る理由は 1 つ**（まだ木が無い）で、マップ側も
  // render() の中で同じことを見ている
  editor.showHint(doc.trees.length === 0);
  updateDirty();
  showName();
  exportApi.refresh();
  // 何も無いところに最初の木が生まれた瞬間だけ、真ん中へ寄せる
  if (wasEmpty && doc.trees.length > 0) map.fitView();
}

/** 選んでいるノードの md 側の範囲（子孫込み） */
const selectedRanges = (): Range[] =>
  selection.ids.flatMap((id) => {
    const s = spots.get(id);
    return s ? [{ from: s.from, to: s.to }] : [];
  });

/** 地図で選び直した。幽霊は要らなくなる */
function setSelection(sel: Selection, reveal: boolean): void {
  selection = sel;
  ghosts = [];
  map.refreshSelection();
  editor.highlight(selectedRanges());
  if (reveal && sel.anchor !== null) {
    const s = spots.get(sel.anchor);
    if (s) editor.reveal(s.from);
  }
}

/** md のカーソルが動いた。掛かるノードに輪を出す（地図は動かさない） */
function onCaret(ranges: Range[]): void {
  map.showCaret(caretIds(doc, spots, ranges));
}

const editor = new MdEditor(mdPane, sync, onCaret);
```

host に足す:

```ts
  selection: () => selection,
  setSelection,
```

- [ ] **Step 2: 型と試験**

Run: `pnpm run check && pnpm test`
Expected: 全部 PASS

- [ ] **Step 3: 煙試験（ブラウザ）**

`preview_start` で dev サーバを開く（`.claude/launch.json` の `mmm-lab` ではなく本体。worktree から見るには主ディレクトリの launch.json を一時的に `--dir .worktrees/feat/select run dev` に書き換え、終わったら `git checkout -- .claude/launch.json`）。md に次を打つ:

```
# r

## a

## b
```

確かめること（`read_page` と screenshot）:
1. 地図で `a` を叩く → `a` の箱に `selected`、md の `## a` の行が薄く塗られる
2. md の `# r` の行末にカーソルを置いて Enter → `## n` と打つ → 地図の選択は `a` のまま（id は 3 → 4 にずれている）
3. md の `## b` の行にカーソルを置く → `b` の箱の内側に輪。選択は動かない
4. 背景を左ドラッグ → 矩形が出て触れた箱が選ばれる。Space を押しながらドラッグ → パン
5. `↓` で `a` → `b`、`Shift+↑` で両方、`Esc` で解除、`Mod+A` で全部、`Home` で寄る

- [ ] **Step 4: コミット**

```bash
git add src/main.ts
git commit -m "feat: ✨ 選択を持ち、md の打鍵をまたいで同じノードを指し続ける

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: docs

**Files:**
- Modify: `docs/spec.md`（「構成」）、`docs/core.md`（「地番」と「同一性」）

- [ ] **Step 1: spec.md「構成」**

`core/` の `tree/` の説明に `follow.mbt が「md がこう変わった。前に選んでいたノードはいまどれか」を答える` を足す。`src/` の行を直す:

```
src/    TypeScript — UI。**描いて、選ぶ。** 操作は次の段から（git に在る）
  coreApi.ts   core の出口と入口。JSON の形を整える唯一の場所（survey が View・地番・目印の行き先を 1 度に受ける）。TS では必ず `core.View` と書く
  caret.ts     md のカーソルがどのノードに掛かっているか（最も深いもの。区間の重なりだけ）
  editor.ts    Markdown 側(CodeMirror 6、履歴も CodeMirror。編集列とカーソルを外へ出し、
               地図の選択を薄塗りで受ける。フェンスの中は map/highlight.ts と同じ言語表で色を付ける)
  mindmap.ts   Mindmap 側(視点と描画と、選択の入力。叩く・矩形・矢印を値にして main へ渡し、
               選択と輪を塗る。パン・ズーム・ピンチ・寄せ・針・書き出し)
```

`map/` の並びに `select(選択の値と、入力でどう変わるか。矩形・矢印・点の当たり)` を足す。`main.ts` の行を `束ねる場所(打鍵 → core.survey → render の 1 本、選択と幽霊、ファイル I/O、帯)` に。`test/` の並びに `select / caret` を足す。

- [ ] **Step 2: core.md**

「地番」の箇条書きに足す:

```
- **ラベルの頭（`label`）** — マーカー（`## ` / `- `）の後ろ。選択が md の打鍵をまたいで
  同じノードを指し続けるための目印。行を持たない Implicit と文書の散文は無い
```

「反映 — reflect.mbt」の節の後ろに:

```
### 同一性 — follow.mbt

`follow(spots, edits, marks) -> [Trail?]`。**md がこう変わった。前のサイクルで選んで
いたノードはいまどれか**に答える。id は読みのサイクルを越えて持たないので、ts は
選んでいるノードの目印（`Mark { from, label }` = マーカーの頭とラベルの頭）を前の
地番から抜いて渡し、core が編集列で写して今の地番に当てる。

- 目印は**ラベルの頭**。行の頭だと `## a` → `### a` で挿入がちょうど頭になり、写す
  向きで結果が割れる
- **マーカー `[from, label)` が丸ごと消されたら死ぬ**（None）。ラベルの頭が消去の
  内側に落ちても死ぬ。それ以外は写し続け、当たらなくても目印を返す（ts は幽霊として
  持ち越し、次のサイクルで当たれば戻る — `## n## a` の途中のサイクルのため）
- ちょうど目印への挿入は動かさない（挿入の前に留まる）。目印で終わる消去はずらす
- Implicit は行が無いので目印も無く、md を触れば選択から落ちる

打鍵ごとの core 呼び出しは `mmmSurvey(md, edits, marks)` の 1 回で、View・地番・
Trail を一度に返す（parse は 1 回）。法則は follow_wbtest.mbt が spec の表の行ごとに
固定する。**同一性は core、選択は ts** — 目印の意味はマーカーの読み方（読みの規則）
だから core、何を選ぶか（どの箱を叩いたか、矩形に何が入るか）は幾何だから ts。
```

- [ ] **Step 3: コミット**

```bash
git add docs/spec.md docs/core.md
git commit -m "docs: 📝 選択の段 — 地番のラベルの頭と同一性（follow）を書く

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 9: 仕上げ — PR

- [ ] **Step 1: 全部を通す**

Run: `pnpm run check && pnpm run check:core && pnpm run test:core && pnpm test`
Expected: 全部 PASS

- [ ] **Step 2: push して PR**

```bash
git push -u origin feat/select
gh pr create --base feat/tree --title "feat: ✨ 選択 — md の打鍵をまたいで同じノードを指し続ける" --body "..."
```

body には spec の要点（同一性は core の follow、選択は ts の値、mmmSurvey 1 回、幽霊、背景ドラッグが矩形選択になりパンは Space/中クリック/指へ）と、試験の数を書く。末尾に `🤖 Generated with [Claude Code](https://claude.com/claude-code)`。
