# paste — 貼る解釈を core に 実装計画

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 貼り付けの字を raw のまま `Graft` に渡し、断片が持ち込むのは形だけ・綴りは席が決める、に core を揃える（#117）。`MoveNode` も同じ規則、`cap` は天井より深い Implicit を空の項目に綴る。

**Architecture:** 綴りの規則を `sign_at`（AddNode の規則の名前）に 1 つ、綴り替えを `respell` に 1 つ置き、`graft` / `move_node` / `cap` がそれを使う。「段落かどうか」は mdAst しか知らないので core/tree に `lines` を足す。TS は `decidePaste` を捨て、`Graft(In(anchor), clip)` を送るだけになる。

**Tech Stack:** MoonBit（core）、TypeScript、Node `node:test`。

**Spec:** `docs/superpowers/specs/2026-09-06-paste-design.md`

## Global Constraints

- 作業は worktree `.worktrees/feat/paste`（枝 `feat/paste`、main から）。1 Task 1 コミット、最後に 1 PR で main に squash（`Closes #117`）
- コミットは `<Type>: <Emoji> #117 <Title>` の日本語。末尾に `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`
- **Windows:** `.mbt` / `.ts` / `.md` は Write / Edit ツールで書く（Bash の heredoc は `\` を潰す）。moon は PATH に在る（`which moon` で確認）
- MoonBit の試験は `cd core && moon test -p mmm-app/core/<pkg> -f <file>`。`inspect` の期待値を更新するときだけ `-u`
- TS: `as T` も `!` も置かない。コメントは日本語、画面の文字は英語。読みの規則（`#` / `-` / URL）を TS で書き直さない
- core の決めを変えたら docs/core.md の該当行を同じコミットで直す（コードのコメントが「決めは …」と指す先を壊さない）
- ゲート: `pnpm run check && pnpm run check:core && pnpm run test:core && pnpm test`

---

## ファイル構成

```
core/op/apply.mbt            sign_at（新）/ respell・as_item（新）/ cap（as_item で書く）/
                             add_node（sign_at を使う）/ graft（形にして席の綴りで差す）/
                             move_node（respell）
core/op/apply_wbtest.mbt     Graft・MoveNode・cap の期待値。総当たりの Op を足す
core/tree/content.mbt        pub fn lines（新）
core/tree/content_wbtest.mbt lines の試験
core/edit/edit_wbtest.mbt    md の入出力で貼り付けの見本を固定
core/edit/law_wbtest.mbt     総当たりの Op を足す
src/main.ts                  paste() が Graft を送るだけになる
src/app/paste.ts             消す
test/paste.test.ts           消す
src/app/copy.ts              冒頭コメント（paste.ts の対 → graft の対）
src/mindmap.ts               addLink のコメント（app/paste.ts を指している）
docs/core.md                 「操作」の決め 2 行、check の説明は変わらない
docs/spec.md                 Mod+V の段落、src の地図から paste.ts を消す
```

---

### Task 0: worktree

**Files:** なし

- [ ] **Step 1: 枝と worktree を作る**

```bash
cd D:/1.atrium/mmm && git worktree add .worktrees/feat/paste -b feat/paste main
```

- [ ] **Step 2: 依存とビルドが通ることを見る**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/paste && pnpm install --frozen-lockfile && pnpm run core && pnpm run test:core
```

Expected: `moon test` が全部 pass。以降のコマンドは全部この worktree の中で打つ。

---

### Task 1: `sign_at` — 席の綴りを名前にする

`add_node` の中に書かれている「新しいノードの綴りは隣に従う」を関数に出す。挙動は変えないので、既存の AddNode の試験がそのまま緑であることが試験。

**Files:**
- Modify: `core/op/apply.mbt`（`sign_in` の直後に `sign_at` を足し、`add_node` を書き直す）

**Interfaces:**
- Produces: `fn sign_at(doc : @tree.Doc, at : NodePlace) -> @tree.Sign?`（席が無ければ None）

- [ ] **Step 1: `sign_at` を足す**

`fn anchor(at : NodePlace) -> Int` の直前に:

```moonbit
///|
/// 席の綴り。新しく差すもの・運ぶものはこれに従う（「綴りは席が決める」）。
/// Before / After は隣の綴り、In は末尾の子の綴り、子が無ければ親（項目なら項目、
/// それ以外は見出し）、In(doc_id) は最後の根（無ければ見出し）。席が無ければ None。
fn sign_at(doc : @tree.Doc, at : NodePlace) -> @tree.Sign? {
  match at {
    Before(node~) => find(doc, node).map(sign_of)
    After(node~) => find(doc, node).map(sign_of)
    In(node~, ..) =>
      if node == @tree.doc_id {
        Some(
          match doc.roots.last() {
            Some(r) => sign_of(r.node)
            None => @tree.Heading
          },
        )
      } else {
        find(doc, node).map(sign_in)
      }
  }
}
```

- [ ] **Step 2: `add_node` を `sign_at` で書く**

```moonbit
///|
/// 新しいノードを差す。綴りは席が決める（`sign_at`）。
fn add_node(doc : @tree.Doc, at : NodePlace, labels : Array[String]) -> Done? {
  guard !labels.is_empty() else { return None }
  guard sign_at(doc, at) is Some(sign) else { return None }
  let first = fresh(doc)
  let news = labels.mapi((k, label) => make(sign, first + k, label))
  done(place(doc, at, news), Some(first))
}
```

- [ ] **Step 3: 試験**

```bash
cd core && moon fmt && moon test -p mmm-app/core/op
```

Expected: 全部 pass（挙動は同じ）。

- [ ] **Step 4: Commit**

```bash
git add core/op/apply.mbt && git commit -m "refactor: ♻️ #117 席の綴りを sign_at に出す（AddNode の規則を名前にする）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: `respell` と `cap` — 飛びは空の項目に綴る

部分木を綴り替える道具を作り、`cap` の `items` をそれで置き換える。決めが 1 つ変わる: 天井より深い Implicit は消えず、空の項目になる（`- - b`）。

**Files:**
- Modify: `core/op/apply.mbt`（`cap`、新しい `respell` / `as_item`）
- Modify: `core/op/apply_wbtest.mbt:1024-1064`（Graft と MoveNode の天井の試験）
- Modify: `docs/core.md`（`cap` の決めの行）

**Interfaces:**
- Produces: `fn respell(n : @tree.Node, sign : @tree.Sign) -> @tree.Node`、`fn as_item(n : @tree.Node) -> @tree.ListNode`

- [ ] **Step 1: 期待値を新しい決めに書き換える（失敗する試験を先に）**

`apply_wbtest.mbt` の `test "Graft は 6 段目の下なら部分木ごと項目にする。途中の飛びは畳む"` を:

```moonbit
///|
test "Graft は 6 段目の下なら部分木ごと項目にする。途中の飛びは空の項目" {
  assert_eq(
    run(
      six([]),
      Graft(at=NodePlace::In(node=7, side=None), md="# p\n\n## q\n\n- i\n"),
    ),
    Some({
      doc: six([item(8, "p", children=[li(9, "q", children=[li(10, "i")])])]),
      focus: Some(8),
    }),
  )
  // `#` → `###` の飛びは Implicit。項目は行を省けないので、空の項目として綴る（形は保つ）
  assert_eq(
    run(
      six([]),
      Graft(at=NodePlace::In(node=7, side=None), md="# p\n\n### q\n"),
    ),
    Some({
      doc: six([item(8, "p", children=[li(9, "", children=[li(10, "q")])])]),
      focus: Some(8),
    }),
  )
}
```

`test "MoveNode は 6 段目の下へ落とせば見出しを項目にする。Implicit は消え、focus も消える"` を:

```moonbit
///|
test "MoveNode は 6 段目の下へ落とせば見出しを項目にする。Implicit は空の項目になり、focus は残る" {
  let d = six([])
  let x = root(h(8, "x", body=[pic(9)], children=[h(10, "y")]), sides=[Right])
  let e : @tree.Doc = { ..d, roots: [..d.roots, x] }
  assert_eq(
    run(e, MoveNode(ids=[8], at=NodePlace::In(node=7, side=None))),
    Some({
      doc: six([item(8, "x", body=[pic(9)], children=[li(10, "y")])]),
      focus: Some(8),
    }),
  )
  let g = root(gap(8, [h(9, "z")]), sides=[Right])
  let f : @tree.Doc = { ..d, roots: [..d.roots, g] }
  assert_eq(
    run(f, MoveNode(ids=[8], at=NodePlace::In(node=7, side=None))),
    Some({ doc: six([item(8, "", children=[li(9, "z")])]), focus: Some(8) }),
  )
}
```

- [ ] **Step 2: 落ちることを見る**

```bash
cd core && moon test -p mmm-app/core/op -f apply_wbtest.mbt
```

Expected: 上の 2 本が FAIL（今は `li(10, "q")` が直下に来る / focus が None）。

- [ ] **Step 3: `respell` / `as_item` を足し、`cap` を書き直す**

`fn cap` の前に:

```moonbit
///|
/// 部分木をその綴りで書き直す。形（ラベル・中身・子・畳み・飛び）は保ち、綴りだけ替える。
/// 項目にするとき、飛び（Implicit）は行を省けないので空の項目として綴る（`- - b`）。
/// 見出しにするとき、Implicit はそのまま — 省いたままで読めない席かは check と mend が見る。
/// 決めは docs/superpowers/specs/2026-09-06-paste-design.md
fn respell(n : @tree.Node, sign : @tree.Sign) -> @tree.Node {
  match sign {
    Item => @tree.ListNode(as_item(n))
    Heading =>
      match n {
        HeadingNode(h) =>
          @tree.HeadingNode({
            ..h,
            children: h.children.map(c => respell(c, Heading)),
          })
        ImplicitNode(g) =>
          @tree.ImplicitNode({
            ..g,
            children: g.children.map(c => respell(c, Heading)),
          })
        ListNode(l) =>
          @tree.HeadingNode({
            id: l.id,
            label: l.label,
            fold: l.fold,
            body: l.body,
            children: l.children.map(c => respell(@tree.ListNode(c), Heading)),
          })
      }
  }
}

///|
/// 部分木を項目で書く。型が「項目の子孫は項目」なので、下は全部項目になる。
fn as_item(n : @tree.Node) -> @tree.ListNode {
  match n {
    ListNode(l) => l
    HeadingNode(h) =>
      { id: h.id, label: h.label, fold: h.fold, body: h.body, children: h.children.map(as_item) }
    ImplicitNode(g) =>
      { id: g.id, label: "", fold: None, body: [], children: g.children.map(as_item) }
  }
}
```

`cap` の中の `fn items(...)` を消し、doc コメントと `node` の最後の腕を:

```moonbit
///|
/// 見出しの天井。`@tree.deepest` より深くに着いた見出しは部分木ごと項目に
/// 綴り替える — 手で書くときも `######` の下は `- ` でしか続けられない。
/// 天井より深い Implicit は空の項目として綴る（飛びは形なので運ぶ。`- - b`）。
/// 天井の Implicit は行を省いた空の見出しなので残す
/// （子が項目になれば `GappedItem` — 綴れば読める）。上の段には触らないので、
/// parse の出力には何もしない。
fn cap(doc : @tree.Doc) -> @tree.Doc {
  fn node(n : @tree.Node, depth : Int, out : Array[@tree.Node]) -> Unit {
    fn under(xs : Array[@tree.Node]) -> Array[@tree.Node] {
      let children = []
      for c in xs {
        node(c, depth + 1, children)
      }
      children
    }

    match n {
      HeadingNode(h) if depth <= @tree.deepest =>
        out.push(@tree.HeadingNode({ ..h, children: under(h.children) }))
      ImplicitNode(g) if depth <= @tree.deepest =>
        out.push(@tree.ImplicitNode({ ..g, children: under(g.children) }))
      ListNode(_) => out.push(n)
      _ => out.push(respell(n, Item))
    }
  }
  // 根は深さ 1 なので必ず 1 つ残り、根の子の数も変わらない（sides はそのまま）
  {
    ..doc,
    roots: doc.roots.map(r => {
      let out = []
      node(r.node, 1, out)
      { ..r, node: out[0] }
    }),
  }
}
```

- [ ] **Step 4: 通ることを見る**

```bash
cd core && moon fmt && moon test -p mmm-app/core/op
```

Expected: 全部 pass。総当たり `apply の結果は check を通る` の `inspect(some, content="308")` は Some の数が変わらないはず（None だった操作は無い）。変わったら数字を読んで理由を言えること。

- [ ] **Step 5: docs/core.md の決めを直す**

「見出しの天井だけは断らずに綴り替える（`cap`）」の項の
`途中の Implicit は項目が飛びを綴れないので消え、子が繰り上がる。` を
`天井より深い Implicit は空の項目 \`- - b\` として綴る（飛びは形なので運ぶ）。` に。

- [ ] **Step 6: Commit**

```bash
git add core/op/apply.mbt core/op/apply_wbtest.mbt docs/core.md && git commit -m "feat: 🪜 #117 天井より深い飛びは消さず空の項目に綴る（respell を道具にする）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: `@tree.lines` — 段落かどうかは mdAst が答える

**Files:**
- Modify: `core/tree/content.mbt`（`bare` の後に `lines`）
- Test: `core/tree/content_wbtest.mbt`

**Interfaces:**
- Produces: `pub fn lines(text : String) -> Array[String]?`（段落 1 つなら行の列。前後の空白・`\r` を除き、空の行は落とす。段落でなければ None）

- [ ] **Step 1: 失敗する試験**

`content_wbtest.mbt` の末尾に:

```moonbit
///|
/// 貼り付けは一番外の段落だけを行ごとにノードにする。段落かどうかは mdAst が言う。
test "lines — 段落なら行の列、それ以外の中身は None" {
  assert_eq(lines("a\nb\n"), Some(["a", "b"]))
  assert_eq(lines("  a  \r\n\r\n"), Some(["a"]))
  assert_eq(lines("**a** and [b](c)\n"), Some(["**a** and [b](c)"]))
  assert_eq(lines("| a | b |\n|---|---|\n| 1 | 2 |\n"), None)
  assert_eq(lines("> q\n"), None)
  assert_eq(lines("<div>x</div>\n"), None)
  assert_eq(lines("```\nx\n```\n"), None)
  assert_eq(lines(""), None)
}
```

- [ ] **Step 2: 落ちることを見る**

```bash
cd core && moon test -p mmm-app/core/tree -f content_wbtest.mbt
```

Expected: FAIL（`lines` が無い）。

- [ ] **Step 3: 実装**

`content.mbt` の `bare` の直後に:

```moonbit
///|
/// 原文のまま持つ中身（Opaque）が段落 1 つなら、その行の列（前後の空白と `\r` を
/// 除き、空の行は落とす）。表・引用・HTML など段落でなければ None。
/// 貼るとき、一番外の段落だけを「行ごとにノード」にするための問い —
/// 段落かどうかは mdAst しか知らないので、ここで読み直す（unbuild の splice と同じ手）。
pub fn lines(text : String) -> Array[String]? {
  let blocks = read(text).children.filter(b => !(b is BlankLines(..)))
  guard blocks is [Paragraph(..)] else { return None }
  Some(
    text
    .split("\n")
    .map(l => l.trim(chars=" \t\r").to_string())
    .filter(l => !l.is_empty())
    .collect(),
  )
}
```

`split` が返す要素の型（`String` か `StringView`）で `to_string()` が要らないと言われたら外す。`merge.mbt:373` の `split("\n").collect()` と同じ書き方に揃える。

- [ ] **Step 4: 通ることを見る**

```bash
cd core && moon fmt && moon test -p mmm-app/core/tree -f content_wbtest.mbt
```

Expected: PASS。

- [ ] **Step 5: Commit**

```bash
git add core/tree/content.mbt core/tree/content_wbtest.mbt && git commit -m "feat: ✨ #117 tree.lines — 原文の中身が段落 1 つなら行の列を返す

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: `graft` — md を読んで形にし、席の綴りで差す

**Files:**
- Modify: `core/op/apply.mbt`（`graft`。`peel` はそのまま）
- Modify: `core/op/apply_wbtest.mbt`（Graft の試験。「項目の下に見出し、に None」と「根の間にも差せる」を書き換え、新しい試験を足す）

**Interfaces:**
- Consumes: `sign_at`（Task 1）、`respell`（Task 2）、`@tree.lines`（Task 3）、既存の `shift` / `peel` / `body` / `place` / `make` / `fresh` / `parent` / `anchor`

- [ ] **Step 1: 失敗する試験**

`test "Graft は空・知らない id・項目の下に見出し、に None"` を:

```moonbit
///|
test "Graft は空・知らない id に None。項目の下の見出しは項目になる" {
  let d = doc([root(h(2, "r", children=[item(3, "a")]), sides=[Right])])
  assert_eq(run(d, Graft(at=NodePlace::In(node=2, side=None), md="")), None)
  assert_eq(run(d, Graft(at=NodePlace::In(node=2, side=None), md="  \n\n")), None)
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=9, side=None), md="# p\n")),
    None,
  )
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=3, side=None), md="# p\n\n## q\n")),
    Some({
      doc: doc([
        root(
          h(2, "r", children=[item(3, "a", children=[li(4, "p", children=[li(5, "q")])])]),
          sides=[Right],
        ),
      ]),
      focus: Some(4),
    }),
  )
}
```

`test "Graft は根の間にも差せる。先頭の散文は行き先の親の body 末尾へ"` を:

```moonbit
///|
/// 断片の一番外の段落は行ごとにノード、それ以外の中身は行き先の親の body の末尾。
/// 文書（anchor 無し）へ中身は断る — 文書の散文はマップに出ない。
test "Graft の一番外 — 段落は行ごとにノード、中身は親の body、文書へ中身は None" {
  let d = doc([root(h(2, "r", body=[pic(3)]))])
  // 根の隣に段落 2 行 → 最後の根に従って見出しの根 2 つ
  assert_eq(
    run(d, Graft(at=NodePlace::After(node=2), md="text\nmore\n")),
    Some({
      doc: doc([root(h(2, "r", body=[pic(3)])), root(h(4, "text")), root(h(5, "more"))]),
      focus: Some(4),
    }),
  )
  // 中身 1 枚は親の body の末尾。focus はその中身
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=2, side=None), md="![](q.png)\n")),
    Some({
      doc: doc([
        root(
          h(2, "r", body=[pic(3), { id: 4, content: Image(alt="", src="q.png", title="") }]),
        ),
      ]),
      focus: Some(4),
    }),
  )
  // URL 1 行は読みがリンクと読むので中身（特別扱いは無い）
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=2, side=None), md="https://x\n")),
    Some({
      doc: doc([
        root(
          h(2, "r", body=[
            pic(3),
            { id: 4, content: Link(text="https://x", href="https://x", title="") },
          ]),
        ),
      ]),
      focus: Some(4),
    }),
  )
  // 文書へ中身は断る
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=@tree.doc_id, side=None), md="![](q.png)\n\n# s\n")),
    None,
  )
  // 混ざった断片 — 一番外の段落はノード、見出しの下の中身はその見出しのまま
  assert_eq(
    run(
      d,
      Graft(
        at=NodePlace::In(node=2, side=None),
        md="intro\n\n# a\n\n![](p.png)\n\n- b\n",
      ),
    ),
    Some({
      doc: doc([
        root(
          h(2, "r", body=[pic(3)], children=[
            h(4, "intro"),
            h(6, "a", body=[pic(7)], children=[h(8, "b")]),
          ]),
          sides=[Right, Right],
        ),
      ]),
      focus: Some(4),
    }),
  )
}
```

新しく:

```moonbit
///|
/// 断片が持ち込むのは形だけ。綴りは席が決める（`sign_at`）。
test "Graft は席の綴りで差す — 見出しの列では見出し、項目の列では項目" {
  // 子が見出しの見出しへ項目 — 見出しの直後の項目は飲み込まれるので、綴り替えないと書けない
  let d = doc([root(h(2, "r", children=[h(3, "a")]), sides=[Right])])
  assert_eq(
    run(d, Graft(at=NodePlace::In(node=2, side=None), md="- b\n  - c\n")),
    Some({
      doc: doc([
        root(h(2, "r", children=[h(3, "a"), h(4, "b", children=[h(5, "c")])]), sides=[
          Right,
          Right,
        ]),
      ]),
      focus: Some(4),
    }),
  )
  // 子が項目の見出しへ見出し（`##` 始まりの飛びは剥がす。Implicit の id 1 つが飛ぶ）
  let e = doc([root(h(2, "r", children=[item(3, "a")]), sides=[Right])])
  assert_eq(
    run(e, Graft(at=NodePlace::In(node=2, side=None), md="## b\n\n- c\n")),
    Some({
      doc: doc([
        root(
          h(2, "r", children=[item(3, "a"), item(5, "b", children=[li(6, "c")])]),
          sides=[Right, Right],
        ),
      ]),
      focus: Some(5),
    }),
  )
  // 子の無い見出しへ項目 — 隣がいないので親に従い見出し（Tab と同じ答え）
  let f = doc([root(h(2, "r"))])
  assert_eq(
    run(f, Graft(at=NodePlace::In(node=2, side=None), md="- b\n")),
    Some({ doc: doc([root(h(2, "r", children=[h(3, "b")]), sides=[Right])]), focus: Some(3) }),
  )
  // 根が見出しの文書へ項目 — 最後の根に従って見出しの根
  assert_eq(
    run(f, Graft(at=NodePlace::In(node=@tree.doc_id, side=None), md="- b\n")),
    Some({ doc: doc([root(h(2, "r")), root(h(3, "b"))]), focus: Some(3) }),
  )
}
```

- [ ] **Step 2: 落ちることを見る**

```bash
cd core && moon test -p mmm-app/core/op -f apply_wbtest.mbt
```

Expected: 上の 3 本が FAIL。

- [ ] **Step 3: `graft` を書き直す**

```moonbit
///|
/// md を読んで形にし、席の綴りで差す。断片が持ち込むのは形（ラベル・中身・子・畳み・
/// 飛び）だけで、`#` の本数・マーカー・字下げ・先頭の飛びは書き方なので捨てる。
/// 一番外の段落は行ごとにノード、それ以外の中身は行き先の親の body 末尾へ
/// （行き先が文書なら断る — 文書の散文はマップに出ない）。
/// 断片が `##` から始まると parse は根を Implicit で包む（深さ 1 の飛び）。
/// それは切り出した場所の名残でしかないので剥がし、相対の形で差す。
/// 決めは docs/superpowers/specs/2026-09-06-paste-design.md
fn graft(doc : @tree.Doc, at : NodePlace, md : String) -> Done? {
  let sub = @tree.parse(md)
  guard !(sub.roots.is_empty() && sub.body.is_empty()) else { return None }
  guard sign_at(doc, at) is Some(sign) else { return None }
  let host = match at {
    In(node~, ..) => node
    _ => {
      guard parent(doc, anchor(at)) is Some(p) else { return None }
      p
    }
  }
  // 一番外の中身を、行のノードになる段落と、body へ行く中身に分ける
  let labels : Array[String] = []
  let rest : Array[@tree.Block] = []
  for b in sub.body {
    let ls = match b.content {
      Opaque(text) => @tree.lines(text)
      _ => None
    }
    match ls {
      Some(ls) => labels.append(ls)
      None => rest.push(b)
    }
  }
  // 行のノードは文書に無い番号から、断片の id はその先へ寄せる
  let first = fresh(doc)
  let by = first + labels.length() - (@tree.doc_id + 1)
  let heads = labels.mapi((k, label) => make(sign, first + k, label))
  let prose = rest.map(b => ({ id: b.id + by, content: b.content } : @tree.Block))
  let subs = peel(sub.roots.map(r => r.node)).map(n => respell(shift(n, by), sign))
  let nodes = [..heads, ..subs]
  let mut cur = doc
  if !prose.is_empty() {
    guard host != @tree.doc_id else { return None }
    guard body(cur, node=host, bs => [..bs, ..prose]) is Some(d) else {
      return None
    }
    cur = d
  }
  if nodes.is_empty() {
    return Some({ doc: cur, focus: Some(prose[0].id) })
  }
  done(place(cur, at, nodes), Some(id_of(nodes[0])))
}
```

`labels.append(ls)` が無ければ `for l in ls { labels.push(l) }`。

- [ ] **Step 4: 通ることを見る**

```bash
cd core && moon fmt && moon test -p mmm-app/core/op
```

Expected: 全部 pass。`test "Graft は parse した根たちを差す。側は捨て、id は鋳直す"` と `"## 始まりの断片は先頭の Implicit を剥がす"` 系は変わらず通る（見出しの列へ見出し）。総当たりの `inspect(some, ...)` の数字が変われば読む。

- [ ] **Step 5: Commit**

```bash
git add core/op/apply.mbt core/op/apply_wbtest.mbt && git commit -m "feat: ✨ #117 Graft は断片を形にして席の綴りで差す（段落は行ごとにノード、中身は body）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: `move_node` — 動かすものも形だけ運ぶ

**Files:**
- Modify: `core/op/apply.mbt`（`move_node`）
- Modify: `core/op/apply_wbtest.mbt:486-506`（「MoveNode は綴りを保つ」）
- Modify: `docs/core.md`（「動かすノードは綴りを保つ」の項）

- [ ] **Step 1: 失敗する試験**

`test "MoveNode は綴りを保つ。項目は見出しの下に置け、見出しは項目の下に置けない"` を:

```moonbit
///|
test "MoveNode は形だけ運び、綴りは席が決める" {
  let d = doc([
    root(h(2, "r", children=[h(3, "a"), item(4, "b")]), sides=[Right, Left]),
  ])
  // 子の無い見出しの下へ項目 — 親に従って見出し
  assert_eq(
    run(d, MoveNode(ids=[4], at=NodePlace::In(node=3, side=None))),
    Some({
      doc: doc([
        root(h(2, "r", children=[h(3, "a", children=[h(4, "b")])]), sides=[
          Right,
        ]),
      ]),
      focus: Some(4),
    }),
  )
  // 項目の下へ見出し — 項目
  assert_eq(
    run(d, MoveNode(ids=[3], at=NodePlace::In(node=4, side=None))),
    Some({
      doc: doc([
        root(h(2, "r", children=[item(4, "b", children=[li(3, "a")])]), sides=[
          Left,
        ]),
      ]),
      focus: Some(3),
    }),
  )
  // 見出しの後ろへ項目 — 隣に従って見出し（今まで Swallowed で断られていた席）
  assert_eq(
    run(d, MoveNode(ids=[4], at=NodePlace::After(node=3))),
    Some({
      doc: doc([root(h(2, "r", children=[h(3, "a"), h(4, "b")]), sides=[Right, Right])]),
      focus: Some(4),
    }),
  )
}
```

2 つ目の `sides` は take の後の残りの sides（b の Left）になるはず。違えば実測の値を書き、理由をコメントに残す。

- [ ] **Step 2: 落ちることを見る**

```bash
cd core && moon test -p mmm-app/core/op -f apply_wbtest.mbt
```

Expected: FAIL（1 つ目は項目のまま、2 つ目は None）。

- [ ] **Step 3: 実装**

```moonbit
///|
/// 動かす。文書順を保って抜き、席に差す。側は行き先が決め、綴りも席が決める（`respell`）。
fn move_node(doc : @tree.Doc, ids : Array[Int], at : NodePlace) -> Done? {
  guard !ids.is_empty() else { return None }
  for id in ids {
    guard find(doc, id) is Some(_) else { return None }
  }
  let picked = pick(doc, ids)
  guard !within(doc, anchor(at), picked) else { return None }
  let taken = []
  guard take(doc, picked, taken) is Some(rest) else { return None }
  guard sign_at(rest, at) is Some(sign) else { return None }
  done(place(rest, at, taken.map(n => respell(n, sign))), Some(picked[0]))
}
```

- [ ] **Step 4: 通ることを見る**

```bash
cd core && moon fmt && moon test -p mmm-app/core/op
```

Expected: 全部 pass。総当たりの Some の数は増える（項目の下への MoveNode が通るようになる）— `inspect` を `-u` で更新し、増えた理由がこれだけかを数字で確かめる（`moon test -p mmm-app/core/op -u`）。

- [ ] **Step 5: docs/core.md の決めを直す**

「**動かすノードは綴りを保つ。** 綴りの変換（見出し ⇄ 項目）は正規形の仕事で、操作には無い。見出しを項目の下へ落とすのは型が許さないので `None`」を:

```
- **差すもの・運ぶものは形だけを持ち込み、綴りは席が決める（`sign_at`）。** 新しい
  ノードも、貼る断片（`Graft`）も、動かす部分木（`MoveNode`）も、隣の綴りに従う —
  兄弟がいればその sign、いなければ親が項目なら項目、それ以外は見出し。根の隣
  （`In(doc_id)`）は最後の根に従う。部分木の下は、親が項目なら全部項目（型）、
  親が見出しなら見出しで続けて天井で `cap` が項目にする（`respell`）。見出しを
  項目の下へ落とせば項目になる。モードは Reform と一緒に来る
```

その直前の「新しいノードの綴りは隣に従う」の項はこれに吸収されるので消す。

- [ ] **Step 6: Commit**

```bash
git add core/op/apply.mbt core/op/apply_wbtest.mbt docs/core.md && git commit -m "feat: ✨ #117 MoveNode も形だけ運び、綴りは席が決める

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: 総当たりと md の見本

**Files:**
- Modify: `core/op/apply_wbtest.mbt`（`apply の結果は check を通る` の ops）
- Modify: `core/edit/law_wbtest.mbt:145-166`（`ops_for`）
- Modify: `core/edit/edit_wbtest.mbt`（貼り付けの見本）

- [ ] **Step 1: 総当たりに項目の断片と中身の断片を足す**

`apply_wbtest.mbt` の `ops.push(Graft(at=NodePlace::In(node=to, side=None), md="# g\n\n- i\n"))` の直後に:

```moonbit
      ops.push(Graft(at=NodePlace::In(node=to, side=None), md="- i\n  - j\n"))
      ops.push(Graft(at=NodePlace::After(node=to), md="![](q.png)\n\ntext\n"))
```

`law_wbtest.mbt` の `ops_for` の `Graft` の直後に同じ 2 行（`In(node=to, side=None)` / `After(node=to)` の綴り）、末尾の `ops.push(AddBlock(at=In(node=@tree.doc_id), ...))` の後に:

```moonbit
  ops.push(Graft(at=In(node=@tree.doc_id, side=None), md="- i\n"))
  ops.push(Graft(at=In(node=@tree.doc_id, side=None), md="text\n"))
```

- [ ] **Step 2: 回して、破れが無いことを見る**

```bash
cd core && moon test -p mmm-app/core/op -p mmm-app/core/edit
```

Expected: `json_inspect(bad, content=[])` は空のまま。`inspect(some, ...)` だけが変わるので `-u` で更新して数字を読む。法則が落ちたら（読み直した木と形が違う・空行が増えた）、それは Graft か unbuild の穴なので、落ちた md を最小化して Task 4 に戻る。

- [ ] **Step 3: md の見本を edit_wbtest に固定する**

`edit_wbtest.mbt` の末尾に:

```moonbit
///|
/// 貼り付けは Graft そのもの（TS は raw を送るだけ）。断片が持ち込むのは形で、綴りは席。
/// 見本は docs/superpowers/specs/2026-09-06-paste-design.md の例
test "Graft — 貼り付けの見本" {
  // 項目を、子が見出しの見出しへ → 見出し
  assert_eq(
    applied("# r\n\n## a\n", Graft(at=In(node=2, side=None), md="- b\n")),
    ("# r\n\n## a\n\n## b\n", Some(4)),
  )
  // 見出し + 項目を、子が項目の見出しへ → 項目
  assert_eq(
    applied("# r\n\n- a\n", Graft(at=In(node=2, side=None), md="## b\n\n- c\n")),
    ("# r\n\n- a\n- b\n  - c\n", Some(4)),
  )
  // カード 1 枚は中身
  assert_eq(
    applied("# r\n", Graft(at=In(node=2, side=None), md="![](q.png)\n")),
    ("# r\n\n![](q.png)\n", Some(3)),
  )
  // 文書へ中身は断る
  assert_eq(
    edit("# r\n", Graft(at=In(node=@tree.doc_id, side=None), md="![](q.png)\n")),
    { edits: [], focus: None },
  )
  // 途中の飛びを 6 段目へ — 飛びは空の項目 `- - - b`
  let six = "# 1\n\n## 2\n\n### 3\n\n#### 4\n\n##### 5\n\n###### t\n"
  assert_eq(
    applied(six, Graft(at=In(node=7, side=None), md="# a\n\n#### b\n")),
    (six + "\n- a\n\n  - - - b\n", Some(8)),
  )
}

///|
test "MoveNode — 項目を子が見出しの見出しへ動かせば見出しになる" {
  assert_eq(
    applied(
      "# r\n\n## a\n\n# s\n\n- b\n",
      MoveNode(ids=[5], at=In(node=2, side=None)),
    ),
    ("# r\n\n## a\n\n## b\n\n# s\n", Some(4)),
  )
}
```

- [ ] **Step 4: 回す**

```bash
cd core && moon fmt && moon test -p mmm-app/core/edit
```

Expected: PASS。正規形の空行の数が違えば期待値を実物に合わせる（正規形は unbuild の決め。ここで変えない）。

- [ ] **Step 5: Commit**

```bash
git add core/op/apply_wbtest.mbt core/edit/law_wbtest.mbt core/edit/edit_wbtest.mbt && git commit -m "test: ✅ #117 貼り付けの見本を md で固定し、総当たりに項目と中身の断片を足す

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: TS — 貼り付けは raw を Graft へ

**Files:**
- Modify: `src/main.ts:35, 508-575`（`paste()`）
- Delete: `src/app/paste.ts`、`test/paste.test.ts`
- Modify: `src/app/copy.ts:1-3`（冒頭コメント）
- Modify: `src/mindmap.ts:337-341`（`addLink` のコメント）
- Modify: `docs/spec.md`（src の地図の `paste(...)` 行、`Mod+V` の文）

- [ ] **Step 1: `paste()` の字の道を書き直す**

`import { decidePaste } from "./app/paste.ts";` を消し、`paste()` の doc コメントと字の道を:

```ts
/**
 * クリップボードを貼る（Mod+V）。画像はテキストより優先し、選んでいる
 * ノード（anchor）へ足す。字は raw のまま `Graft` へ — 何を貼るか（見出し・項目は
 * 形だけ運んで綴りは貼り先に従う、段落は行ごとに子、それ以外の中身はカード）は
 * core が読んで決める。TS は `#` も URL も見ない。
 * 決めは docs/superpowers/specs/2026-09-06-paste-design.md
 */
```

`const clip = await navigator.clipboard.readText();` 以降を:

```ts
    const clip = await navigator.clipboard.readText();
    if (gen !== docGen) return;
    // 空はしくじりではなく「何も無い」なので黙る
    if (clip.trim() === "") return;
    apply({ kind: "graft", at: { kind: "in", node: anchor ?? core.DOC_ID, side: null }, md: clip }, false);
```

- [ ] **Step 2: 消す・コメントを直す**

```bash
git rm src/app/paste.ts test/paste.test.ts
```

`src/app/copy.ts` の冒頭 `paste.ts の対で、貼る側の graft が断片を parse して木として差すので、深さの付け直しはしない。` を `貼る側（main.ts の paste → core の Graft）が断片を形として読み直すので、深さも綴りも付け直さない。` に。

`src/mindmap.ts` の `addLink` のコメント `（app/paste.ts は骨格の有無を core に読ませるが、ここは「URL か否か」だけの単純な形）` を `（Mod+V は raw を core に読ませるが、ここは「URL か否か」だけを見て、違えば言う）` に。

- [ ] **Step 3: docs/spec.md**

src の地図の `paste(クリップボードから何を貼るかの判定。骨格の有無は core に読ませる) /` の行を消す。

「構造・選択」の `` `Mod+V` の中身は、見出しつき md なら**子ツリー**に、ただのテキストなら**行ごとに子ノード**に、URL 単体なら**そのノードの内容**(リンクカード)に、画像は保存して `![]()` を挿入する。`` を:

```
`Mod+V` は字を raw のまま core に読ませる。見出し・項目は**形だけ**運び、綴りは
貼り先に従う(子が見出しなら見出し、項目なら項目。深い所は天井で項目)。ただの
テキストは**行ごとに子ノード**、画像・リンク・コードなどの中身は**そのノードの
内容**(カード。URL 1 行もリンクカード)、画像のデータは保存して `![]()` を挿入する。
```

- [ ] **Step 4: 通す**

```bash
pnpm run check && pnpm test
```

Expected: `check` 0 errors、test は paste.test.ts が消えた分だけ減って全部 pass。

- [ ] **Step 5: 実機**

`preview_start` は他セッションの launch.json を触るので、主ディレクトリの `.claude/launch.json` に worktree 用の 1 件（`pnpm --dir D:/1.atrium/mmm/.worktrees/feat/paste exec vite --port 13140 --strictPort`、port 13140）を足して開く。クリップボードは差し替える:
`Object.defineProperty(navigator, 'clipboard', {value: {readText: async () => '- x', writeText: async () => {}}})`。
`#md=` の hash で issue #117 の文書を開き、シナリオを選んで Mod+V → `#### x` が末尾に付くこと。`![](q.png)` を貼って画像カードになること。終わったら launch.json の追加分は戻す。

- [ ] **Step 6: Commit**

```bash
git add -A src test docs/spec.md && git commit -m "feat: ✨ #117 Mod+V は字を raw のまま Graft へ渡す（判定は core の 1 か所）

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: PR

- [ ] **Step 1: ゲート**

```bash
pnpm run check && pnpm run check:core && pnpm run test:core && pnpm test
```

- [ ] **Step 2: push と PR**

```bash
git push -u origin feat/paste
```

`gh pr create` の本文は spec の「決め」の要約 + 例の表 + `Closes #117`、末尾に `🤖 Generated with [Claude Code](https://claude.com/claude-code)`。マージは squash（`gh pr merge --squash`、`--delete-branch` は付けない。リモート枝は `git push origin --delete feat/paste`、worktree は main 側から `git worktree remove`）。

---

## Self-Review

- spec「字は raw で core へ」→ Task 7。「形だけ」「綴りは席」→ Task 1・4・5。「散文は一番外の段落だけ行ごと」「中身は body、文書へは断る」→ Task 3・4。「URL は特別扱いしない」→ Task 4 の試験。「飛びは形、項目の列では空の項目」→ Task 2。「先頭の飛びは剥がす」→ 既存 `peel`（変えない）。「frontmatter は捨てる」→ parse の `sub.frontmatter` を読まない（既存どおり）。「MoveNode も揃える」→ Task 5。試験の 4 つ → Task 2〜6。docs → Task 2・5・7
- 型の名: `sign_at` / `respell` / `as_item` / `@tree.lines` は全 Task で同じ綴り
- 見送り: 押す前に沈める判定、#118 の 3 つ、Reform
