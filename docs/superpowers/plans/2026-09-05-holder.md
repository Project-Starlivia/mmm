# holder — 選択の持ち主を 1 つに（計画）

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 選択を 1 つにし、持っている側（フォーカスのあるペイン）が決める。core の `follow` / ts の幽霊 / `keep` を消し、core の答え（View + 地番）・地図の選択の位置・持ち主・選択を CodeMirror の EditorState の field に置く。

**Architecture:** core は `survey(md)` / `edit(md, op)` のまま（`delete` がノードでも隣の focus を返すようになる）。ts は `state.ts` に 4 つの StateField（tree / anchors / holder / choice）と 3 つの effect を持ち、`caret.ts` の純関数 `derive` が選択を導く。main.ts は変数を持たず、CodeMirror の 1 トランザクションが 1 サイクル。操作の入口は `apply`（focus を選ぶ）と `write`（選択に触らない）の 2 つ。

**Tech Stack:** MoonBit（core、`moon test`）、TypeScript + CodeMirror 6（`@codemirror/state` 6.7 / `view` 6.43）、Node 24 の `node --test`（型ストリップで .ts をそのまま走らせる）、Vite。

**Spec:** `docs/superpowers/specs/2026-09-05-holder-design.md`（決めの根拠は全部ここ。棚卸しは `ai-docs/codemirror.md`）

## Global Constraints

- 作業は worktree `D:\1.atrium\mmm\.worktrees\feat\holder`（枝 `feat/holder`、main から）。Bash の cwd は勝手に主ディレクトリへ戻ることがあるので、**編集の前に `pwd` を出すか絶対パスで書く**
- コミットは `<type>: <emoji> #49 <日本語の題>`、末尾に `Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>`。1 タスク 1 コミット
- TS: **`as` と `!`（non-null assertion）を書かない。** `instanceof` / 型ガード / 既定値で確かめる。画面に出る文字は英語、コードのコメントと docs は日本語
- TS のファイルは **Write / Edit ツールで書く**（シェルの heredoc はバックスラッシュを 1 段落とす）。`.mbt` も同じ
- core: `moon fmt` に通る形（`pnpm run check:core`）。局所関数は定義順（相互再帰不可）。`try?` は deprecated
- 試験: `pnpm test`（core をビルドしてから TS）/ `pnpm run test:core` / `pnpm run check` / `pnpm run check:core`。**どのコミットでも 4 つ全部が通る**
- rebase はしない。main が進んでいたら `git merge main`
- spec の決めを変えない。変えたくなったら止めて相談

---

## ファイル構成

```
core/op/walk.mbt             siblings / ancestors（読むだけの道具）を足す
core/op/apply.mbt            delete の focus（heirs）
core/op/apply_wbtest.mbt     Delete の focus の表
core/edit/law_wbtest.mbt     「目印の生存」を消し、「Delete の focus」の法則を足す
core/tree/follow.mbt         消す
core/tree/follow_wbtest.mbt  消す
core/tree/build.mbt          Spot.label のコメント（理由を anchors に）
core/tree/js/exports.mbt     mmmSurvey(md)

src/coreApi.ts               survey(md)。Mark / Trail / trails を消す
src/caret.ts                 caretIds の点の規則、buried、Holder / Anchors / Caret、derive
src/map/select.ts            neighbor / under を消す
src/map/keys.ts              Intent.keep を消す
src/state.ts                 tree / anchors / holder / choice の StateField、effect、anchorsOf / anchorsFor（新設）
src/editor.ts                field を載せる。updateListener 1 本。apply(sets, focus?) / select / hold
src/mindmap.ts               MapHost に holder()。輪 or 枠の塗り分け。apply の第 3 引数を消す
src/main.ts                  変数と半サイクルを畳む。holder の focusin、choose、apply / write

test/coreApi.test.ts         survey(md) の形
test/copy.test.ts            survey(md)
test/caret.test.ts           点の規則、buried、derive
test/select.test.ts          neighbor の行を消す
test/keys.test.ts            keep を消す
test/state.test.ts           EditorState を node で流す（新設）

docs/design.md, docs/core.md, docs/spec.md
```

---

### Task 1: core — Delete のノードの focus は次の兄弟 → 前の兄弟 → 親

**Files:**
- Modify: `core/op/walk.mbt`（末尾に 2 関数）
- Modify: `core/op/apply.mbt:105-131`（`delete` と、その前に `heirs`）
- Modify: `core/op/apply_wbtest.mbt:119-151`（期待値）、新しい test を 1 つ
- Modify: `core/edit/law_wbtest.mbt`（新しい法則を 1 つ）

**Interfaces:**
- Consumes: `walk.mbt` の `each` / `find` / `parent` / `owner` / `pick` / `has` / `kids` / `id_of`、`apply.mbt` の `erase`
- Produces: `delete(doc, ids) -> Done?` の focus が、ノードを消しても `Some`（消えていない次の兄弟 → 前の兄弟 → 祖先。中身なら持ち主）。何も残らなければ `None`

- [ ] **Step 1: 期待値を新しい規則に書き換える（既存の 3 test）**

`core/op/apply_wbtest.mbt` の 3 つの Delete の test を次にする。

```moonbit
///|
test "Delete は部分木ごと。根の子なら side も、Implicit の掃除も。focus は残った親" {
  let d = doc([
    root(
      h(2, "r", children=[h(3, "a", children=[h(4, "x")]), gap(5, [h(6, "y")])]),
      sides=[Right, Left],
    ),
  ])
  // a の次の兄弟は Implicit 5 だが y ごと消えるので、親 r
  assert_eq(
    run(d, Delete(ids=[4, 6, 3])),
    Some({ doc: doc([root(h(2, "r"), sides=[])]), focus: Some(2) }),
  )
}

///|
test "Delete は中身も 1 枚ずつ（持ち主が focus）。根も Root ごと（隣の根が focus）" {
  let d = doc([root(h(2, "r", body=[pic(3), code(4)])), root(h(5, "s"))])
  assert_eq(
    run(d, Delete(ids=[3])),
    Some({
      doc: doc([root(h(2, "r", body=[code(4)])), root(h(5, "s"))]),
      focus: Some(2),
    }),
  )
  // 持ち主ごと消せば、先頭は文書順で r（ノード）なので、次の根 s
  assert_eq(
    run(d, Delete(ids=[2, 3])),
    Some({ doc: doc([root(h(5, "s"))]), focus: Some(5) }),
  )
  // 末尾の根を消せば前の根
  assert_eq(
    run(d, Delete(ids=[5])),
    Some({ doc: doc([root(h(2, "r", body=[pic(3), code(4)]))]), focus: Some(2) }),
  )
}

///|
test "Delete は空と知らない id に None" {
  let d = doc([root(h(2, "r"))])
  assert_eq(run(d, Delete(ids=[])), None)
  assert_eq(run(d, Delete(ids=[2, 9])), None)
}
```

- [ ] **Step 2: 新しい規則の test を足す**

`core/op/apply_wbtest.mbt` の「Delete は空と知らない id に None」の直後に足す。

```moonbit
///|
test "Delete したノードの focus は次の兄弟 → 前の兄弟 → 親。消えるものは飛ばし、畳まれた兄弟はそれ自身" {
  let d = doc([
    root(
      h(2, "r", children=[h(3, "a"), h(4, "b", children=[h(5, "x")]), h(6, "c")]),
      sides=[Right, Right, Right],
    ),
  ])
  let focus = (op : Op) => run(d, op).map(x => x.focus)
  // 次の兄弟
  assert_eq(focus(Delete(ids=[3])), Some(Some(4)))
  // 末尾なら前の兄弟。その子孫（x）ではない
  assert_eq(focus(Delete(ids=[6])), Some(Some(4)))
  // 次が一緒に消えるなら飛ばす
  assert_eq(focus(Delete(ids=[3, 4])), Some(Some(6)))
  // 兄弟が尽きれば親
  assert_eq(focus(Delete(ids=[3, 4, 6])), Some(Some(2)))
  // 子孫も選んでいても、基準は文書順の先頭（a）
  assert_eq(focus(Delete(ids=[5, 3])), Some(Some(4)))
  // 最後の根なら何も無い
  assert_eq(run(doc([root(h(2, "r"))]), Delete(ids=[2])).map(x => x.focus), Some(None))
  // 前の兄弟が畳まれていれば、その兄弟そのもの（埋もれた子孫には落ちない）
  let folded : @tree.Fold = { open: false, summary: Some("b") }
  let f = doc([
    root(
      h(2, "r", children=[
        h(3, "b", fold=Some(folded), children=[h(4, "y")]),
        h(5, "c"),
      ]),
      sides=[Right, Right],
    ),
  ])
  assert_eq(run(f, Delete(ids=[5])).map(x => x.focus), Some(Some(3)))
}
```

- [ ] **Step 3: 落ちることを確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder/core && moon test -p mmm-app/core/op`
Expected: Delete の test が 4 つ FAIL（focus が None / 期待と違う）。他は PASS

- [ ] **Step 4: walk.mbt に兄弟と祖先を足す**

`core/op/walk.mbt` の末尾（`has` の後）に足す。

```moonbit
///|
/// 同じ親の子の id、文書順。根なら根どうし。中身には応えない（空）。
fn siblings(doc : @tree.Doc, id : Int) -> Array[Int] {
  match parent(doc, id) {
    Some(p) if p != @tree.doc_id =>
      match find(doc, p) {
        Some(n) => kids(n).map(id_of)
        None => []
      }
    Some(_) => doc.roots.map(r => id_of(r.node))
    None => []
  }
}

///|
/// 祖先の id、近いほうから。根なら空。中身なら持ち主から。
fn ancestors(doc : @tree.Doc, id : Int) -> Array[Int] {
  let found : Ref[Array[Int]] = { val: [] }
  each(doc, fn(x, _, path) { if x == id { found.val = path.rev() } })
  found.val
}
```

`siblings` で中身の id を渡すと `parent` は持ち主を返すので、その子の列（ノード）が返ってしまう。呼ぶ側（`heirs`）は中身には呼ばない。

- [ ] **Step 5: apply.mbt の delete を書き換える**

`core/op/apply.mbt` の `delete`（と直前のコメント）を次に置き換え、その前に `heirs` を置く。

```moonbit
///|
/// 消した後に選ぶ候補、近いほうから。中身なら持ち主とその祖先。ノードなら
/// 次の兄弟たち → 前の兄弟たち（近い順）→ 祖先。消えるものは呼ぶ側が落とす。
/// 兄弟と親は、消したノードが見えていれば見えている（畳まれるのは子孫）ので、
/// 埋もれたノードには落ちない。
fn heirs(doc : @tree.Doc, id : Int) -> Array[Int] {
  let out : Array[Int] = []
  if owner(doc, block=id) is None {
    let sibs = siblings(doc, id)
    match sibs.search(id) {
      Some(i) => {
        for k in (i + 1)..<sibs.length() {
          out.push(sibs[k])
        }
        for k in 0..<i {
          out.push(sibs[i - 1 - k])
        }
      }
      None => ()
    }
  }
  for a in ancestors(doc, id) {
    out.push(a)
  }
  out
}

///|
/// 選んだものを文書順に消す。知らない id が 1 つでもあれば None。
/// focus は先頭（文書順）の消したものの隣 — 中身なら持ち主、ノードなら次の兄弟 →
/// 前の兄弟 → 親。消えるものは飛ばす。何も残らなければ None（最後の根、文書の散文）。
/// 「消した後に何を選ぶか」の答えはこの focus 1 本（ts は隣を決めない）。
fn delete(doc : @tree.Doc, ids : Array[Int]) -> Done? {
  guard !ids.is_empty() && ids.all(id => has(doc, id)) else { return None }
  let gone = pick(doc, ids)
  let wants = heirs(doc, gone[0])
  let mut cur = doc
  for id in gone {
    guard erase(cur, id) is Some(d) else { return None }
    cur = d
  }
  let mut focus = None
  for w in wants {
    if focus is None && has(cur, w) {
      focus = Some(w)
    }
  }
  Some({ doc: cur, focus })
}
```

- [ ] **Step 6: op の試験が通ることを確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder/core && moon test -p mmm-app/core/op`
Expected: 全部 PASS

- [ ] **Step 7: 法則を足す（law_wbtest）**

`core/edit/law_wbtest.mbt` の頭のコメントの `目印の生存 — …` の行を次に置き換える。

```
//   Delete の focus — 消した後の focus は消えていない兄弟か祖先（中身なら持ち主）で、
//              畳みの中に埋もれていない。無いのは最後の根と文書の散文を消したときだけ
```

`under_fold` の直後（`owners_of` の前）に 2 つの道具と test を足す。

```moonbit
///|
/// ノードの親の id。根なら doc_id。中身と知らない id は None
fn parent_of(doc : @tree.Doc, id : Int) -> Int? {
  let found : Ref[Int?] = { val: None }
  fn list(l : @tree.ListNode, above : Int) {
    if l.id == id {
      found.val = Some(above)
    }
    for k in l.children {
      list(k, l.id)
    }
  }

  fn node(x : @tree.Node, above : Int) {
    let (mine, kids) = match x {
      HeadingNode(h) => (h.id, h.children)
      ListNode(l) => {
        list(l, above)
        return
      }
      ImplicitNode(g) => (g.id, g.children)
    }
    if mine == id {
      found.val = Some(above)
    }
    for k in kids {
      node(k, mine)
    }
  }

  for r in doc.roots {
    node(r.node, @tree.doc_id)
  }
  found.val
}

///|
/// f が id の祖先か（親も含む。doc_id は祖先に数えない）
fn is_ancestor(doc : @tree.Doc, f : Int, id : Int) -> Bool {
  let mut cur = parent_of(doc, id)
  while cur is Some(p) && p != @tree.doc_id {
    if p == f {
      return true
    }
    cur = parent_of(doc, p)
  }
  false
}

///|
test "法則 — Delete の focus は消えていない兄弟か祖先（中身なら持ち主）で、畳みの中に埋もれていない" {
  let bad : Array[String] = []
  let mut n = 0
  for i, md in corpus {
    let before = @tree.parse(md)
    for id in ids(before) {
      guard @op.apply(before, Delete(ids=[id])) is Some(done) else { continue }
      let what = "\{i} Delete \{id}"
      let block_owner = owner_of(before, id)
      match done.focus {
        None => {
          let sole_root = before.roots.length() == 1 &&
            parent_of(before, id) == Some(@tree.doc_id)
          if !sole_root && block_owner != Some(@tree.doc_id) {
            bad.push("\{what}: focus が無い")
          }
        }
        Some(f) => {
          n = n + 1
          if !ids(done.doc).contains(f) {
            bad.push("\{what}: focus \{f} が後の木に無い")
            continue
          }
          let near = match block_owner {
            Some(o) => o == f || is_ancestor(before, f, o)
            None =>
              parent_of(before, f) == parent_of(before, id) ||
              is_ancestor(before, f, id)
          }
          if !near {
            bad.push("\{what}: focus \{f} は兄弟でも祖先でもない")
          }
          match parent_of(done.doc, f) {
            Some(p) if p != @tree.doc_id && under_fold(done.doc, p) =>
              bad.push("\{what}: focus \{f} は埋もれている")
            _ => ()
          }
        }
      }
    }
  }
  json_inspect(bad, content=[])
  // 網の広さ。corpus を触れば動く
  inspect(n, content="")
}
```

- [ ] **Step 8: 数を埋めて通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder/core && moon test -p mmm-app/core/edit --update`
Expected: `inspect(n, content="")` が実際の数（0 より大きい）で埋まる。`bad` は空。`git diff core/edit/law_wbtest.mbt` で埋まった数だけが差分であることを見る

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run test:core && pnpm run check:core`
Expected: 全部 PASS、fmt の差分なし（差分が出たら `cd core && moon fmt` で整える）

- [ ] **Step 9: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add core/op/walk.mbt core/op/apply.mbt core/op/apply_wbtest.mbt core/edit/law_wbtest.mbt && git commit -m "feat: ✨ #49 Delete したノードの focus は次の兄弟 → 前の兄弟 → 親

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 2: core — follow を消し、mmmSurvey(md) にする

**Files:**
- Delete: `core/tree/follow.mbt`、`core/tree/follow_wbtest.mbt`
- Modify: `core/tree/js/exports.mbt:50-86`
- Modify: `core/edit/law_wbtest.mbt`（「目印の生存」の test と `owners_of` を消す）
- Modify: `core/tree/build.mbt:45-47`（Spot のコメント）

**Interfaces:**
- Produces: `mmmSurvey(md) -> "{ view, spots }"`（JS の export）。`Mark` / `Trail` / `follow` は無くなる

- [ ] **Step 1: 2 ファイルを消す**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git rm core/tree/follow.mbt core/tree/follow_wbtest.mbt
```

- [ ] **Step 2: exports.mbt の mmmSurvey を書き換える**

`core/tree/js/exports.mbt` の `mmm_survey` とその doc コメント、`Survey` struct を次に置き換える。

```moonbit
///|
/// 打鍵 1 回ぶんの読み。View と地番を 1 度の parse で返す（読みは打鍵ごとに 1 回に保つ）。
/// 選択の持ち越しは core に無い — 位置は ts が CodeMirror に預けて写す。
#export_name("mmmSurvey")
pub fn mmm_survey(md : String) -> String {
  let (doc, spots) = @tree.survey(md)
  let out : Survey = { view: @view.project(doc), spots }
  ToJson::to_json(out).stringify()
}

///|
/// mmmSurvey の返り値の形。JSON にするためだけの器
priv struct Survey {
  view : @view.View
  spots : Map[Int, @tree.Spot]
} derive(ToJson)
```

- [ ] **Step 3: law_wbtest から目印の生存を消す**

`core/edit/law_wbtest.mbt` から `owners_of` 関数（`/// 中身だけを変える操作で、目印を置いておきたい持ち主のノード。` から `out\n}` まで）と、test `"法則 — 目印の生存。…"` を丸ごと消す。`under_fold` は Task 1 の法則が使うので残す。

- [ ] **Step 4: build.mbt のコメントを直す**

`core/tree/build.mbt` の Spot の doc コメント（`/// 選択が md の打鍵をまたいで同じノードを指し続けるための目印（follow.mbt）。` の行）を次にする。

```
/// 地図の選択の位置（ts の anchors）に使うラベルの頭。位置は CodeMirror が編集で写す。
```

- [ ] **Step 5: 通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run test:core && pnpm run check:core && pnpm run core`
Expected: 全部 PASS。`follow` を参照する場所が残っていればここで落ちる（`grep -rn "follow\|Mark\|Trail" core --include=*.mbt` が exports / law の残りを出さないこと）

- [ ] **Step 6: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add -A core && git commit -m "refactor: ♻️ #49 follow を消す — 位置の写しは CodeMirror に預ける

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 3: ts — coreApi の survey(md)

**Files:**
- Modify: `src/coreApi.ts:1-10, 82-112, 224-249`
- Modify: `test/coreApi.test.ts:97-124`
- Modify: `test/copy.test.ts:12`
- Modify: `src/main.ts`（`sync` のつなぎ、`paste` の `hasSkeleton`）

**Interfaces:**
- Produces: `core.survey(md: string): Survey`、`interface Survey { view: View; spots: Map<number, Spot> }`。`Mark` / `Trail` / `Survey.trails` は無くなる
- main.ts は**つなぎ**: 選択の id を番号のまま持ち、無くなった番号だけ落とす（Task 7 で state.ts に置き換わる）

- [ ] **Step 1: 試験を新しい形にする**

`test/coreApi.test.ts` の 2 つの test（`survey の JSON — …` と `survey は core を往復する — …`）を次に置き換える。

```ts
test("survey の JSON — spots の鍵は数になり、label の無い鍵は null", () => {
  const s = decodeSurvey({
    view: { roots: [] },
    spots: { "1": { from: 0, to: 0 }, "2": { from: 0, label: 2, to: 4 } },
  });
  assert.deepEqual(s.spots.get(1), { from: 0, label: null, to: 0 });
  assert.deepEqual(s.spots.get(2), { from: 0, label: 2, to: 4 });
});

test("survey は core を往復する — View と地番", () => {
  const s = survey("# r\n\n## a\n");
  assert.equal(s.view.roots.length, 1);
  assert.deepEqual(s.spots.get(3), { from: 5, label: 8, to: 10 });
});
```

`test/copy.test.ts:12` の `survey(md, [], [])` を `survey(md)` にする。

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run check`
Expected: FAIL（引数の数が違う）

- [ ] **Step 3: coreApi.ts を書き換える**

頭のコメント 3 行目を `// 使う側は \`import * as core\` で \`core.View\` / \`core.survey(md)\` と書く。` に。

`Mark` / `Trail` の interface（`/** 前のサイクルの目印。…` から `Trail` の閉じ括弧まで）を消し、`Survey` と `survey` を次にする。

```ts
/** 打鍵 1 回ぶんの読み。View と地番 */
export interface Survey {
  view: View;
  spots: Map<number, Spot>;
}

/**
 * md を core に読ませ、View と地番を 1 度に受け取る。読みのサイクルの唯一の入口。
 * 選択の持ち越しは core に無い — 位置は ts が CodeMirror に預けて写す（state.ts）
 */
export const survey = (md: string): Survey => decodeSurvey(JSON.parse(mbt.mmmSurvey(md)));
```

`mark` / `trail` のデコーダ（`const mark = …` と `const trail = …`）を消し、`decodeSurvey` を次にする。

```ts
/** core の JSON（`mmmSurvey` の出力）を Survey にする。Map の鍵は文字列で来る */
export function decodeSurvey(json: unknown): Survey {
  const o = record(json);
  const spots = new Map<number, Spot>();
  for (const [k, v] of Object.entries(field(o, "spots", record))) {
    const id = Number(k);
    if (!Number.isInteger(id)) bad(`spots の鍵 ${k}`);
    spots.set(id, spot(v));
  }
  return { view: decode(field(o, "view", (v) => v)), spots };
}
```

- [ ] **Step 4: main.ts をつなぐ（つなぎ。Task 7 で消える）**

`src/main.ts` で:

`Carried` の interface と `ghosts` の変数（`/** 持ち越す目印と、…` から `let ghosts: Carried[] = [];` まで）を消す。

`sync` を次にする。

```ts
/**
 * 本文が変わった。**ここが読みのサイクルの唯一の入口** — 打鍵も、開くも、
 * 新規も、リンクで開くも、全部ここを通って同じ順で映る。
 *
 * つなぎ: 選択の id は番号のまま持ち、無くなった番号だけ落とす（Task 7 で
 * state.ts の field に置き換わる）
 */
function sync(next: string, _edits: core.Edit[]): void {
  const wasEmpty = doc.roots.length === 0;
  const r = core.survey(next);
  text = next;
  doc = r.view;
  spots = r.spots;
  const was = selection();
  const ids = was.ids.filter((id) => spots.has(id));
  const card = picked();
  choice =
    card !== null && spots.has(card)
      ? { kind: "card", id: card }
      : { kind: "nodes", sel: { ids, anchor: was.anchor !== null && ids.includes(was.anchor) ? was.anchor : (ids.at(-1) ?? null) } };
  map.render();
  editor.highlight(currentHighlight());
  editor.showHint(doc.roots.length === 0);
  updateDirty();
  showName();
  exportApi.refresh();
  if (wasEmpty && doc.roots.length > 0) map.fitView();
}
```

`choose` の `ghosts = [];` の行とコメントの「幽霊は要らなくなる。」を消す。`paste` の `core.survey(md, [], [])` を `core.survey(md)` にする。

- [ ] **Step 5: 通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm test && pnpm run check`
Expected: 全部 PASS

- [ ] **Step 6: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add src/coreApi.ts src/main.ts test/coreApi.test.ts test/copy.test.ts && git commit -m "refactor: ♻️ #49 survey(md) — 目印と幽霊を ts から外す

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 4: ts 純粋層 — caret.ts に点の規則・buried・derive

**Files:**
- Modify: `src/caret.ts`（全体）
- Modify: `test/caret.test.ts`（期待値 2 か所、test 3 つ追加）

**Interfaces:**
- Consumes: `core.View` / `core.Node` / `core.Spot`、`map/select.ts` の `Choice` / `NOTHING`
- Produces:
  ```ts
  export interface Range { from: number; to: number }
  export interface Caret { ranges: Range[]; head: number }        // md のカーソル。head は主カーソルの頭
  export type Holder = "md" | "map";
  export type Anchors =
    | { kind: "nodes"; at: number[]; anchor: number | null }
    | { kind: "card"; at: number }
    | null;
  export function caretIds(view: core.View, spots: Map<number, core.Spot>, ranges: Range[]): number[]
  export function buried(view: core.View): Set<number>
  export function derive(view: core.View, spots: Map<number, core.Spot>, holder: Holder, caret: Caret, anchors: Anchors): Choice
  ```

- [ ] **Step 1: 試験を書く**

`test/caret.test.ts` を次にする（`node` に `fold` と `blocks` を足し、期待値を点の規則に変え、buried / derive を足す）。

```ts
// md のカーソルと地図の位置が、どのノード（またはカード）に掛かっているか。
// 区間の重なりだけの層。
//
// 実行: pnpm test

import { test } from "node:test";
import assert from "node:assert/strict";
import type * as core from "../src/coreApi.ts";
import { type Anchors, buried, caretIds, derive } from "../src/caret.ts";
import { NOTHING } from "../src/map/select.ts";

const node = (
  id: number,
  label: string | null,
  children: core.Node[] = [],
  blocks: core.Block[] = [],
  fold: core.Fold | null = null,
): core.Node => ({ id, label, fold, blocks, children });

const view = (...roots: core.Node[]): core.View => ({
  frontmatter: null,
  roots: roots.map((n) => ({ node: n, sides: n.children.map(() => "Right" as const) })),
});

const spots = (rows: [number, number, number | null, number][]): Map<number, core.Spot> =>
  new Map(rows.map(([id, from, label, to]) => [id, { from, label, to }]));

/** "# r\n\n## a\n\nhello\n\n## b\n" */
const doc = view(node(2, "r", [node(3, "a"), node(5, "b")]));
const at = spots([
  [1, 0, null, 0],
  [2, 0, 2, 23],
  [3, 5, 8, 17],
  [5, 18, 21, 23],
]);
const point = (p: number) => [{ from: p, to: p }];

test("最も深いノードだけ。自身の文は地番の頭から最初の子の頭まで", () => {
  assert.deepEqual(caretIds(doc, at, point(1)), [2]);
  assert.deepEqual(caretIds(doc, at, point(7)), [3]);
  assert.deepEqual(caretIds(doc, at, point(12)), [3]); // hello の中
  assert.deepEqual(caretIds(doc, at, point(20)), [5]);
});

test("閉じ際は中と見なす。点が継ぎ目ちょうどなら始まる側 1 つ", () => {
  assert.deepEqual(caretIds(doc, at, point(5)), [3]); // r の文が閉じ、a が始まる → a
  assert.deepEqual(caretIds(doc, at, point(17)), [3]); // a の文の閉じ際。b は 18 から
  assert.deepEqual(caretIds(doc, at, point(23)), [5]); // 文書の末尾で追記中も出る
});

test("範囲と複数カーソルは掛かるもの全部。無ければ空", () => {
  assert.deepEqual(caretIds(doc, at, [{ from: 1, to: 20 }]), [2, 3, 5]);
  assert.deepEqual(caretIds(doc, at, [{ from: 1, to: 1 }, { from: 20, to: 20 }]), [2, 5]);
  assert.deepEqual(caretIds(doc, at, [{ from: 5, to: 6 }]), [2, 3]); // 範囲なら継ぎ目の両側
  assert.deepEqual(caretIds(doc, at, []), []);
});

test("Implicit は行が無く自身の文が空なので掛からない", () => {
  // "# r\n\n### x\n": r=2, Implicit=3, x=4
  const gap = view(node(2, "r", [node(3, null, [node(4, "x")])]));
  const s = spots([
    [1, 0, null, 0],
    [2, 0, 2, 11],
    [3, 5, null, 11],
    [4, 5, 9, 11],
  ]);
  assert.deepEqual(caretIds(gap, s, point(7)), [4]);
  assert.deepEqual(caretIds(gap, s, point(5)), [4]);
});

/** "# r\n\n## a\n\n### x\n\n## b\n" で a が畳まれている: r=2, a=3, x=4, b=5 */
const folded: core.Fold = { open: false, summary: "a" };
const hidden = view(node(2, "r", [node(3, "a", [node(4, "x")], [], folded), node(5, "b")]));
const hiddenAt = spots([
  [1, 0, null, 0],
  [2, 0, 2, 23],
  [3, 5, 8, 17],
  [4, 11, 15, 17],
  [5, 18, 21, 23],
]);

test("buried — 畳まれたノードの子孫。畳まれたノード自身は入らない", () => {
  assert.deepEqual([...buried(hidden)], [4]);
  assert.deepEqual([...buried(doc)], []);
});

const caret = (p: number, to = p) => ({ ranges: [{ from: p, to }], head: to });

test("derive — md が持つ間はカーソルから。anchor は主カーソルの頭のノード", () => {
  assert.deepEqual(derive(doc, at, "md", caret(12), null), { kind: "nodes", sel: { ids: [3], anchor: 3 } });
  assert.deepEqual(derive(doc, at, "md", caret(1, 20), null), { kind: "nodes", sel: { ids: [2, 3, 5], anchor: 5 } });
  // anchors が在っても見ない
  assert.deepEqual(derive(doc, at, "md", caret(12), { kind: "nodes", at: [21], anchor: 21 }), {
    kind: "nodes",
    sel: { ids: [3], anchor: 3 },
  });
  // 埋もれたノードは選択に居ない
  assert.deepEqual(derive(hidden, hiddenAt, "md", caret(13), null), { kind: "nodes", sel: { ids: [], anchor: null } });
});

test("derive — 地図が持つ間は位置から。点はラベルの頭、カードは中身の頭", () => {
  const nodes: Anchors = { kind: "nodes", at: [8, 21], anchor: 21 };
  assert.deepEqual(derive(doc, at, "map", caret(1), nodes), { kind: "nodes", sel: { ids: [3, 5], anchor: 5 } });
  // 潰れて同じノードに落ちた点は 1 つに
  assert.deepEqual(derive(doc, at, "map", caret(1), { kind: "nodes", at: [8, 9], anchor: 9 }), {
    kind: "nodes",
    sel: { ids: [3], anchor: 3 },
  });
  // 無ければ何も選んでいない
  assert.deepEqual(derive(doc, at, "map", caret(1), null), NOTHING);
  // カード: a(3) の中身 code(4) が [10, 18) に書かれている（地番は手で組む）
  const carded = view(node(2, "r", [node(3, "a", [], [{ id: 4, content: { kind: "code", info: "", text: "x" } }])]));
  const cardAt = spots([
    [1, 0, null, 0],
    [2, 0, 2, 18],
    [3, 5, 8, 18],
    [4, 10, null, 18],
  ]);
  assert.deepEqual(derive(carded, cardAt, "map", caret(1), { kind: "card", at: 10 }), { kind: "card", id: 4 });
  assert.deepEqual(derive(carded, cardAt, "map", caret(1), { kind: "card", at: 3 }), NOTHING);
  // 埋もれたノードに掛かる位置は落とす
  assert.deepEqual(derive(hidden, hiddenAt, "map", caret(1), { kind: "nodes", at: [15, 21], anchor: 15 }), {
    kind: "nodes",
    sel: { ids: [5], anchor: 5 },
  });
});
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run check`
Expected: FAIL（`buried` / `derive` が無い）

- [ ] **Step 3: caret.ts を書く**

`src/caret.ts` を次にする。

```ts
// md のカーソル、または地図の選択の位置が、どのノード（かカード）に掛かっているか。
// DOM も文書の意味も知らない、区間の重なりだけの層。選択の規則はここにしか無い。
//
// 自身の文 = 地番の頭から最初の子の頭まで（中身は子より前に書かれる）。地番は
// 入れ子なので、これで「最も深いノード」が出る。子孫まで含む `to` で見ると、
// カーソル 1 つでも根までの祖先が全部光り、選択と見分けが付かない（spec.md
// 「選択の持ち主」）。

import type * as core from "./coreApi.ts";
import { type Choice, NOTHING } from "./map/select.ts";

/** md 側のカーソル 1 つ、または選択 1 つぶん（`from === to` なら点） */
export interface Range {
  from: number;
  to: number;
}

/** md のカーソルぜんぶ。head は主カーソルの頭（anchor になる） */
export interface Caret {
  ranges: Range[];
  head: number;
}

/** 選択を持っている側。フォーカスが最後に入ったペイン */
export type Holder = "md" | "map";

/** 地図の選択の位置。ノードはラベルの頭、カードは中身の原文の頭。CodeMirror が編集で写す */
export type Anchors =
  | { kind: "nodes"; at: number[]; anchor: number | null }
  | { kind: "card"; at: number }
  | null;

/** ノードの自身の文 `[from, to)`。Implicit は空なので入らない */
interface Own {
  id: number;
  from: number;
  to: number;
}

function owns(view: core.View, spots: Map<number, core.Spot>): Own[] {
  const out: Own[] = [];
  const walk = (n: core.Node): void => {
    const s = spots.get(n.id);
    if (s) {
      const first = n.children.length > 0 ? spots.get(n.children[0].id) : undefined;
      const to = first ? first.from : s.to;
      if (to > s.from) out.push({ id: n.id, from: s.from, to });
    }
    for (const k of n.children) walk(k);
  };
  for (const t of view.roots) walk(t.node);
  return out;
}

/**
 * 1 つの範囲に掛かる自身の文。**閉じ際は中と見なす** — 区間は半開だが、カーソルは
 * その位置に立てる。半開で読むと文書の末尾では何にも掛からず、追記しているあいだ
 * 印が出ない。**点が継ぎ目ちょうどなら始まる側 1 つ** — 両側を返すと、地図へ移った
 * 直後の Delete が 2 つ消す。範囲なら掛かる全部
 */
function hits(all: Own[], r: Range): Own[] {
  const on = all.filter((o) => o.from <= r.to && o.to >= r.from);
  if (r.from !== r.to) return on;
  const starting = on.filter((o) => o.from === r.from);
  return starting.length > 0 ? starting : on;
}

/** その範囲たちが掛かるノードの id、文書順。無ければ空 */
export function caretIds(view: core.View, spots: Map<number, core.Spot>, ranges: Range[]): number[] {
  if (ranges.length === 0) return [];
  const all = owns(view, spots);
  const picked = new Set<number>();
  for (const r of ranges) for (const o of hits(all, r)) picked.add(o.id);
  return all.filter((o) => picked.has(o.id)).map((o) => o.id);
}

/** 畳まれて埋もれたノード（fold のあるノードの子孫）。箱を持たないので選択に居られない */
export function buried(view: core.View): Set<number> {
  const out = new Set<number>();
  const walk = (n: core.Node, under: boolean): void => {
    if (under) out.add(n.id);
    for (const k of n.children) walk(k, under || n.fold !== null);
  };
  for (const t of view.roots) walk(t.node, false);
  return out;
}

/** その位置を原文に含む中身の id。持ち主が埋もれていれば無い */
function blockAt(view: core.View, spots: Map<number, core.Spot>, p: number, gone: Set<number>): number | null {
  let found: number | null = null;
  const walk = (n: core.Node): void => {
    if (!gone.has(n.id)) {
      for (const b of n.blocks) {
        const s = spots.get(b.id);
        if (s && s.from <= p && p < s.to) found = b.id;
      }
    }
    for (const k of n.children) walk(k);
  };
  for (const t of view.roots) walk(t.node);
  return found;
}

/**
 * 選択。**持ち主が決める** — md が持つ間はカーソルから、地図が持つ間は位置から。
 * 選択に居るのは箱のあるものだけ（埋もれたノードは落とす）。anchor が落ちれば末尾
 */
export function derive(
  view: core.View,
  spots: Map<number, core.Spot>,
  holder: Holder,
  caret: Caret,
  anchors: Anchors,
): Choice {
  const gone = buried(view);
  const point = (p: number): number | null => caretIds(view, spots, [{ from: p, to: p }])[0] ?? null;
  const nodes = (ids: number[], anchor: number | null): Choice => {
    const kept = [...new Set(ids)].filter((id) => !gone.has(id)).sort((a, b) => a - b);
    const a = anchor !== null && kept.includes(anchor) ? anchor : (kept.at(-1) ?? null);
    return { kind: "nodes", sel: { ids: kept, anchor: a } };
  };
  if (holder === "md") return nodes(caretIds(view, spots, caret.ranges), point(caret.head));
  if (anchors === null) return NOTHING;
  if (anchors.kind === "card") {
    const id = blockAt(view, spots, anchors.at, gone);
    return id === null ? NOTHING : { kind: "card", id };
  }
  const ids = anchors.at.flatMap((p) => {
    const id = point(p);
    return id === null ? [] : [id];
  });
  return nodes(ids, anchors.anchor === null ? null : point(anchors.anchor));
}
```

- [ ] **Step 4: 通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm test && pnpm run check`
Expected: 全部 PASS

- [ ] **Step 5: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add src/caret.ts test/caret.test.ts && git commit -m "feat: ✨ #49 derive — 持ち主とカーソル・位置から選択を導く。継ぎ目の点は始まる側

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 5: ts — keep を消す（select / keys / mindmap / main）

**Files:**
- Modify: `src/map/select.ts:150-173`（`under` / `neighbor` を消す）
- Modify: `src/map/keys.ts:7, 18-21, 46-52, 168-170`
- Modify: `src/mindmap.ts:51-54, 820`
- Modify: `src/main.ts`（`apply` の第 3 引数）
- Modify: `test/keys.test.ts:142-163`、`test/select.test.ts:9, 97-104`

**Interfaces:**
- Produces: `Intent` の op が `{ kind: "op"; op: core.Op; edit: boolean }`。`MapHost.apply(op, edit)`。`neighbor` / `under` は無くなる

- [ ] **Step 1: 試験を書き換える**

`test/keys.test.ts` の `"Delete は選択を消し、隣を keep する"` と `"Mod+C は写す。…"` の期待値から `keep: …,` の行を全部消し、前者の題を `"Delete は選択を消す。隣を選ぶのは core の focus"` にする。

`test/select.test.ts` の `"消した後の隣 — …"` の test を消し、import から `neighbor` を外す。

- [ ] **Step 2: select.ts から消す**

`src/map/select.ts` の `under` と `neighbor`（`/** id の祖先に ids のどれかが居るか（= 一緒に消える） */` から `neighbor` の閉じ括弧まで）を消す。

- [ ] **Step 3: keys.ts を直す**

import から `neighbor` を外す。`Intent` の `op` の腕を次にする。

```ts
  /** 操作を md に映す。edit なら focus をそのまま編集開始。消した後に選ぶ隣は core の focus */
  | { kind: "op"; op: core.Op; edit: boolean }
```

`remove` を次にする。

```ts
/** 選択を消す（Delete と Mod+X が同じものを使う）。消した後に選ぶ隣は core が focus で返す */
const remove = (sel: Selection): Intent | null =>
  sel.ids.length === 0 ? null : { kind: "op", op: { kind: "delete", ids: sel.ids }, edit: false };
```

`keyed` の中の `remove(L, sel)` 2 か所を `remove(sel)` に。`keyedCard` の `// 消した後に持ち主を選ぶのは core の focus（持ち主ごと書き直されるので目印は追えない）` を `// 消した後に持ち主を選ぶのは core の focus` に。

- [ ] **Step 4: mindmap.ts と main.ts の apply**

`src/mindmap.ts` の `MapHost.apply` を次にする。

```ts
  /** 操作を md に映す。edit なら、映した後の focus をそのまま編集開始
   *  （ノードのときだけ — 中身の focus はカードとして選ぶ）。返り値は映した focus */
  apply(op: core.Op, edit: boolean): number | null;
```

`act` の `this.host.apply(intent.op, intent.edit, intent.keep);` を `this.host.apply(intent.op, intent.edit);` に。

`src/main.ts` の `apply` の doc コメントから `keep` の段落（`\`keep\` は消す前に…` の 3 行）を消し、シグネチャを `function apply(op: core.Op, edit: boolean): number | null {` に、`if (keep !== null) setSelection(...)` の行とその上のコメント 1 行を消す。

- [ ] **Step 5: 通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm test && pnpm run check && grep -rn "keep\|neighbor" src test --include=*.ts`
Expected: 全部 PASS。grep は `keepalive` 等の無関係な語しか出ない

- [ ] **Step 6: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add src/map/select.ts src/map/keys.ts src/mindmap.ts src/main.ts test/keys.test.ts test/select.test.ts && git commit -m "refactor: ♻️ #49 keep と neighbor を消す — 消した後の隣は core の focus

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 6: ts 状態 — state.ts（field と effect）

**Files:**
- Create: `src/state.ts`
- Create: `test/state.test.ts`

**Interfaces:**
- Consumes: `core.survey` / `core.Survey` / `core.isNode`、`caret.ts` の `derive` / `Holder` / `Anchors` / `Caret`、`map/select.ts` の `Choice` / `NOTHING`
- Produces:
  ```ts
  export const tree: StateField<core.Survey>          // docChanged のときだけ survey(doc)
  export const holder: StateField<Holder>             // setHolder で置く。初期 "md"
  export const anchors: StateField<Anchors>           // changes で写る。setAnchors / focused で置く
  export const choice: StateField<Choice>             // derive(tree, holder, selection, anchors)
  export const fields: Extension                      // 上の 4 つ
  export const setAnchors: StateEffectType<Anchors>
  export const focused: StateEffectType<number | null> // 操作の focus（後の木の id）。anchors が位置に写す
  export const setHolder: StateEffectType<Holder>
  export function anchorsOf(t: core.Survey, id: number | null): Anchors   // focus → 位置
  export function anchorsFor(t: core.Survey, c: Choice): Anchors           // 選択（id）→ 位置
  export const caretOf = (s: EditorState): Caret
  ```

- [ ] **Step 1: 試験を書く**

`test/state.test.ts`:

```ts
// 文書から導けるものは全部 EditorState に居る。DOM 無しでトランザクションを流して固定する。
//
// 実行: pnpm test

import { test } from "node:test";
import assert from "node:assert/strict";
import { EditorState } from "@codemirror/state";
import { anchors, anchorsFor, anchorsOf, choice, fields, focused, holder, setAnchors, setHolder, tree } from "../src/state.ts";
import { NOTHING } from "../src/map/select.ts";

/** "# r\n\n## a\n\n## b\n": r=2 [0,2,16], a=3 [5,8,10], b=4 [11,14,16] */
const md = "# r\n\n## a\n\n## b\n";
const make = (doc: string, cursor = 0): EditorState =>
  EditorState.create({ doc, selection: { anchor: cursor }, extensions: fields });

test("tree は doc から導く。doc が変わらないトランザクションでは同じ値のまま（parse しない）", () => {
  const s = make(md);
  assert.equal(s.field(tree).view.roots.length, 1);
  assert.deepEqual(s.field(tree).spots.get(3), { from: 5, label: 8, to: 10 });
  const s2 = s.update({ effects: setHolder.of("map") }).state;
  assert.equal(s2.field(tree), s.field(tree));
  const s3 = s.update({ changes: { from: 0, to: 0, insert: "#" } }).state;
  assert.notEqual(s3.field(tree), s.field(tree));
});

test("md が持つ間、choice はカーソルから", () => {
  const s = make(md, 8);
  assert.equal(s.field(holder), "md");
  assert.deepEqual(s.field(choice), { kind: "nodes", sel: { ids: [3], anchor: 3 } });
  const moved = s.update({ selection: { anchor: 13 } }).state;
  assert.deepEqual(moved.field(choice), { kind: "nodes", sel: { ids: [4], anchor: 4 } });
});

test("地図が持つ間、choice は anchors から。上に足しても位置が写って同じノード", () => {
  const s = make(md).update({ effects: [setHolder.of("map"), setAnchors.of({ kind: "nodes", at: [8], anchor: 8 })] }).state;
  assert.deepEqual(s.field(choice), { kind: "nodes", sel: { ids: [3], anchor: 3 } });
  const grown = s.update({ changes: { from: 5, to: 5, insert: "## n\n\n" } }).state;
  assert.deepEqual(grown.field(anchors), { kind: "nodes", at: [14], anchor: 14 });
  assert.deepEqual(grown.field(choice), { kind: "nodes", sel: { ids: [4], anchor: 4 } });
  // 行頭に # を足しても、ラベルの頭は素直にずれる
  const deeper = grown.update({ changes: { from: 11, to: 11, insert: "#" } }).state;
  assert.deepEqual(deeper.field(choice), { kind: "nodes", sel: { ids: [4], anchor: 4 } });
});

test("undo（変更の逆）で戻しても選択は残る", () => {
  const s = make(md).update({ effects: [setHolder.of("map"), setAnchors.of({ kind: "nodes", at: [13], anchor: 13 })] }).state;
  const tr = s.update({ changes: { from: 5, to: 11 } }); // ## a の行を消す
  const cut = tr.state;
  assert.deepEqual(cut.field(choice), { kind: "nodes", sel: { ids: [3], anchor: 3 } }); // b は 3 番に
  const back = cut.update({ changes: tr.changes.invert(s.doc) }).state;
  assert.deepEqual(back.field(anchors), { kind: "nodes", at: [13], anchor: 13 });
  assert.deepEqual(back.field(choice), { kind: "nodes", sel: { ids: [4], anchor: 4 } });
});

test("位置が消えた点に潰れれば、そこに掛かるノード", () => {
  const s = make(md).update({ effects: [setHolder.of("map"), setAnchors.of({ kind: "nodes", at: [8], anchor: 8 })] }).state;
  const cut = s.update({ changes: { from: 5, to: 11 } }).state; // a の行ごと消す → 位置 5 = b の頭
  assert.deepEqual(cut.field(choice), { kind: "nodes", sel: { ids: [3], anchor: 3 } });
});

test("focused は同じトランザクションの後の木で位置に写す。null なら選択を空に", () => {
  const s = make(md).update({ effects: setHolder.of("map") }).state;
  // a の行を消して b（後の木では 3）を focus に
  const cut = s.update({ changes: { from: 5, to: 11 }, effects: focused.of(3) }).state;
  assert.deepEqual(cut.field(anchors), { kind: "nodes", at: [8], anchor: 8 });
  assert.deepEqual(cut.field(choice), { kind: "nodes", sel: { ids: [3], anchor: 3 } });
  const none = cut.update({ effects: focused.of(null) }).state;
  assert.deepEqual(none.field(choice), NOTHING);
});

test("anchorsOf / anchorsFor — id と位置の往復。カードは中身の頭", () => {
  // "# r\n" 0-3、"\n" 4、"## a\n" 5-9、"\n" 10、フェンスは 11 から
  const withCard = make("# r\n\n## a\n\n```\nx\n```\n");
  const t = withCard.field(tree);
  assert.deepEqual(anchorsOf(t, 3), { kind: "nodes", at: [8], anchor: 8 });
  assert.deepEqual(anchorsOf(t, 4), { kind: "card", at: 11 });
  assert.equal(anchorsOf(t, null), null);
  assert.deepEqual(anchorsFor(t, { kind: "nodes", sel: { ids: [2, 3], anchor: 3 } }), { kind: "nodes", at: [2, 8], anchor: 8 });
  assert.deepEqual(anchorsFor(t, { kind: "card", id: 4 }), { kind: "card", at: 11 });
  assert.deepEqual(anchorsFor(t, NOTHING), { kind: "nodes", at: [], anchor: null });
});
```

- [ ] **Step 2: 落ちることを確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run check`
Expected: FAIL（`../src/state.ts` が無い）

- [ ] **Step 3: state.ts を書く**

```ts
// 文書から導けるものは全部 EditorState に居る。
//
// CodeMirror が構文木を `syntaxTree(state)` の StateField で持つのと同じ位置に、
// core の答え（View + 地番）を置く。地図の選択の位置・持ち主・選択もここ。
// 1 トランザクション = 1 サイクルで、位置は CodeMirror が編集で写す（`mapPos`）。
// DOM を知らない — node の試験でトランザクションを流して固定する（test/state.test.ts）。
//
// field の依存はアクセス時に解ける（`tr.state.field(other)` で other の新しい値が
// 先に計算される。並べ順は関係ない。循環は投げる）— ai-docs/codemirror.md。
// 決めは docs/superpowers/specs/2026-09-05-holder-design.md。

import { type EditorState, type Extension, StateEffect, StateField, type Transaction } from "@codemirror/state";
import * as core from "./coreApi.ts";
import { type Anchors, type Caret, type Holder, derive } from "./caret.ts";
import type { Choice } from "./map/select.ts";

/** core が読んだ木と地番。doc が変わったときだけ読み直す */
export const tree = StateField.define<core.Survey>({
  create: (s) => core.survey(s.doc.toString()),
  update: (v, tr) => (tr.docChanged ? core.survey(tr.newDoc.toString()) : v),
});

/** 地図で選ぶ・引き継ぐ・捨てる */
export const setAnchors = StateEffect.define<Anchors>();
/** 操作の focus（後の木の id）。anchors が同じトランザクションの後の木で位置に写す。null は空に */
export const focused = StateEffect.define<number | null>();
/** フォーカスがペインに入った */
export const setHolder = StateEffect.define<Holder>();

/** 選択を持っている側。窓やメニューへ抜けても変わらない（粘る）。起動は md */
export const holder = StateField.define<Holder>({
  create: () => "md",
  update(v, tr) {
    for (const e of tr.effects) if (e.is(setHolder)) v = e.value;
    return v;
  },
});

/** 位置を編集で写す。挿入がちょうどその位置なら前に留まる（assoc = -1）— ラベルの頭に字を打てば、その字がラベルの先頭 */
const carry = (tr: Transaction, a: Anchors): Anchors => {
  if (a === null) return null;
  const at = (p: number): number => tr.changes.mapPos(p, -1);
  if (a.kind === "card") return { kind: "card", at: at(a.at) };
  return { kind: "nodes", at: a.at.map(at), anchor: a.anchor === null ? null : at(a.anchor) };
};

/** 地図の選択の位置。doc が変われば写り、effect で置き換わる */
export const anchors = StateField.define<Anchors>({
  create: () => null,
  update(v, tr) {
    if (tr.docChanged) v = carry(tr, v);
    for (const e of tr.effects) {
      if (e.is(setAnchors)) v = e.value;
      if (e.is(focused)) v = anchorsOf(tr.state.field(tree), e.value);
    }
    return v;
  },
});

/** md のカーソル。head は主カーソルの頭 */
export const caretOf = (s: EditorState): Caret => ({
  ranges: s.selection.ranges.map((r) => ({ from: r.from, to: r.to })),
  head: s.selection.main.head,
});

const derived = (s: EditorState): Choice => {
  const t = s.field(tree);
  return derive(t.view, t.spots, s.field(holder), caretOf(s), s.field(anchors));
};

/** 選択。持ち主が決める（caret.ts の derive） */
export const choice = StateField.define<Choice>({
  create: derived,
  update: (_, tr) => derived(tr.state),
});

export const fields: Extension = [tree, holder, anchors, choice];

/** focus の id をその木の地番で位置に。ノードならラベルの頭、中身なら原文の頭。無ければ null */
export function anchorsOf(t: core.Survey, id: number | null): Anchors {
  if (id === null) return null;
  const s = t.spots.get(id);
  if (!s) return null;
  if (!core.isNode(t.view, id)) return { kind: "card", at: s.from };
  return s.label === null ? null : { kind: "nodes", at: [s.label], anchor: s.label };
}

/** 選択（id）をその木の地番で位置に。地図で選んだときの setAnchors の値。Implicit は行が無いので入らない */
export function anchorsFor(t: core.Survey, c: Choice): Anchors {
  if (c.kind === "card") {
    const s = t.spots.get(c.id);
    return s ? { kind: "card", at: s.from } : null;
  }
  const label = (id: number): number | null => t.spots.get(id)?.label ?? null;
  const at = c.sel.ids.flatMap((id) => {
    const l = label(id);
    return l === null ? [] : [l];
  });
  return { kind: "nodes", at, anchor: c.sel.anchor === null ? null : label(c.sel.anchor) };
}
```

`NOTHING` は `derive` の側（caret.ts）が使う。state.ts では `Choice` の型だけを使うので、import は `import type { Choice } from "./map/select.ts";` でよい。

- [ ] **Step 4: 通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm test && pnpm run check`
Expected: 全部 PASS。落ちたら `state.test.ts` の地番の数字（`## n` を足した後の 14、`#` を足した後の位置）を `survey` の実値で確かめる — 期待値の誤りと実装の誤りを分ける

- [ ] **Step 5: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add src/state.ts test/state.test.ts && git commit -m "feat: ✨ #49 state.ts — tree / anchors / holder / choice を EditorState の field に

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 7: ts 配線 — editor / main / mindmap

**Files:**
- Modify: `src/editor.ts`（constructor / state / setText / apply / caret → select / hold / state）
- Modify: `src/main.ts`（状態の節、サイクル、apply / write、host、focusin）
- Modify: `src/mindmap.ts`（MapHost に `holder()`、輪 or 枠、`showCaret` → 私有 `ring`）

**Interfaces:**
- Consumes: `state.ts` の全部、`caret.ts` の `Holder`
- Produces:
  ```ts
  // editor.ts
  class MdEditor {
    constructor(parent: HTMLElement, onUpdate: (state: EditorState, prev: EditorState | null) => void)
    readonly view: EditorView
    get state(): EditorState
    text(): string
    setText(text: string): void                          // 履歴も新しく。onUpdate(state, null)
    apply(sets: core.Edit[][], focus?: number | null): void   // changes（合成）+ focus が渡されれば focused の effect。1 dispatch
    select(a: Anchors): void                             // setAnchors
    hold(h: Holder, a?: Anchors): void                   // setHolder（+ setAnchors）
    highlight(ranges: Range[]): void; reveal(pos: number): void; undo(); redo(); focus(); setTheme(dark)
  }
  // mindmap.ts
  interface MapHost { …; holder(): Holder; apply(op, edit): number | null; … }   // selection() / picked() は値、setSelection / setPicked は host が位置に写す
  ```

- [ ] **Step 1: editor.ts**

import に足す: `import { type Anchors, type Holder, type Range } from "./caret.ts";`（既存の `import type { Range }` を置き換え）と `import { fields, focused, setAnchors, setHolder } from "./state.ts";`。`ChangeSet` は残す。

頭のコメントを次にする。

```ts
// Markdown のペイン。CodeMirror 6。**文書の真実はこの中の文字列**で、履歴も
// CodeMirror が持つ。文書から導けるもの（core の木・地図の選択の位置・持ち主・選択）も
// EditorState の field に居る（state.ts）。1 トランザクション = 1 サイクルで、
// `onUpdate(state, prev)` が 1 回届く。読みは決して書かない。
```

クラスの欄 `private onChange` / `private onCaret` を `private onUpdate: (state: EditorState, prev: EditorState | null) => void;` に。constructor は `(parent: HTMLElement, onUpdate: …)` で `this.onUpdate = onUpdate;`。

`state(doc)` の extensions: `highlightField,` の前に `fields,` を足し、`updateListener` を次にする。

```ts
        EditorView.updateListener.of((u) => this.onUpdate(u.state, u.startState)),
```

`setText` の末尾 2 行（`this.onChange(...)` と `this.onCaret(...)`）を `this.onUpdate(this.view.state, null);` に。doc コメントの「編集列は全文の置き換え — 前の文書の目印は全部死ぬ」を「前の文書の選択は field ごと新しくなる」に。

`apply` を次にする。

```ts
  /**
   * 操作の編集列を 1 トランザクションで当てる。undo は 1 手になる。編集列を 2 つ以上
   * 渡せば**順に**当てる（後の列の座標は前の列を当てた後の md）。`focus` を渡せば
   * 同じトランザクションで `focused` の effect が乗り、anchors が後の木で位置に写す
   * （持ち主の操作）。渡さなければ選択に触らない（それ以外の書き込み）
   */
  apply(sets: core.Edit[][], focus?: number | null): void {
    let changes = ChangeSet.empty(this.view.state.doc.length);
    for (const set of sets) changes = changes.compose(ChangeSet.of(set, changes.newLength));
    this.view.dispatch({ changes, effects: focus === undefined ? [] : [focused.of(focus)] });
  }

  /** 地図で選び直した。位置は host が地番で写してから渡す */
  select(a: Anchors): void {
    this.view.dispatch({ effects: setAnchors.of(a) });
  }

  /** フォーカスがペインに入った。md → map なら引き継ぐ位置も一緒に */
  hold(h: Holder, a?: Anchors): void {
    this.view.dispatch({ effects: a === undefined ? [setHolder.of(h)] : [setHolder.of(h), setAnchors.of(a)] });
  }

  get state(): EditorState {
    return this.view.state;
  }
```

`caret()` を消す。`highlight` / `reveal` / `undo` / `redo` / `focus` / `setTheme` / `text` / `showHint` はそのまま。

- [ ] **Step 2: mindmap.ts**

頭のコメントの「**選択の値は持たない** — … 値そのものは main.ts が持つ。」を「**選択の値は持たない** — 入力を map/select.ts の値にして host へ渡し、host の値を塗るだけ。値は EditorState の field（state.ts）。」に。

`MapHost` に足す（`selection()` の前）:

```ts
  /** 選択を持っている側。md なら輪（内側）、map なら枠（selected）で塗る */
  holder(): Holder;
```

import に `import type { Holder } from "./caret.ts";` を足す。

欄 `private caretIds: number[] = [];` とそのコメントを消す。`render()` の `this.renderer.paintSelection(...)` から `this.showCaret(this.caretIds);` までの 5 行を `this.paintChoice();` に。`refreshSelection()` の `this.renderer.paintSelection(...)` を `this.paintChoice();` に。

`showCaret` を私有にして名前を変える（doc コメントも）。

```ts
  /**
   * 選択を塗る。md が持つ間は**輪**（箱の内側）、地図が持つ間は **selected**（枠）。
   * 輪と枠は「誰が持っているか」の印（spec.md「選択の持ち主」）
   */
  private paintChoice(): void {
    const ids = this.host.selection().ids;
    const md = this.host.holder() === "md";
    this.renderer.paintSelection(new Set(md ? [] : ids));
    this.ring(md ? ids : []);
  }

  /**
   * 輪を、掛かっているノードの**内側**へ重ねる。箱の無い（畳まれて埋もれた）
   * ノードには出さない。本数が変わったときだけ作り足す/捨てる
   */
  private ring(ids: number[]): void {
    const boxes = ids.flatMap((id) => {
      const b = this.layout.boxes.get(id);
      return b ? [b] : [];
    });
    while (this.caretRings.length > boxes.length) this.caretRings.pop()?.remove();
    while (this.caretRings.length < boxes.length) {
      const ring = svgEl("rect", { class: "caret-ring" });
      this.caretLayer.append(ring);
      this.caretRings.push(ring);
    }
    const inset = 3;
    boxes.forEach((b, i) => {
      const ring = this.caretRings[i];
      ring.setAttribute("x", String(b.x + inset));
      ring.setAttribute("y", String(b.y + inset));
      ring.setAttribute("width", String(b.w - inset * 2));
      ring.setAttribute("height", String(b.h - inset * 2));
    });
  }
```

- [ ] **Step 3: main.ts — 状態の節とサイクル**

import に足す: `import type { EditorState } from "@codemirror/state";`、`import * as st from "./state.ts";`。`caretIds` / `Range` の import は `import type { Range } from "./caret.ts";` だけに。

頭のコメントを次にする。

```ts
// 束ねる場所。文書の真実は md ペインの文字列で、マップはその写し。
// 文書から導けるもの（core の木・地図の選択の位置・持ち主・選択）は EditorState の
// field（state.ts）に居て、1 トランザクションが 1 サイクル — `onUpdate` に 1 回届く。
// ここには変数を置かない。読みは決して書かない。
// ここに居るのは、そのサイクルと、保存とファイル I/O、帯のメニュー。
//
// **操作の入口は 2 つ。** `apply`（持ち主の操作。focus を選ぶ）と `write`（それ以外の
// 書き込み。選択に触らない）。選択を書くのは持ち主の操作だけ（design.md）。
```

`// ---------- app state ----------` の節の `let text` / `let doc` / `let spots` / `let choice` / `const selection` / `const picked` を、次の読み口に置き換える（`docGen` / `savedText` / `savedName` はそのまま）。

```ts
/** 読み口。値は全部 EditorState の field に居る */
const state = (): EditorState => editor.state;
const text = (): string => editor.text();
const doc = (): core.View => state().field(st.tree).view;
const spots = (): Map<number, core.Spot> => state().field(st.tree).spots;
const choice = (): Choice => state().field(st.choice);
const selection = (): Selection => nodesOf(choice());
const picked = (): number | null => cardOf(choice());
```

`declaredFolder` の `doc.frontmatter` を `doc().frontmatter` に。以降、ファイル全体で `text` → `text()`、`doc` → `doc()`、`spots` → `spots()` に直す（`text !== savedText` / `text.slice` / `copyText(text, doc, spots, …)` / `hasImages(text)` / `linkOf(text)` / `io.save(text)` / `savedText = text` / `core.isNode(doc, id)` / `deriveName(doc)` / `declare` の `setImageFolder(text, doc.frontmatter, next)` と `core.splice(text, …)` / `retarget(doc, …)` など。`grep -n "\btext\b\|\bdoc\b\|\bspots\b" src/main.ts` で漏れを見る）。

`// ---------- サイクル ----------` の `sync` を次にする。

```ts
/**
 * 1 トランザクション = 1 サイクル。木が変わっていれば描き直し、そうでなければ塗り直し。
 * **ここが読みのサイクルの唯一の出口** — 打鍵も、開くも、操作も、選び直しも、
 * フォーカスの移動も、全部ここへ 1 回届く。prev が null なら文書を丸ごと入れ替えた
 */
function onUpdate(s: EditorState, prev: EditorState | null): void {
  const t = s.field(st.tree);
  if (prev === null || prev.field(st.tree) !== t) {
    map.render();
    // 白紙の言い出し。**出る理由は 1 つ**（まだ木が無い）で、マップ側も render() の中で同じことを見ている
    editor.showHint(t.view.roots.length === 0);
    updateDirty();
    showName();
    exportApi.refresh();
    // 何も無いところに最初の木が生まれた瞬間だけ、真ん中へ寄せる
    const wasEmpty = prev === null || prev.field(st.tree).view.roots.length === 0;
    if (wasEmpty && t.view.roots.length > 0) map.fitView();
  } else {
    map.refreshSelection();
  }
  // md 側の薄塗りは地図が持つ間だけ（md が持つ間はカーソルそのものが在る）
  editor.highlight(s.field(st.holder) === "map" ? currentHighlight() : []);
}
```

`selectedRanges` / `pickedRange` / `currentHighlight` はそのまま（`spots` → `spots()`）。`choose` / `setSelection` / `setPicked` を次にする。

```ts
/** 地図で選び直した。id を位置に写して CodeMirror に置く（onUpdate が塗る）。reveal は md 側を anchor の頭へスクロールするか */
function choose(next: Choice, reveal: boolean): void {
  editor.select(st.anchorsFor(state().field(st.tree), next));
  const anchor = nodesOf(next).anchor;
  if (reveal && anchor !== null) {
    const s = spots().get(anchor);
    if (s) editor.reveal(s.from);
  }
}

const setSelection = (sel: Selection, reveal: boolean): void => choose({ kind: "nodes", sel }, reveal);

const setPicked = (id: number | null): void => choose(id === null ? NOTHING : { kind: "card", id }, false);
```

`apply` を次にし、その下に `write` を置く。

```ts
/**
 * 持ち主の操作を md に映す。**選択を書く入口はここ 1 本** — 地図は md に触らない。
 * 操作 1 回 = CodeMirror の 1 トランザクション（編集列 + focus の effect）で、undo は
 * CodeMirror のもの。focus は anchors が後の木で位置に写す（state.ts）。
 * できない操作は core が空の編集列で言う。いまは雑に、しらせを出すだけ。
 *
 * focus はノードとも中身とも限らない。ノードで `edit` ならその場編集を開く。
 * 呼び出し側が続けられるよう focus を返す（Link / Code が使う）
 */
function apply(op: core.Op, edit: boolean): number | null {
  const r = core.edit(text(), op);
  // core は断りを「編集なし・focus なし」で言う。編集が無くても focus が在るのは、
  // 何も変わらなかった操作（同じ名前への Rename など）で、しらせは出さない
  if (r.focus === null && r.edits.length === 0) {
    failed("Couldn't do that here");
    return null;
  }
  const before = selection().anchor;
  editor.apply([r.edits], r.focus);
  if (r.focus === null) return null;
  // 別のノードへ移ったときだけ md を寄せる（同じノードに留まる操作で手元を揺らさない）
  if (r.focus !== before) {
    const s = spots().get(r.focus);
    if (s) editor.reveal(s.from);
  }
  // 畳まれて埋もれたノードには箱が無く、その場編集を開けない
  if (edit && core.isNode(doc(), r.focus) && !map.beginEdit(r.focus, null)) {
    failed("Couldn't start editing — the node is folded");
  }
  return r.focus;
}

/**
 * それ以外の書き込みを md に映す（ファイルの投下・お絵描き・画像の貼り付け）。
 * **選択には触らない** — 地図にフォーカスが無くても起きる操作なので、持ち主が決めた
 * 選択を横から書き換えない（design.md「選択を書くのは持ち主の操作だけ」）
 */
function write(op: core.Op): void {
  const r = core.edit(text(), op);
  if (r.edits.length === 0) {
    if (r.focus === null) failed("Couldn't do that here");
    return;
  }
  editor.apply([r.edits]);
}
```

`onCaret` 関数を消す。`const editor = new MdEditor(mdPane, sync, onCaret);` を `const editor = new MdEditor(mdPane, onUpdate);` に。

`host` に `holder: () => state().field(st.holder),` を足す。`attachImage` の `apply(...)` を `write(...)` に。`declare` の `editor.apply(...sets)` を `editor.apply(sets)` に。

- [ ] **Step 4: main.ts — 持ち主の focusin**

`const map = new Mindmap(mapPane, host);` の直後に足す。

```ts
// ---------- 持ち主 ----------
//
// 選択は持っている側（フォーカスが最後に入ったペイン）が決める。md → map は、その
// 瞬間のカーソルのノードを位置にして引き継ぐ。map → md は捨てる（md のカーソルは
// 動かさない）。窓・メニュー・帯へ抜けても変わらない — 2 つのペインの focusin だけを見る
mdPane.addEventListener("focusin", () => {
  if (state().field(st.holder) !== "md") editor.hold("md", null);
});
mapPane.addEventListener("focusin", () => {
  if (state().field(st.holder) !== "map") editor.hold("map", st.anchorsFor(state().field(st.tree), choice()));
});
```

- [ ] **Step 5: 型と試験を通す**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run check && pnpm test && grep -n "ghosts\|Carried\|caretIds\|onCaret\|showCaret" src/*.ts src/**/*.ts`
Expected: 全部 PASS。grep は何も出ない

- [ ] **Step 6: ブラウザで煙試験**

主ディレクトリの `.claude/launch.json` に `mmm-dist`（`vite preview`、4173）が在る前提。worktree のビルドを主ディレクトリの `dist` に出してから見る。

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && pnpm run core && pnpm exec vite build --outDir D:/1.atrium/mmm/dist --emptyOutDir
```

`preview_start` で `mmm-dist` を開き、`Alt+1` で md を出す（Browser pane は狭いので 1 ペイン表示）。確かめること:

1. md に `# r` `## a` `## b` を打つ → 打っている行のノードに**輪**が出る（枠は出ない）
2. `Alt+2` で地図へ移る → 輪が**枠**になり、`Alt+1` で戻すと md 側に薄塗りが出ている
3. 地図で b を選んで Delete → a が選ばれる（枠）。Mod+Z → b が戻り、選択は a のまま
4. 地図で a を選び、`Alt+1` で md へ → 枠が消え、カーソルの行のノードに輪
5. 地図で Enter → 新しいノードが選ばれてラベル欄が開く
6. `read_console_messages` にエラーが無い

Browser ツールの罠（メモリ）: HMR でなく build なので reload は `navigate` で。仮装 viewport 下ではクリック座標が DOM の 1/4。キー入力は `javascript_tool` で `KeyboardEvent` を dispatch するのが確実。

- [ ] **Step 7: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add src/editor.ts src/main.ts src/mindmap.ts && git commit -m "feat: ✨ #49 選択の持ち主を 1 つに — main の変数と半サイクルを畳み、apply と write に分ける

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

### Task 8: docs — design / core / spec

**Files:**
- Modify: `docs/design.md`（全体）
- Modify: `docs/core.md:154-155, 184, 233-234, 239-261, 321-324`
- Modify: `docs/spec.md`（構成の `coreApi.ts` / `caret.ts` / `editor.ts` / `mindmap.ts` / `main.ts` の行、`state.ts` の行を足す、「二つをまたぐ印」の節）

- [ ] **Step 1: design.md**

全体を次にする。

```markdown
# 設計の型

部品ごとに「何をしているか」「どの既知の型か」「段の間で何を約束するか」を 1 ページで言う。
設計を監督するための地図。細部は [spec.md](spec.md)（UI）と [core.md](core.md)（core）。

## 部品と型

| 部品 | やっていること | 既知の型 |
|---|---|---|
| md が真実、木は派生 | 打鍵 → parse → View → 描画。読みは書かない | LSP / IDE の「テキストが真実、AST は派生」 |
| 方言（md.mbt） | ライブラリの mdAst を mmm の決めに揃える層。読みは `dialect ∘ parse`、書きは `spell ∘ serialize`。癖を知るのはここだけ | 腐敗防止層（anti-corruption layer）。アダプタ |
| Op → apply → check | 操作は値。木に当てるのは純粋関数。書けない木は check が断る | Command + 不変条件で弾く |
| merge | 正規形を base にした 3-way merge。base → theirs（操作の差）だけを ours（原文）に写し、流儀の差は写さない。要素は手前の隙間を持つ。最後に読み直して形を検証 | 3-way merge（git）。recast（変わっていないノードは元の原文を再利用）。React の keyed diff |
| focus | 操作が「次に選ぶもの」を返す。ノードを消せば次の兄弟 → 前の兄弟 → 親 | ProseMirror の transaction が selection を運ぶ。Lexical の `$removeNode` |
| Intent の表 | キー・右クリック・ドラッグ・貼り付けを純粋な表で Intent にし、`apply` 1 本へ | エディタの keymap → command |
| 状態と拍（state.ts） | doc・カーソル・履歴と並べて、core の答え（View + 地番）・地図の選択の位置・持ち主・選択を EditorState の field に置く。1 トランザクション = 1 サイクル。位置は CodeMirror が編集で写す | Lezer の `syntaxTree`（構文木が StateField）。Redux 型の単一 store |
| 選択の持ち主（derive） | 持ち主（フォーカスのあるペイン）が決める。md が持つ間はカーソルから、地図が持つ間は位置から導く値。地図は塗るだけ | VS Code / Obsidian のアウトライン（カーソル追従）+ CodeMirror の選択（位置を編集で写す） |

### なぜ CodeMirror 6 か

md のプレーンテキストが真実なので、候補はプレーンテキストのエディタ（CodeMirror 6 / Monaco）に
絞られる。Monaco はタッチ非対応で大きい。ProseMirror / Lexical 系は自前の木が真実で「md が真実」と
衝突する。CodeMirror 6 は状態が不変値 + トランザクションで、位置の写しと履歴が同じ拍に在り、
`@codemirror/state` は DOM 無しで動く（node で試験できる）。Lezer の markdown がフェンスの色を
map と同じ表から出す。棚卸しは [ai-docs/codemirror.md](../ai-docs/codemirror.md)。

## 段の間の法則

段ごとに閉じた法則（op × check、op × 合流、読み × 書き）は各 wbtest が持つ。
段をまたぐ前提は、頼る側の段の試験に書く。

- **行の不可侵**（merge） — 行（ラベル・畳み・種類）が同じノードの行は、どの編集の範囲にも入らない。
  地図の選択の位置（ラベルの頭）はこれに頼る
- **隙間の保存**（merge / edit） — 編集を当てた md に、元に無かった連続空行と頭の空行は無い。
  merge の変え方と、edit の全操作の両方で回す
- **形の一致**（edit） — edit の編集を md に当てて読み直せば、apply の後の木と形が一致する
- **focus**（edit） — 読み替えた focus は、後の木で focus だったのと同じ部分木を指す
- **Delete の focus**（edit） — 消した後の focus は消えていない兄弟か祖先（中身なら持ち主）で、
  畳みの中に埋もれていない。無いのは最後の根と文書の散文を消したときだけ
- **id の順 = 文書順**（ts） — 読みは文書順に番号を振るので、id の大小がそのまま文書順。
  select.ts の並べ替えと `Layout.order` はこれに頼る
- **中身は子より前に書かれる**（ts） — ノードの自身の文は地番の頭から最初の子の頭まで。caret.ts はこれに頼る
- **選択に居るのは箱のあるものだけ**（derive） — 畳まれて埋もれたノードは選択に入らない
- **選択を書くのは持ち主の操作だけ**（main.ts） — `apply` は focus を選ぶ。`write`（投下・宣言・
  画像の保存）は md を書くだけで選択に触らない

## 同一性は focus 1 本

操作の結果は **focus**（core が返す）。それ以外は同一性を持たず、位置（テキストの中の場所）から
毎サイクル導く。位置は CodeMirror が編集で写す。サイクルを越えて持つ id は無い。
```

- [ ] **Step 2: core.md**

`- **ラベルの頭（\`label\`）** — …` の 2 行を次にする。

```
- **ラベルの頭（`label`）** — マーカー（`## ` / `- `）の後ろ。地図の選択の位置（ts の anchors）に
  使う。行を持たない Implicit と文書の散文は無い
```

`   触らない** — そこに選択の目印が在る（follow.mbt）` を `   触らない** — そこに地図の選択の位置（ts の anchors）が在る` に。

`**行が同じノードの行はどの編集の範囲にも入らない**（行の不可侵）、…段をまたぐ法則は edit/law_wbtest.mbt — 中身だけを変える操作の後、持ち主のラベルの目印は同じノードに着く（目印の生存）。` の末尾を `段をまたぐ法則は edit/law_wbtest.mbt — Delete の focus は消えていない兄弟か祖先で、埋もれていない（Delete の focus）。` に。

`### 同一性 — follow.mbt` の節（見出しから `…幾何だから ts。` まで）を次に置き換える。

```markdown
### 同一性 — focus 1 本

core が持つ同一性は操作の focus だけ。選択の持ち越しは core に無い — id は読みのサイクルの中で
しか通じず、ts は位置（ラベルの頭）を CodeMirror に預けて編集で写す（`src/state.ts`、
design.md「状態と拍」）。打鍵ごとの core 呼び出しは `mmmSurvey(md)` の 1 回で、View と地番を返す。
```

`- **中身を消した focus は持ち主。** ノードを消した focus は無い（隣を選ぶのは UI の仕事）が、…` の項を次にする。

```
- **消した focus は隣。** 中身を消せば持ち主。ノードを消せば先頭（文書順）の次の兄弟 → 前の兄弟
  → 親で、消えるものは飛ばす。兄弟と親は消したノードが見えていれば見えている（畳まれるのは
  子孫）ので、埋もれたノードに落ちない。何も残らなければ無い（最後の根）。「消した後に何を
  選ぶか」の答えはこの 1 本
```

- [ ] **Step 3: spec.md — 構成**

`coreApi.ts` の行を `core の出口と入口。JSON の形を整える唯一の場所（survey が View・地番を受け、edit が Op を送る）。TS では必ず \`core.View\` と書く` に。`caret.ts` の行を `md のカーソル、または地図の選択の位置がどのノードに掛かるか（最も深いもの。区間の重なりだけ）。derive が持ち主とそれらから選択を導く。選択の規則はここだけ` に。`editor.ts` の行を `Markdown 側(CodeMirror 6、履歴も CodeMirror。state.ts の field を載せ、1 トランザクションを 1 回 onUpdate で外へ。地図の選択を薄塗りで受ける。フェンスの中は map/highlight.ts と同じ言語表で色を付ける)` に。`caret.ts` の次に行を足す:

```
  state.ts     文書から導けるものの置き場(EditorState の field: tree = core の答え、anchors = 地図の
               選択の位置、holder = 持ち主、choice = 選択。位置は CodeMirror が編集で写す。effect は
               setAnchors / focused / setHolder。DOM を知らない)
```

`mindmap.ts` の行の「選択と輪を塗る」を「選択を輪（md が持つ間）か枠（地図が持つ間）で塗る」に。`main.ts` の行を次にする。

```
  main.ts      束ねる場所(1 トランザクション = 1 サイクルの出口 onUpdate、操作の入口 apply(op, edit)
               — focus を選ぶ — と write(op) — 選択に触らない、持ち主の focusin、
               貼り付け・投下・描いた絵の保存、ファイル I/O、帯。変数は持たない)
```

- [ ] **Step 4: spec.md — 「二つをまたぐ印」→「選択の持ち主」**

節 `### 二つをまたぐ印` を丸ごと次に置き換える（次の `### エクスポート` まで）。

```markdown
### 選択の持ち主

**選択は 1 つで、持っている側（フォーカスのあるペイン）が決める。**

| 持ち主 | 選択 | 見え方 |
|---|---|---|
| md | カーソル（と選択・複数カーソル）が掛かっているノード | 地図: 箱の**内側**の輪。md: 何も塗らない |
| Mindmap | 地図で選んだもの（ノードの並びかカード 1 枚） | 地図: `selected` の枠。md: その範囲の薄塗り |

持ち主はフォーカスが最後に入ったペインで、窓・メニュー・帯へ抜けても変わらない。md から
地図へ移れば、その瞬間のカーソルのノードを引き継ぐ。地図から md へ戻れば地図の選択は捨て、
**md のカーソルは動かさない**（書いていた場所が飛ばない）。

**輪と枠は「誰が持っているか」の印。** 外から掴む枠が地図の選択、中に居る輪が md の
カーソル。同じ 1 つの選択の 2 つの顔なので、濃さで competing しない。輪はノードの子では
なく world に浮かぶ別の印（`CardPick` と同じ — 子にすると動くたびに中身が作り直される）。
濃さは輪だけが持つ（`--accent-soft` は面を塗るための薄さで、2px の線に当てると消える）。

**md 側の薄塗りはテキスト選択ではない**（CodeMirror の装飾）。実選択にするとカーソルを
奪う。地図が持つ間だけ出る。

**どれに掛かるかは、そのノード自身の文に掛かっているかどうか、それだけ**（`src/caret.ts`）。
自身の文とは骨格の行と、子に取られていない中身（あいだの空行も含む）。文書は隙間なく
分かれるので、点でも範囲でも複数カーソルでも同じ 1 つの規則。文書の頭・最初の見出しより
前の散文・区切り行には何も出ない。**子孫まで含む `to` で見てはいけない** — カーソル 1 つで
ルートまでの祖先が全部光り、選択と見分けが付かなかった。

**閉じ際は中と見なす。** 区間は半開だが、カーソルはその位置に立てる。半開のまま読むと文書の
末尾では何にも掛からず、追記しているあいだ印が出ない。**点が継ぎ目ちょうどなら始まる側 1 つ**
— 両側を返すと、地図へ移った直後の Delete が 2 つ消す。範囲なら掛かる全部。

**選択に居るのは箱のあるものだけ。** 畳んだ枝の中に居るときは、どこも光らない。

**地図が持つ間の選択は位置で持つ。** ノードはラベルの頭、カードは中身の頭を CodeMirror に
預け（`src/state.ts`）、打鍵・undo・操作で CodeMirror が位置を写す。undo で戻しても同じノードが
選ばれている。位置が消えた点に潰れれば、そこに掛かるノード（隣）。**操作の後は core の
focus** — ノードを消せば次の兄弟 → 前の兄弟 → 親（core.md「操作」）。

**選択を書くのは持ち主の操作だけ。** ファイルの投下・宣言の書き換え・画像の保存は md を
書くだけで、選択に触らない（`main.ts` の `write`）。

複数選択は任意の集合。md 側は範囲・複数カーソルが掛かる全部、地図側は `Mod+クリック` /
矩形 / `Shift+矢印`。
```

- [ ] **Step 5: 確かめる**

Run: `cd D:/1.atrium/mmm/.worktrees/feat/holder && grep -rn "follow\|幽霊\|keep\|neighbor\|二つをまたぐ印\|目印" docs/design.md docs/core.md docs/spec.md`
Expected: `docs/spec.md` の「目印」が他の意味（画像の場所取りなど）で残る以外は何も出ない。出たら文脈を読んで直す。`docs/superpowers/` は記録なので触らない

- [ ] **Step 6: Commit**

```bash
cd D:/1.atrium/mmm/.worktrees/feat/holder && git add docs/design.md docs/core.md docs/spec.md && git commit -m "docs: 📝 #49 選択の持ち主 — design / core / spec を 1 本の focus と EditorState の拍で書き直す

Co-Authored-By: Claude Fable 5.1 <noreply@anthropic.com>"
```

---

## 終わったら

- `git merge main` で main を取り込み（rebase しない）、`pnpm test` / `pnpm run test:core` / `pnpm run check` / `pnpm run check:core` を通す
- PR を main 向けに 1 本（題 `feat: ✨ #49 選択の持ち主を 1 つに — 位置は CodeMirror が写す`、本文は spec の要約 + `Closes #49`、末尾 `🤖 Generated with [Claude Code](https://claude.com/claude-code)`）。squash merge
- issue #66（複数カーソル）と #67（CodeMirror を選んだ理由）: #67 は Task 8 の design.md で答えているので PR に `Closes #67`。#66 はこの枝では触らない
- マージ後、枝と worktree を消す（worktree の中のセッションは自分を消せないので main 側で）
