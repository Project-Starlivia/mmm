## Naming
命名は、何をするにも最も重要かつ困難な作業である。
名前とは、そのものを表す最もシンプルかつジェネリックな概念であり、そのもの自身でもある。

これはファイル名・関数名・変数名(・クラス名)・コミット名・コメントやdocsの固有名詞など、ありとあらゆる場面で必要とされ、そのすべて対して一切怠ってはならない。

### Quality
0からでも万人が現状と判断に認知負荷をかけずに納得ができる状態を理想として作業する。
記憶という曖昧なもので覆い隠さず、Repositoryの今を持ってプロダクトを表す。複雑さではなく簡潔と合理をもって伝える。

そのために過不足なくシンプルであり、欲求に最大限ジェネリックであることを徹底する必要がある。

## Automatic
我々は怠け者である。同じことの繰り返しを嫌い、無駄に徹底的に抗う。

共通化・構造化を駆使し、よりシンプルな解法がないかを常に模索し続ける。
AIの推論力・スピードは優れているが、決まった動作を繰り返すだけならスクリプトであるべきである。

## Coding
実装時、エンジニアは常に今の最適を追い求めるべきである。肥大を許さず、今だけを根絶し、複雑さを恨むべきである。
設計は常に歪もうとし、実装とコメントは常に巨大化しようとする。常に求められていることを構造的に捉え、可能な限りシンプルかつジェネリックに構築し、可能な限りシンプルかつジェネリックに命名・記述する。

負債を決して見逃さず、徹底して厳正なコードを追い求める。

# Git

## Commit
基本的に[Semantic Commit Message](https://gist.github.com/joshbuchea/6f47e86d2510bce28f8e7f42ae84c716)を採用する。
- フォーマット: <Type>: <Emoji> #<Issue Number> <Title>
- 例: feat: ✨ #123 ログイン機能の実装をする
- TypeとTitleは必須
- Issue Numberは強く推奨
- Emojiは任意（なんかかわいいのであったほうが好き）
- Description（スリーライン）は任意

## Branch・Worktree
並行して作業する際は、ブランチを切る。
ブランチ名は、<type>/<name>の形式とする。
- type — `feat`/`fix`/`docs`/`style`/`refactor`/`perf`/`test`のみ
- name — lowerの`kebab-case`のみシンプルに。IssueNumberなど余計なものはつけない。

main以外のブランチで作業するときは、`.worktrees/<ブランチ名>`のワークツリーで作業する。ローカルで並列もできるように。

ブランチは作業が終了したときにはリモート・ローカルともに、Worktreeと合わせて破棄する。履歴の残りとトレードだが、常にクリーンな状態を保つ。作業中の苦闘のHowは不要。WhyをDocsに残せば良い。

## Merge
PRのマージにはSquash Mergeを使用する。具体的な作業ログとはトレードだが、機能ごとの綺麗なコミットログを残す。理由は上記と同じく、履歴の残りとトレードだが、常にクリーンな状態を保つ意図。

### Conflict
コンフリクト時は、Rebaseではなく対象ブランチへのMergeを使用する。Gitの履歴を変に捻じ曲げない。
Claude CodeはRebaseを使用しがちなので許さないこと。

# Github

## Stacked Pull Requests
大きな機能・変更に際し、さらに細かなブランチを作って進捗を管理する場合、Stacked PRを使用する。純粋に視認性が高いため。またこの仕組みに限りRebaseを許容する。

https://docs.github.com/en/pull-requests/how-tos/stacked-pull-requests
